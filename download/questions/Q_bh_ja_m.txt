{ id: 'bh-ja-m2001', game: 'bug-hunter', language: 'java', difficulty: 'medium',
    code: 'public class Resource {\n  private Connection conn;\n  public void open() {\n    conn = DriverManager.getConnection(url);\n  }\n  // no close method\n}',
    options: ['Missing close() method and AutoCloseable implementation — resource leak', 'Add a finalize() method', 'Make Connection static', 'Use try-with-resources pattern'],
    correctIndex: 0, explanation: 'The class opens a connection but never closes it, causing resource leaks. Implement AutoCloseable and a close() method, or use try-with-resources.' },
{ id: 'bh-ja-m2002', game: 'bug-hunter', language: 'java', difficulty: 'medium',
    code: 'public class MathUtils {\n  public static int divide(int a, int b) {\n    return a / b;\n  }\n}',
    options: ['No check for b == 0 — throws ArithmeticException for division by zero', 'Add try-catch inside the method', 'Change int to double', 'Return 0 when b is 0'],
    correctIndex: 0, explanation: 'Integer division by zero throws ArithmeticException. Add validation: if (b == 0) throw new IllegalArgumentException("Divisor cannot be zero") or return a meaningful default.' },
{ id: 'bh-ja-m2003', game: 'bug-hunter', language: 'java', difficulty: 'medium',
    code: 'List<String> filtered = new ArrayList<>();\nfor (String item : items) {\n  if (item.startsWith("A")) {\n    filtered = item;\n  }\n}',
    options: ['Use filtered.add(item) instead of filtered = item — type mismatch', 'Change List to String', 'Use filtered.set(item)', 'Add cast to String'],
    correctIndex: 0, explanation: 'filtered is a List<String>, but item is a String. Assigning filtered = item causes a type mismatch. Use filtered.add(item) to add matching items.' },
{ id: 'bh-ja-m2004', game: 'bug-hunter', language: 'java', difficulty: 'medium',
    code: 'public class Config {\n  private static Config instance;\n  private Config() {}\n  public static Config getInstance() {\n    if (instance == null) {\n      instance = new Config();\n    }\n    return instance;\n  }\n  public void reload() {\n    instance = new Config();\n  }\n}',
    options: ['reload() breaks the singleton contract — it replaces the instance', 'This is a correct singleton', 'Make reload() private', 'Add synchronized to reload()'],
    correctIndex: 0, explanation: 'reload() creates a new instance, breaking the singleton pattern. Other code holding the old reference will use the stale instance. Instead, reload the config data without creating a new object.' },
{ id: 'bh-ja-m2005', game: 'bug-hunter', language: 'java', difficulty: 'medium',
    code: 'public List<User> getUsers() {\n  return users;\n}\n\n// External code:\ngetUsers().clear();',
    options: ['Return a defensive copy: return new ArrayList<>(users) or Collections.unmodifiableList(users)', 'This is correct', 'Make users final', 'Add synchronized keyword'],
    correctIndex: 0, explanation: 'Returning the internal list directly allows external code to modify it. Return a copy or an unmodifiable view to protect internal state.' },
{ id: 'bh-ja-m2006', game: 'bug-hunter', language: 'java', difficulty: 'medium',
    code: 'public class Stack<E> {\n  private E[] elements;\n  public Stack() {\n    elements = new E[10];\n  }\n}',
    options: ['Cannot create generic arrays — use (E[]) new Object[10] with suppression', 'Change E[] to Object[]', 'Use ArrayList<E> instead', 'Both A and C are valid approaches'],
    correctIndex: 3, explanation: 'Java does not allow creating generic arrays (new E[10]). Use (E[]) new Object[10] with @SuppressWarnings, or preferably use ArrayList<E> which handles generics correctly.' },
{ id: 'bh-ja-m2007', game: 'bug-hunter', language: 'java', difficulty: 'medium',
    code: 'public boolean equals(MyClass other) {\n  return this.value == other.value;\n}',
    options: ['Must override equals(Object) not equals(MyClass) to follow the contract', 'Add hashCode() method', 'Use .equals() for value comparison', 'Make the method static'],
    correctIndex: 0, explanation: 'Overloading equals(MyClass) instead of overriding equals(Object) means the method will not be called by collections. Always use @Override public boolean equals(Object obj).' },
{ id: 'bh-ja-m2008', game: 'bug-hunter', language: 'java', difficulty: 'medium',
    code: 'public class Timer {\n  private static long startTime;\n  public static void start() { startTime = System.currentTimeMillis(); }\n  public static long elapsed() { return System.currentTimeMillis() - startTime; }\n}',
    options: ['startTime defaults to 0 — elapsed() returns wrong value if called before start()', 'This is correct', 'Use nanoTime() instead', 'Make methods non-static'],
    correctIndex: 0, explanation: 'If elapsed() is called before start(), startTime is 0, returning an enormous value. Add a boolean flag or use Long that can be null to detect uninitialized state.' },
{ id: 'bh-ja-m2009', game: 'bug-hunter', language: 'java', difficulty: 'medium',
    code: 'Map<String, Integer> wordCount = new HashMap<>();\nfor (String word : words) {\n  wordCount.put(word, wordCount.get(word) + 1);\n}',
    options: ['get() returns null for missing keys, causing NullPointerException — use getOrDefault(word, 0) + 1', 'Use putIfAbsent first', 'Use merge() method', 'Both A and C are valid fixes'],
    correctIndex: 3, explanation: 'wordCount.get(word) returns null for new keys. Adding 1 to null throws NullPointerException. Use getOrDefault(word, 0) + 1 or merge(word, 1, Integer::sum).' },
{ id: 'bh-ja-m2010', game: 'bug-hunter', language: 'java', difficulty: 'medium',
    code: 'public class Pair<A, B> {\n  private A first;\n  private B second;\n  public boolean equals(Object o) {\n    Pair<A, B> other = (Pair<A, B>) o;\n    return first.equals(other.first) && second.equals(other.second);\n  }\n}',
    options: ['No null check on first/second — use Objects.equals(first, other.first)', 'Add hashCode() method', 'Check instanceof before casting', 'Both A and C — null safety and type safety'],
    correctIndex: 3, explanation: 'If first or second is null, .equals() throws NullPointerException. Also, the cast fails for non-Pair objects. Use Objects.equals() for null safety and instanceof check before casting.' },
{ id: 'bh-ja-m2011', game: 'bug-hunter', language: 'java', difficulty: 'medium',
    code: 'class Base {\n  void process(List<String> items) { }\n}\nclass Sub extends Base {\n  void process(List<Integer> items) { }\n}',
    options: ['Type erasure makes both signatures process(List) — does not actually override', 'This is correct overloading', 'Add @Override annotation', 'Change List to ArrayList'],
    correctIndex: 0, explanation: 'After type erasure, both methods have the same signature process(List). The Sub method does not override Base — it attempts to, causing a compile error. Use different method names.' },
{ id: 'bh-ja-m2012', game: 'bug-hunter', language: 'java', difficulty: 'medium',
    code: 'public class Cache<K, V> {\n  private Map<K, V> map = new HashMap<>();\n  public void put(K key, V value) {\n    map.put(key, value);\n  }\n  public V get(K key) {\n    return map.get(key);\n  }\n  public int size() {\n    return map.size();\n  }\n  public void clear() {\n    map = new HashMap<>();\n  }\n}',
    options: ['Not thread-safe — use ConcurrentHashMap or synchronization', 'This is a correct cache implementation', 'Add volatile to map', 'Use WeakHashMap instead'],
    correctIndex: 0, explanation: 'HashMap is not thread-safe. Concurrent access from multiple threads can cause data corruption or infinite loops during resizing. Use ConcurrentHashMap for thread-safe caching.' },
{ id: 'bh-ja-m2013', game: 'bug-hunter', language: 'java', difficulty: 'medium',
    code: 'public class Builder {\n  private String name;\n  private int age;\n  public Builder name(String name) {\n    this.name = name;\n    return this;\n  }\n  public Builder age(int age) {\n    this.age = age;\n    return new Builder();\n  }\n}',
    options: ['age() returns new Builder() instead of this — breaks method chaining', 'This is correct builder pattern', 'Make methods static', 'Add validation logic'],
    correctIndex: 0, explanation: 'age() creates a new Builder instead of returning this, losing the name that was already set. Builder methods should always return this for proper chaining.' },
{ id: 'bh-ja-m2014', game: 'bug-hunter', language: 'java', difficulty: 'medium',
    code: 'List<Integer> numbers = Arrays.asList(1, 2, 3);\nnumbers.stream()\n  .filter(n -> n > 1)\n  .map(n -> n * 2);\nSystem.out.println(numbers);',
    options: ['Stream is not collected — add .collect(Collectors.toList()) to get results', 'This modifies the original list', 'The stream is lazy — it will execute on print', 'Use parallelStream() instead'],
    correctIndex: 0, explanation: 'Stream operations are lazy and return new streams. Without a terminal operation like collect(), the pipeline does nothing. Add .collect(Collectors.toList()) to execute and capture results.' },
{ id: 'bh-ja-m2015', game: 'bug-hunter', language: 'java', difficulty: 'medium',
    code: 'public class Service {\n  private final List<Listener> listeners = new ArrayList<>();\n  public void addListener(Listener l) {\n    listeners.add(l);\n  }\n  public void removeListener(Listener l) {\n    listeners.remove(l);\n  }\n  public void notifyAll(String msg) {\n    for (Listener l : listeners) {\n      l.onMessage(msg);\n    }\n  }\n}',
    options: ['ConcurrentModificationException if listener removes itself during notification — use CopyOnWriteArrayList or iterate over a copy', 'This is correct', 'Make listeners synchronized', 'Use Vector instead'],
    correctIndex: 0, explanation: 'If a listener removes itself during onMessage(), the for-each loop throws ConcurrentModificationException. Use CopyOnWriteArrayList or iterate over a snapshot: new ArrayList<>(listeners).' },
{ id: 'bh-ja-m2016', game: 'bug-hunter', language: 'java', difficulty: 'medium',
    code: 'public class Config {\n  private Map<String, String> defaults = new HashMap<>();\n  private Map<String, String> overrides = new HashMap<>();\n  \n  public String get(String key) {\n    return overrides.getOrDefault(key, defaults.get(key));\n  }\n}',
    options: ['defaults.get(key) can return null even with getOrDefault — use defaults.getOrDefault(key, null) and handle null explicitly', 'This is correct', 'Use merge() for combining', 'Add containsKey check first'],
    correctIndex: 0, explanation: 'If key is not in overrides, getOrDefault falls back to defaults.get(key) which can return null. The getOrDefault on overrides only handles missing keys, not null values in defaults.' },
{ id: 'bh-ja-m2017', game: 'bug-hunter', language: 'java', difficulty: 'medium',
    code: 'public class MathHelper {\n  public static double average(double... values) {\n    double sum = 0;\n    for (double v : values) sum += v;\n    return sum / values.length;\n  }\n}',
    options: ['No check for empty varargs — values.length is 0 causes NaN', 'This always works', 'Use DoubleStream instead', 'Add throws clause'],
    correctIndex: 0, explanation: 'If called with no arguments, values.length is 0, and sum / 0 returns NaN. Add a check: if (values.length == 0) throw new IllegalArgumentException().' },
{ id: 'bh-ja-m2018', game: 'bug-hunter', language: 'java', difficulty: 'medium',
    code: 'public class TreeNode {\n  TreeNode left, right;\n  int value;\n  public TreeNode(int value) {\n    this.value = value;\n  }\n  public TreeNode copy() {\n    TreeNode node = new TreeNode(value);\n    node.left = left;\n    node.right = right;\n    return node;\n  }\n}',
    options: ['copy() is shallow — left and right still reference original subtrees. Use deep copy with recursive calls', 'This is a correct deep copy', 'Use clone() instead', 'Make TreeNode immutable'],
    correctIndex: 0, explanation: 'The copy() method only creates a new root node but shares left and right with the original. Modifying the copy also modifies the original tree. Implement deep copy recursively.' },
{ id: 'bh-ja-m2019', game: 'bug-hunter', language: 'java', difficulty: 'medium',
    code: 'public class Calculator {\n  private double result;\n  public Calculator add(double x) { result += x; return this; }\n  public Calculator subtract(double x) { result -= x; return this; }\n  public double getResult() { return result; }\n}',
    options: ['Calling getResult() multiple times returns the accumulated result — no reset method', 'This is a correct fluent API', 'Add @Override annotations', 'Make result static'],
    correctIndex: 0, explanation: 'There is no way to reset result between calculations. After add(5).add(3), getResult() returns 8, and subsequent calls still return 8. Add a reset() method.' },
{ id: 'bh-ja-m2020', game: 'bug-hunter', language: 'java', difficulty: 'medium',
    code: 'public <T extends Comparable<T>> T max(T a, T b) {\n  return (a.compareTo(b) > 0) ? a : b;\n}\n\npublic void test() {\n  String result = max("apple", 42);\n}',
    options: ['Type safety violated — 42 is Integer not String, but generic erasure allows it at compile time in some cases', 'This works correctly', 'Add bounded type parameter', 'Use T extends Number'],
    correctIndex: 0, explanation: 'The generic method should prevent mixing types, but raw types or incorrect casting can bypass the check. This specific call would fail compilation since Integer is not T extends Comparable<T> where T is String.' },
{ id: 'bh-ja-m2021', game: 'bug-hunter', language: 'java', difficulty: 'medium',
    code: 'public class FileReader {\n  public String read(String path) {\n    BufferedReader reader = new BufferedReader(new java.io.FileReader(path));\n    String line = reader.readLine();\n    return line;\n  }\n}',
    options: ['Reader is never closed — use try-with-resources to ensure cleanup', 'This is correct', 'Add finally block with close()', 'Use Scanner instead'],
    correctIndex: 0, explanation: 'The BufferedReader is never closed, causing a resource leak. Use try-with-resources: try (BufferedReader reader = ...) to ensure the reader is always closed.' },
{ id: 'bh-ja-m2022', game: 'bug-hunter', language: 'java', difficulty: 'medium',
    code: 'ScheduledExecutorService scheduler = Executors.newScheduledThreadPool(1);\nscheduler.scheduleAtFixedRate(task, 0, 1, TimeUnit.SECONDS);\n// application shutdown',
    options: ['Scheduler is never shut down — call scheduler.shutdown() to prevent thread leak', 'This is correct', 'Use Executors.newSingleThreadExecutor()', 'Add daemon thread flag'],
    correctIndex: 0, explanation: 'The ScheduledExecutorService creates non-daemon threads that prevent JVM shutdown. Always call scheduler.shutdown() or scheduler.shutdownNow() when done.' },
{ id: 'bh-ja-m2023', game: 'bug-hunter', language: 'java', difficulty: 'medium',
    code: 'public class Counter {\n  private int count = 0;\n  public void increment() { count++; }\n  public int getCount() { return count; }\n}',
    options: ['count++ is not atomic — use AtomicInteger or synchronized for thread safety', 'This is correct for single-threaded use', 'Make count volatile', 'Use AtomicLong instead'],
    correctIndex: 0, explanation: 'count++ is not an atomic operation (read-modify-write). In multi-threaded environments, increments can be lost. Use AtomicInteger.incrementAndGet() or synchronized.' },
{ id: 'bh-ja-m2024', game: 'bug-hunter', language: 'java', difficulty: 'medium',
    code: 'public class DateRange {\n  private Date start, end;\n  public DateRange(Date start, Date end) {\n    this.start = start;\n    this.end = end;\n  }\n  public boolean contains(Date d) {\n    return d.after(start) && d.before(end);\n  }\n}',
    options: ['Date is mutable — external code can modify start/end. Use defensive copies in constructor and getters', 'This is correct', 'Use LocalDateTime instead', 'Make fields final'],
    correctIndex: 3, explanation: 'Date objects are mutable, so callers can modify the internal state. Use defensive copies, prefer LocalDateTime (immutable), or make fields final with defensive copying.' },
{ id: 'bh-ja-m2025', game: 'bug-hunter', language: 'java', difficulty: 'medium',
    code: 'public class PriorityQueue<T extends Comparable<T>> {\n  private List<T> items = new ArrayList<>();\n  public void push(T item) { items.add(item); }\n  public T pop() { return items.remove(0); }\n}',
    options: ['This is a FIFO queue, not a priority queue — items.remove(0) removes first, not highest priority', 'This is a correct priority queue', 'Use Collections.sort() before pop', 'Add heap-based ordering'],
    correctIndex: 0, explanation: 'Adding to the end and removing from the front makes this a regular FIFO queue, not a priority queue. A priority queue needs to order by priority, typically using a heap data structure.' },
{ id: 'bh-ja-m2026', game: 'bug-hunter', language: 'java', difficulty: 'medium',
    code: 'public class IdGenerator {\n  private static long lastId = System.currentTimeMillis();\n  public static synchronized long nextId() {\n    return lastId++;\n  }\n}',
    options: ['Clock-dependent IDs can collide if system time changes — use AtomicLong.incrementAndGet() instead', 'This is correct', 'Remove synchronized keyword', 'Use UUID instead'],
    correctIndex: 0, explanation: 'Using system time as a base ID is fragile — clock adjustments can cause collisions. Use AtomicLong starting from 0 or 1, or UUID for guaranteed uniqueness.' },
{ id: 'bh-ja-m2027', game: 'bug-hunter', language: 'java', difficulty: 'medium',
    code: 'List<String> result = new ArrayList<>();\nitems.stream()\n  .filter(s -> s.length() > 3)\n  .forEach(s -> result.add(s));',
    options: ['Use .collect(Collectors.toList()) instead of forEach with add — not thread-safe and less idiomatic', 'This works correctly', 'Use parallelStream()', 'Add synchronized to add'],
    correctIndex: 0, explanation: 'Using forEach with external state mutation is an anti-pattern. It fails with parallel streams and is less readable. Use .collect(Collectors.toList()) for proper functional style.' },
{ id: 'bh-ja-m2028', game: 'bug-hunter', language: 'java', difficulty: 'medium',
    code: 'public class LRUCache<K, V> extends LinkedHashMap<K, V> {\n  private final int maxSize;\n  public LRUCache(int maxSize) {\n    this.maxSize = maxSize;\n  }\n  @Override\n  protected boolean removeEldestEntry(Map.Entry<K, V> eldest) {\n    return size() > maxSize;\n  }\n}',
    options: ['Missing accessOrder=true in super constructor: super(maxSize, 0.75f, true)', 'This is a correct LRU cache', 'Use HashMap instead', 'Add synchronized methods'],
    correctIndex: 0, explanation: 'LinkedHashMap defaults to insertion order, not access order. Without accessOrder=true, recently accessed entries do not move to the end. Call super(maxSize, 0.75f, true).' },
{ id: 'bh-ja-m2029', game: 'bug-hunter', language: 'java', difficulty: 'medium',
    code: 'public class App {\n  private static final Logger logger = Logger.getLogger(App.class.getName());\n  public void process() {\n    logger.info("Processing started");\n    try {\n      doWork();\n    } catch (Exception e) {\n      logger.info("Error: " + e);\n    }\n  }\n}',
    options: ['Use logger.log(Level.SEVERE, "Error", e) to log full stack trace, not info with toString', 'This is correct error handling', 'Use System.err instead', 'Remove the try-catch'],
    correctIndex: 0, explanation: 'logger.info("Error: " + e) logs at INFO level and only shows the exception message, not the stack trace. Use logger.log(Level.SEVERE, "Error", e) for proper error logging with full stack trace.' },
{ id: 'bh-ja-m2030', game: 'bug-hunter', language: 'java', difficulty: 'medium',
    code: 'public class EventBus {\n  private Map<Class<?>, List<Consumer<?>>> handlers = new HashMap<>();\n  public <T> void register(Class<T> type, Consumer<T> handler) {\n    handlers.computeIfAbsent(type, k -> new ArrayList<>()).add(handler);\n  }\n  @SuppressWarnings("unchecked")\n  public <T> void publish(T event) {\n    List<Consumer<?>> list = handlers.get(event.getClass());\n    if (list != null) {\n      for (Consumer<?> h : list) {\n        ((Consumer<T>) h).accept(event);\n      }\n    }\n  }\n}',
    options: ['The unchecked cast is unsafe for subtypes — event.getClass() may not match registered supertype', 'This is correct', 'Use weak references for handlers', 'Add thread safety'],
    correctIndex: 0, explanation: 'If you register a handler for Number and publish an Integer, event.getClass() is Integer but the handler is registered under Number. Use getAssignableFrom() or register both types.' },
{ id: 'bh-ja-m2031', game: 'bug-hunter', language: 'java', difficulty: 'medium',
    code: 'Optional<String> name = getName();\nif (name.isPresent()) {\n  return name.get().toUpperCase();\n} else {\n  return "UNKNOWN";\n}',
    options: ['Use name.map(String::toUpperCase).orElse("UNKNOWN") for cleaner Optional usage', 'This is correct but verbose', 'Use name.get() directly', 'Change Optional to null check'],
    correctIndex: 0, explanation: 'While the isPresent/get pattern works, it defeats the purpose of Optional. The idiomatic approach is name.map(String::toUpperCase).orElse("UNKNOWN"), which is cleaner and safer.' },
{ id: 'bh-ja-m2032', game: 'bug-hunter', language: 'java', difficulty: 'medium',
    code: 'public class TextFile {\n  private List<String> lines;\n  public TextFile(String path) throws IOException {\n    lines = Files.readAllLines(Paths.get(path));\n  }\n  public void append(String line) throws IOException {\n    lines.add(line);\n    Files.write(Paths.get(path), lines); // path not stored\n  }\n}',
    options: ['path is not stored as a field — the second Files.write() cannot access it', 'This is correct', 'Store path as instance field', 'Use FileWriter instead'],
    correctIndex: 2, explanation: 'The constructor parameter path is local — it is not saved. The append() method cannot access path for Files.write(). Add private String path as an instance field and store it in the constructor.' },
{ id: 'bh-ja-m2033', game: 'bug-hunter', language: 'java', difficulty: 'medium',
    code: 'public class RegexValidator {\n  private Pattern pattern;\n  public RegexValidator(String regex) {\n    pattern = Pattern.compile(regex);\n  }\n  public boolean isValid(String input) {\n    return pattern.matcher(input).matches();\n  }\n}',
    options: ['This is a correct and efficient regex validator — Pattern is compiled once', 'Pattern should be compiled each time', 'Use String.matches() instead', 'Add null check for input'],
    correctIndex: 0, explanation: 'Compiling the Pattern once in the constructor is the correct approach. String.matches() recompiles the pattern every call. This implementation is efficient and correct.' },
{ id: 'bh-ja-m2034', game: 'bug-hunter', language: 'java', difficulty: 'medium',
    code: 'public class ThreadPool {\n  private ExecutorService executor = Executors.newFixedThreadPool(4);\n  public <T> Future<T> submit(Callable<T> task) {\n    return executor.submit(task);\n  }\n}',
    options: ['No shutdown method — executor threads prevent JVM exit. Add shutdown() and shutdownNow()', 'This is correct', 'Make executor volatile', 'Use cached thread pool instead'],
    correctIndex: 0, explanation: 'The ExecutorService is never shut down, causing a resource leak. Non-daemon threads keep the JVM alive. Add a shutdown() method that calls executor.shutdown().' },
{ id: 'bh-ja-m2035', game: 'bug-hunter', language: 'java', difficulty: 'medium',
    code: 'public class DeepCopy {\n  public static <T> T copy(T obj) {\n    ByteArrayOutputStream bos = new ByteArrayOutputStream();\n    ObjectOutputStream oos = new ObjectOutputStream(bos);\n    oos.writeObject(obj);\n    ByteArrayInputStream bis = new ByteArrayInputStream(bos.toByteArray());\n    ObjectInputStream ois = new ObjectInputStream(bis);\n    return (T) ois.readObject();\n  }\n}',
    options: ['Streams are never closed — use try-with-resources. Also, T must implement Serializable', 'This is correct', 'Add throws declarations', 'Use clone() instead'],
    correctIndex: 0, explanation: 'None of the streams are closed, causing resource leaks. Wrap in try-with-resources. Also, the generic type T must implement Serializable, but there is no bound enforcing this.' },
{ id: 'bh-ja-m2036', game: 'bug-hunter', language: 'java', difficulty: 'medium',
    code: 'public class RateLimiter {\n  private long lastCall = 0;\n  private long interval;\n  public RateLimiter(long intervalMs) {\n    this.interval = intervalMs;\n  }\n  public boolean allow() {\n    long now = System.currentTimeMillis();\n    if (now - lastCall >= interval) {\n      lastCall = now;\n      return true;\n    }\n    return false;\n  }\n}',
    options: ['Not thread-safe — multiple threads can read lastCall simultaneously. Use AtomicLong or synchronized', 'This is correct for single thread', 'Use nanoTime() instead', 'Add volatile to lastCall'],
    correctIndex: 0, explanation: 'The check-then-update pattern is a race condition. Two threads can both read the same lastCall value and both return true. Use AtomicLong.compareAndSet() or synchronized.' },
{ id: 'bh-ja-m2037', game: 'bug-hunter', language: 'java', difficulty: 'medium',
    code: 'public class CSVParser {\n  public static List<String[]> parse(String line) {\n    return Arrays.stream(line.split(","))\n      .map(s -> s.split(":"))\n      .collect(Collectors.toList());\n  }\n}',
    options: ['The method name says CSV but splits on colon too — misleading. Also does not handle quoted fields with commas', 'This is correct CSV parsing', 'Use OpenCSV library', 'Both the name issue and quote handling are problems'],
    correctIndex: 3, explanation: 'CSV parsing must handle quoted fields containing commas (e.g., "Smith, John",25). The naive split approach breaks on such data. Also, the secondary split on : suggests key:value pairs, not CSV.' },
{ id: 'bh-ja-m2038', game: 'bug-hunter', language: 'java', difficulty: 'medium',
    code: 'public class Matrix {\n  private double[][] data;\n  public Matrix(int rows, int cols) {\n    data = new double[rows][cols];\n  }\n  public Matrix multiply(Matrix other) {\n    Matrix result = new Matrix(data.length, other.data[0].length);\n    for (int i = 0; i < data.length; i++)\n      for (int j = 0; j < other.data[0].length; j++)\n        for (int k = 0; k < data[0].length; k++)\n          result.data[i][j] += data[i][k] * other.data[k][j];\n    return result;\n  }\n}',
    options: ['No dimension validation — if data[0].length != other.data.length, array index out of bounds', 'This is correct', 'Add transpose method', 'Use BigInteger for precision'],
    correctIndex: 0, explanation: 'Matrix multiplication requires that the number of columns in the first matrix equals the number of rows in the second. Without validation, incompatible dimensions cause ArrayIndexOutOfBoundsException.' },
{ id: 'bh-ja-m2039', game: 'bug-hunter', language: 'java', difficulty: 'medium',
    code: 'public class StringUtils {\n  public static boolean isBlank(String s) {\n    return s == null || s.length() == 0;\n  }\n}',
    options: ['Does not handle whitespace-only strings — add s.trim().isEmpty()', 'This is correct', 'Use s.equals("")', 'Add s.isBlank() check'],
    correctIndex: 0, explanation: 'A string of spaces "   " is not null and has length > 0, but should be considered blank. Use s == null || s.trim().isEmpty() to handle whitespace.' },
{ id: 'bh-ja-m2040', game: 'bug-hunter', language: 'java', difficulty: 'medium',
    code: 'public class App {\n  private static final ExecutorService POOL = Executors.newCachedThreadPool();\n  public static void main(String[] args) {\n    for (int i = 0; i < 10000; i++) {\n      POOL.submit(() -> { while(true) Thread.sleep(1000); });\n    }\n  }\n}',
    options: ['CachedThreadPool creates unbounded threads — use FixedThreadPool to limit concurrency', 'This is correct', 'Add thread priority', 'Use ForkJoinPool instead'],
    correctIndex: 0, explanation: 'CachedThreadPool creates a new thread for each task when existing threads are busy. 10,000 tasks = 10,000 threads = OutOfMemoryError. Use FixedThreadPool with a reasonable limit.' },
{ id: 'bh-ja-m2041', game: 'bug-hunter', language: 'java', difficulty: 'medium',
    code: 'public class Settings {\n  private Properties props = new Properties();\n  public void load() {\n    InputStream is = new FileInputStream("config.properties");\n    props.load(is);\n  }\n}',
    options: ['InputStream never closed and FileNotFoundException not handled — use try-with-resources', 'This is correct', 'Add throws IOException', 'Use FileReader instead'],
    correctIndex: 0, explanation: 'The InputStream is never closed, and the method does not declare or handle FileNotFoundException. Use try-with-resources: try (InputStream is = new FileInputStream(...)) { props.load(is); }.' },
{ id: 'bh-ja-m2042', game: 'bug-hunter', language: 'java', difficulty: 'medium',
    code: 'public class CircularBuffer<T> {\n  private T[] buffer;\n  private int head = 0, tail = 0;\n  public CircularBuffer(int size) {\n    buffer = (T[]) new Object[size];\n  }\n  public void add(T item) {\n    buffer[tail] = item;\n    tail = (tail + 1) % buffer.length;\n  }\n  public T remove() {\n    T item = buffer[head];\n    head = (head + 1) % buffer.length;\n    return item;\n  }\n}',
    options: ['No empty/full detection — add isEmpty() and isFull() checks, or track count', 'This is a correct circular buffer', 'Use ArrayDeque instead', 'Make buffer volatile'],
    correctIndex: 0, explanation: 'Without tracking the number of elements, you cannot distinguish between empty and full states (head == tail means both). Add a count field or a boolean flag for full state.' },
{ id: 'bh-ja-m2043', game: 'bug-hunter', language: 'java', difficulty: 'medium',
    code: 'public class Config {\n  private static Properties props;\n  static {\n    props = new Properties();\n    props.setProperty("timeout", "30");\n  }\n  public static int getTimeout() {\n    return (Integer) props.get("timeout");\n  }\n}',
    options: ['props.get("timeout") returns String, not Integer — use Integer.parseInt(props.getProperty("timeout"))', 'This is correct', 'Change cast to (int)', 'Use props.getInt()'],
    correctIndex: 0, explanation: 'Properties stores values as Strings. Casting a String to Integer throws ClassCastException. Use Integer.parseInt(props.getProperty("timeout")) for proper conversion.' },
{ id: 'bh-ja-m2044', game: 'bug-hunter', language: 'java', difficulty: 'medium',
    code: 'public class ObservableList<T> extends ArrayList<T> {\n  private List<Consumer<T>> listeners = new ArrayList<>();\n  @Override\n  public boolean add(T item) {\n    notifyListeners(item);\n    return super.add(item);\n  }\n  private void notifyListeners(T item) {\n    for (Consumer<T> l : listeners) l.accept(item);\n  }\n}',
    options: ['Notification fires before item is actually added — move notifyListeners after super.add()', 'This is correct', 'Use CopyOnWriteArrayList for listeners', 'Add remove notification too'],
    correctIndex: 0, explanation: 'Listeners are notified before the item is added to the list. If a listener queries the list, it will not contain the new item yet. Move the notification after super.add().' },
{ id: 'bh-ja-m2045', game: 'bug-hunter', language: 'java', difficulty: 'medium',
    code: 'public class BloomFilter {\n  private BitSet bits;\n  private int size;\n  public BloomFilter(int size) {\n    this.size = size;\n    bits = new BitSet(size);\n  }\n  public void add(String item) {\n    bits.set(hash1(item) % size);\n    bits.set(hash2(item) % size);\n  }\n  public boolean contains(String item) {\n    return bits.get(hash1(item) % size) && bits.get(hash2(item) % size);\n  }\n  private int hash1(String s) { return s.hashCode(); }\n  private int hash2(String s) { return s.length() * 31 + s.charAt(0); }\n}',
    options: ['hash2 can produce negative values — use Math.abs() before modulo, and hash quality is poor', 'This is a correct bloom filter', 'Use more hash functions', 'Add remove() method'],
    correctIndex: 0, explanation: 'hashCode() and the custom hash2 can return negative integers. Negative % size produces negative indices, causing IndexOutOfBoundsException. Use Math.abs() or & (size - 1) for power-of-two sizes.' },
