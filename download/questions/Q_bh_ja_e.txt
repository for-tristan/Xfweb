{ id: 'bh-ja-e2001', game: 'bug-hunter', language: 'java', difficulty: 'easy',
    code: 'public class Main {\n  public static void main(String[] args) {\n    int x = 5;\n    if (x = 10) {\n      System.out.println("ten");\n    }\n  }\n}',
    options: ['Change = to == for comparison', 'Change int to boolean', 'Add a cast to the condition', 'Change x to boolean type'],
    correctIndex: 0, explanation: 'In Java, = is assignment and == is comparison. x = 10 assigns 10 to x. Since int cannot be used as boolean, this causes a compilation error. Use x == 10.' },
{ id: 'bh-ja-e2002', game: 'bug-hunter', language: 'java', difficulty: 'easy',
    code: 'String s = "hello";\nif (s == "hello") {\n  System.out.println("match");\n}',
    options: ['Use s.equals("hello") instead of == for String comparison', 'This is correct', 'Use s.compareTo("hello")', 'Change to s === "hello"'],
    correctIndex: 0, explanation: '== compares object references, not content. Two String objects with the same text may not be the same reference. Always use .equals() for String comparison.' },
{ id: 'bh-ja-e2003', game: 'bug-hunter', language: 'java', difficulty: 'easy',
    code: 'int[] arr = {1, 2, 3};\nSystem.out.println(arr[3]);',
    options: ['Use arr[2] for the last element (index 2)', 'Use arr.length instead', 'Use arr[-1] for last element', 'Change array size to 4'],
    correctIndex: 0, explanation: 'Array indices go from 0 to length-1. arr[3] is out of bounds for an array of size 3. The last valid index is 2.' },
{ id: 'bh-ja-e2004', game: 'bug-hunter', language: 'java', difficulty: 'easy',
    code: 'public class Main {\n  public static void main(String[] args) {\n    System.out.println("Hello")\n  }\n}',
    options: ['Add semicolon after println statement', 'Change println to print', 'Remove the quotes', 'Add a return statement'],
    correctIndex: 0, explanation: 'Java requires semicolons after statements. System.out.println("Hello") needs a semicolon at the end.' },
{ id: 'bh-ja-e2005', game: 'bug-hunter', language: 'java', difficulty: 'easy',
    code: 'for (int i = 0; i <= 10; i++) {\n  System.out.println(i);\n}',
    options: ['This prints 0 to 10 — use i < 10 for 0 to 9', 'This prints 1 to 10', 'This is infinite loop', 'This prints only even numbers'],
    correctIndex: 0, explanation: 'Using <= includes 10, printing 11 numbers (0-10). If you want 0-9, use i < 10. Off-by-one is a very common loop error.' },
{ id: 'bh-ja-e2006', game: 'bug-hunter', language: 'java', difficulty: 'easy',
    code: 'public class Dog {\n  String name;\n  void Dog(String name) {\n    this.name = name;\n  }\n}',
    options: ['Remove void — constructors have no return type', 'Add static keyword', 'Add public modifier', 'Change to new Dog()'],
    correctIndex: 0, explanation: 'Constructors in Java must not have a return type, not even void. Remove void to make Dog(String name) a proper constructor.' },
{ id: 'bh-ja-e2007', game: 'bug-hunter', language: 'java', difficulty: 'easy',
    code: 'int x = 10;\nString s = x;',
    options: ['Use String.valueOf(x) or Integer.toString(x)', 'Add (String) cast', 'Change int to String', 'Use x.toString()'],
    correctIndex: 0, explanation: 'Java does not allow implicit conversion from int to String. Use String.valueOf(x), Integer.toString(x), or x + "" to convert.' },
{ id: 'bh-ja-e2008', game: 'bug-hunter', language: 'java', difficulty: 'easy',
    code: 'switch (day) {\n  case 1: System.out.println("Mon");\n  case 2: System.out.println("Tue");\n  case 3: System.out.println("Wed");\n}',
    options: ['Add break after each case to prevent fall-through', 'Add a default case only', 'Change switch to if-else', 'Remove the case keywords'],
    correctIndex: 0, explanation: 'Without break statements, execution falls through from the matching case to all subsequent cases. Add break after each case block.' },
{ id: 'bh-ja-e2009', game: 'bug-hunter', language: 'java', difficulty: 'easy',
    code: 'List<String> list = new ArrayList<>();\nlist.add("apple");\nlist.add("banana");\nfor (int i = 0; i <= list.size(); i++) {\n  System.out.println(list.get(i));\n}',
    options: ['Change <= to < to avoid IndexOutOfBoundsException', 'Use list.length() instead', 'Use for-each loop', 'Change get(i) to get(i-1)'],
    correctIndex: 0, explanation: 'list.size() returns 2, but valid indices are 0-1. Using <= causes IndexOutOfBoundsException at index 2. Use < instead.' },
{ id: 'bh-ja-e2010', game: 'bug-hunter', language: 'java', difficulty: 'easy',
    code: 'public class Calculator {\n  int add(int a, int b) {\n    a + b;\n  }\n}',
    options: ['Add return keyword: return a + b;', 'Change method to void', 'Add static keyword', 'Use System.out.println instead'],
    correctIndex: 0, explanation: 'The method declares int return type but has no return statement. a + b computes the value but discards it. Add return before a + b.' },
{ id: 'bh-ja-e2011', game: 'bug-hunter', language: 'java', difficulty: 'easy',
    code: 'try {\n  int result = 10 / 0;\n} catch (Exception e) {\n  System.out.println("Error");\n} catch (ArithmeticException e) {\n  System.out.println("Math error");\n}',
    options: ['ArithmeticException must come before Exception — reorder catch blocks', 'Remove the first catch block', 'Use multi-catch syntax', 'Change Exception to RuntimeException'],
    correctIndex: 0, explanation: 'Exception catches all exceptions including ArithmeticException, making the second catch unreachable. Place specific exceptions before general ones.' },
{ id: 'bh-ja-e2012', game: 'bug-hunter', language: 'java', difficulty: 'easy',
    code: 'String[] names = {"Alice", "Bob"};\nString name = names[1];\nname = "Charlie";\nSystem.out.println(names[1]);',
    options: ['names[1] is still "Bob" — Strings are immutable, name is a separate reference', 'names[1] is "Charlie"', 'This throws ArrayStoreException', 'Both references change'],
    correctIndex: 0, explanation: 'Assigning name = "Charlie" only changes the local variable, not the array element. Strings are immutable and name is a separate reference. To change the array: names[1] = "Charlie".' },
{ id: 'bh-ja-e2013', game: 'bug-hunter', language: 'java', difficulty: 'easy',
    code: 'public class Counter {\n  private static int count = 0;\n  public int getCount() {\n    return count;\n  }\n  public static void increment() {\n    count++;\n  }\n}',
    options: ['getCount() should be static since count is static, or count should be non-static', 'This is correct as is', 'increment() should be non-static', 'Remove private from count'],
    correctIndex: 0, explanation: 'While this compiles, accessing a static field through an instance method is misleading. Either make getCount() static too, or make count an instance variable if each object needs its own counter.' },
{ id: 'bh-ja-e2014', game: 'bug-hunter', language: 'java', difficulty: 'easy',
    code: 'double result = 10 / 3;\nSystem.out.println(result);',
    options: ['Use 10.0 / 3 or (double) 10 / 3 for decimal result', 'This prints 3.333...', 'Change double to float', 'Use Math.divide(10, 3)'],
    correctIndex: 0, explanation: '10 / 3 is integer division, resulting in 3, which is then cast to 3.0. Use 10.0 / 3 to get 3.333... by making one operand a double.' },
{ id: 'bh-ja-e2015', game: 'bug-hunter', language: 'java', difficulty: 'easy',
    code: 'String text = null;\nSystem.out.println(text.length());',
    options: ['Check for null first: if (text != null) text.length()', 'Use text.size() instead', 'Change null to empty string ""', 'Use String.length(text)'],
    correctIndex: 0, explanation: 'Calling .length() on null throws NullPointerException. Always check for null before calling methods on potentially null references.' },
{ id: 'bh-ja-e2016', game: 'bug-hunter', language: 'java', difficulty: 'easy',
    code: 'int[] nums = {5, 3, 1, 4, 2};\nArrays.sort(nums);\nSystem.out.println(nums[0]);',
    options: ['This works correctly — Arrays.sort sorts in-place, nums[0] is 1', 'nums[0] is still 5', 'Need to reassign: nums = Arrays.sort(nums)', 'Arrays.sort returns a new array'],
    correctIndex: 0, explanation: 'Arrays.sort() sorts the array in place, modifying the original. After sorting, nums becomes {1, 2, 3, 4, 5}, so nums[0] is 1. This is correct.' },
{ id: 'bh-ja-e2017', game: 'bug-hunter', language: 'java', difficulty: 'easy',
    code: 'Scanner sc = new Scanner(System.in);\nint age = sc.nextInt();\nString name = sc.nextLine();\nSystem.out.println(name);',
    options: ['nextInt() leaves newline — add sc.nextLine() after nextInt()', 'Use next() instead of nextLine()', 'Use readLine() instead', 'This works correctly'],
    correctIndex: 0, explanation: 'nextInt() reads the number but leaves the newline character in the buffer. nextLine() then reads just the empty newline. Add sc.nextLine() after nextInt() to consume the leftover newline.' },
{ id: 'bh-ja-e2018', game: 'bug-hunter', language: 'java', difficulty: 'easy',
    code: 'public class Shape {\n  double area;\n  abstract double getArea();\n}',
    options: ['Class must be declared abstract if it has abstract methods', 'Remove the abstract keyword', 'Make getArea() return area directly', 'Add static to getArea()'],
    correctIndex: 0, explanation: 'A class with abstract methods must be declared as abstract. Add abstract to the class declaration: public abstract class Shape.' },
{ id: 'bh-ja-e2019', game: 'bug-hunter', language: 'java', difficulty: 'easy',
    code: 'Map<String, Integer> map = new HashMap<>();\nmap.put("a", 1);\nmap.put("b", 2);\nfor (String key : map) {\n  System.out.println(key);\n}',
    options: ['Use map.keySet() or map.entrySet() to iterate', 'Use map.forEach() only', 'Use map.iterator()', 'Change HashMap to TreeMap'],
    correctIndex: 0, explanation: 'You cannot iterate a Map directly with for-each. Use map.keySet() for keys, map.values() for values, or map.entrySet() for key-value pairs.' },
{ id: 'bh-ja-e2020', game: 'bug-hunter', language: 'java', difficulty: 'easy',
    code: 'String s1 = "hello";\nString s2 = "he" + "llo";\nSystem.out.println(s1 == s2);',
    options: ['This prints true — compile-time string concatenation uses the string pool', 'This prints false', 'This depends on JVM', 'Throws an error'],
    correctIndex: 0, explanation: 'Strings computed from constant expressions at compile time are interned. "he" + "llo" becomes "hello" at compile time, so s1 and s2 reference the same pool object.' },
{ id: 'bh-ja-e2021', game: 'bug-hunter', language: 'java', difficulty: 'easy',
    code: 'public class Main {\n  static int x;\n  public static void main(String[] args) {\n    System.out.println(x);\n  }\n}',
    options: ['This prints 0 — static int defaults to 0 in Java', 'This prints null', 'This causes compilation error', 'This prints undefined'],
    correctIndex: 0, explanation: 'In Java, static and instance fields are initialized to default values. int defaults to 0, so this prints 0. Local variables, however, must be explicitly initialized.' },
{ id: 'bh-ja-e2022', game: 'bug-hunter', language: 'java', difficulty: 'easy',
    code: 'public class Test {\n  public static void main(String[] args) {\n    int x;\n    System.out.println(x);\n  }\n}',
    options: ['Local variable x must be initialized before use', 'This prints 0', 'This prints null', 'This prints garbage value'],
    correctIndex: 0, explanation: 'Unlike class fields, local variables in Java have no default value and must be initialized before use. The compiler will reject this code.' },
{ id: 'bh-ja-e2023', game: 'bug-hunter', language: 'java', difficulty: 'easy',
    code: 'int a = 5;\nint b = a++;\nSystem.out.println(b);',
    options: ['b is 5 — post-increment returns the old value first', 'b is 6', 'b is 0', 'This throws an error'],
    correctIndex: 0, explanation: 'a++ is post-increment: it returns the current value (5) then increments a to 6. So b gets 5. Use ++a (pre-increment) to assign 6.' },
{ id: 'bh-ja-e2024', game: 'bug-hunter', language: 'java', difficulty: 'easy',
    code: 'String s = "Hello World";\nSystem.out.println(s.charAt(11));',
    options: ['Use s.charAt(10) for last char or s.charAt(s.length()-1)', 'This prints "d"', 'This prints "l"', 'Use s[11] instead'],
    correctIndex: 0, explanation: '"Hello World" has 11 characters (indices 0-10). charAt(11) throws StringIndexOutOfBoundsException. Use charAt(s.length()-1) for the last character.' },
{ id: 'bh-ja-e2025', game: 'bug-hunter', language: 'java', difficulty: 'easy',
    code: 'ArrayList<String> list = new ArrayList<>();\nlist.add("A");\nlist.add("B");\nlist.remove("A");\nSystem.out.println(list);',
    options: ['This works correctly — remove("A") removes by object, prints [B]', 'This removes index A causing error', 'This does not remove anything', 'Remove returns false'],
    correctIndex: 0, explanation: 'list.remove("A") calls remove(Object), which removes the first occurrence of "A". The list becomes ["B"]. Be careful: list.remove(0) removes by index instead.' },
{ id: 'bh-ja-e2026', game: 'bug-hunter', language: 'java', difficulty: 'easy',
    code: 'if ("hello".equals("hello")) {\n  System.out.println("equal");\n}',
    options: ['This works correctly — .equals() compares String content', 'This throws NullPointerException', '== is needed instead', 'This prints nothing'],
    correctIndex: 0, explanation: 'Using "literal".equals(variable) is the safe way to compare strings in Java. Even if the variable is null, the literal .equals() call avoids NullPointerException.' },
{ id: 'bh-ja-e2027', game: 'bug-hunter', language: 'java', difficulty: 'easy',
    code: 'public class Parent {\n  private String name;\n  public Parent(String name) {\n    this.name = name;\n  }\n}\npublic class Child extends Parent {\n  public Child(String name) { }\n}',
    options: ['Child constructor must call super(name) — Parent has no default constructor', 'Add a default constructor to Parent', 'Remove the constructor from Parent', 'Make name protected instead of private'],
    correctIndex: 0, explanation: 'Since Parent defines a constructor with parameters, Java does not generate a default constructor. Child must explicitly call super(name) in its constructor.' },
{ id: 'bh-ja-e2028', game: 'bug-hunter', language: 'java', difficulty: 'easy',
    code: 'Set<Integer> set = new HashSet<>();\nset.add(1);\nset.add(2);\nset.add(1);\nSystem.out.println(set.size());',
    options: ['Size is 2 — Set rejects duplicates', 'Size is 3', 'This throws DuplicateException', 'Size is 1'],
    correctIndex: 0, explanation: 'HashSet does not allow duplicates. Adding 1 twice results in only one copy. set.size() returns 2.' },
{ id: 'bh-ja-e2029', game: 'bug-hunter', language: 'java', difficulty: 'easy',
    code: 'Integer a = 128;\nInteger b = 128;\nSystem.out.println(a == b);',
    options: ['This prints false — Integer cache only covers -128 to 127', 'This prints true', 'This depends on JVM version', 'This throws NullPointerException'],
    correctIndex: 0, explanation: 'Java caches Integer objects from -128 to 127. Beyond that range, == compares references, not values. 128 is outside the cache, so a and b are different objects. Use .equals().' },
{ id: 'bh-ja-e2030', game: 'bug-hunter', language: 'java', difficulty: 'easy',
    code: 'public class Main {\n  void method() {\n    class LocalClass {\n      static int x = 5;\n    }\n  }\n}',
    options: ['Local classes cannot have static members — remove static from x', 'This is valid code', 'Change int to final', 'Move LocalClass outside method'],
    correctIndex: 0, explanation: 'Local classes (defined inside methods) cannot have static declarations. Remove static or move the class outside the method.' },
{ id: 'bh-ja-e2031', game: 'bug-hunter', language: 'java', difficulty: 'easy',
    code: 'try {\n  throw new Exception("error");\n} finally {\n  return;\n}',
    options: ['return in finally swallows the exception — remove return from finally', 'This is correct', 'finally should come before catch', 'Add catch block'],
    correctIndex: 0, explanation: 'A return statement in finally silently discards any thrown exception. This is almost always a bug. Remove the return from finally to let the exception propagate.' },
{ id: 'bh-ja-e2032', game: 'bug-hunter', language: 'java', difficulty: 'easy',
    code: 'Object[] arr = new String[3];\narr[0] = 42;',
    options: ['Cannot store Integer in String[] — ArrayStoreException at runtime', 'This works due to autoboxing', 'This causes compile error', 'Change Object[] to int[]'],
    correctIndex: 0, explanation: 'The array is created as String[] but referenced as Object[]. At runtime, arr[0] = 42 throws ArrayStoreException because 42 is not a String.' },
{ id: 'bh-ja-e2033', game: 'bug-hunter', language: 'java', difficulty: 'easy',
    code: 'String s = "hello";\ns.toUpperCase();\nSystem.out.println(s);',
    options: ['Strings are immutable — assign result: s = s.toUpperCase()', 'This prints "HELLO"', 'toUpperCase() modifies in-place', 'Use s.toUpper() instead'],
    correctIndex: 0, explanation: 'String methods return new String objects — they do not modify the original. Without assigning the result, s.toUpperCase() is computed but discarded. s remains "hello".' },
{ id: 'bh-ja-e2034', game: 'bug-hunter', language: 'java', difficulty: 'easy',
    code: 'public enum Day {\n  MON, TUE, WED;\n  private Day() {}\n}',
    options: ['Enum constructors are always private — remove private keyword or the entire constructor', 'Add public constructor', 'Make it protected', 'Add parameters to constructor'],
    correctIndex: 0, explanation: 'Enum constructors are implicitly private. Adding private is redundant but allowed. You cannot make them public or protected. The constructor can be removed entirely.' },
{ id: 'bh-ja-e2035', game: 'bug-hunter', language: 'java', difficulty: 'easy',
    code: 'int x = 10;\nif (x > 5);\n{\n  System.out.println("big");\n}',
    options: ['Remove semicolon after if — it makes the if body empty', 'This prints nothing', 'Change > to >=', 'Add else clause'],
    correctIndex: 0, explanation: 'The semicolon after if (x > 5) creates an empty if body. The block { System.out.println("big"); } always executes regardless of the condition.' },
{ id: 'bh-ja-e2036', game: 'bug-hunter', language: 'java', difficulty: 'easy',
    code: 'List<String> list = Arrays.asList("A", "B", "C");\nlist.add("D");',
    options: ['Arrays.asList returns fixed-size list — use new ArrayList<>(Arrays.asList(...))', 'Use Collections.addAll instead', 'This works correctly', 'Change List to LinkedList'],
    correctIndex: 0, explanation: 'Arrays.asList() returns a fixed-size list that does not support add() or remove(). Wrap it with new ArrayList<>() to get a modifiable list.' },
{ id: 'bh-ja-e2037', game: 'bug-hunter', language: 'java', difficulty: 'easy',
    code: 'public class Test {\n  private int value;\n  public Test(int value) {\n    value = value;\n  }\n}',
    options: ['Use this.value = value — the parameter shadows the field', 'This is correct', 'Change the parameter name', 'Remove the private keyword'],
    correctIndex: 0, explanation: 'value = value assigns the parameter to itself (no-op). Use this.value = value to distinguish the instance field from the parameter.' },
{ id: 'bh-ja-e2038', game: 'bug-hunter', language: 'java', difficulty: 'easy',
    code: 'System.out.println(0.1 + 0.2 == 0.3);',
    options: ['This prints false — floating-point arithmetic has precision errors', 'This prints true', 'Use BigDecimal for precision', 'Both A and C are correct'],
    correctIndex: 3, explanation: '0.1 + 0.2 equals 0.30000000000000004 in IEEE 754 floating-point, not exactly 0.3. Use BigDecimal or compare with a small epsilon tolerance.' },
{ id: 'bh-ja-e2039', game: 'bug-hunter', language: 'java', difficulty: 'easy',
    code: 'String[] arr = {"a", "b", "c"};\nfor (String s : arr) {\n  s = s.toUpperCase();\n}\nSystem.out.println(arr[0]);',
    options: ['arr[0] is still "a" — modifying the loop variable does not change the array', 'arr[0] is "A"', 'This throws ConcurrentModificationException', 'Use for loop with index'],
    correctIndex: 0, explanation: 's is a local copy of the reference. Reassigning s does not modify the array element. Use a traditional for loop with arr[i] = arr[i].toUpperCase() to modify elements.' },
{ id: 'bh-ja-e2040', game: 'bug-hunter', language: 'java', difficulty: 'easy',
    code: 'public interface Drawable {\n  void draw();\n}\npublic class Circle implements Drawable {\n  public void draw(int size) { }\n}',
    options: ['draw(int size) does not implement draw() — method signature must match exactly', 'Add @Override annotation', 'Make draw() static', 'Add default implementation'],
    correctIndex: 0, explanation: 'The interface method draw() takes no parameters, but the class defines draw(int size). These are different methods. The class must implement draw() with the same signature.' },
{ id: 'bh-ja-e2041', game: 'bug-hunter', language: 'java', difficulty: 'easy',
    code: 'List<Integer> list = new ArrayList<>();\nlist.remove(1);',
    options: ['list is empty — remove(1) throws IndexOutOfBoundsException', 'This returns false', 'This removes Integer 1', 'Autoboxing handles this'],
    correctIndex: 0, explanation: 'The list is empty, so remove(1) throws IndexOutOfBoundsException. Also, remove(1) removes the element at index 1, not the Integer value 1. Use remove(Integer.valueOf(1)) for value removal.' },
{ id: 'bh-ja-e2042', game: 'bug-hunter', language: 'java', difficulty: 'easy',
    code: 'class Animal {\n  String sound() { return "generic"; }\n}\nclass Dog extends Animal {\n  String sound() { return "woof"; }\n}\nAnimal a = new Dog();\nSystem.out.println(a.sound());',
    options: ['This prints "woof" — Java uses runtime polymorphism for overridden methods', 'This prints "generic"', 'Compile error — need cast', 'Depends on JVM implementation'],
    correctIndex: 0, explanation: 'Java uses dynamic dispatch for instance methods. Even though the reference type is Animal, the actual object is Dog, so Dog.sound() is called. This is polymorphism.' },
{ id: 'bh-ja-e2043', game: 'bug-hunter', language: 'java', difficulty: 'easy',
    code: 'public class Outer {\n  private int x = 10;\n  class Inner {\n    void show() { System.out.println(x); }\n  }\n}\nOuter.Inner obj = new Outer.Inner();',
    options: ['Need Outer instance first: new Outer().new Inner()', 'This works correctly', 'Make Inner static', 'Add public to Inner constructor'],
    correctIndex: 0, explanation: 'Non-static inner classes require an instance of the outer class. Create with: Outer outer = new Outer(); Outer.Inner obj = outer.new Inner();' },
{ id: 'bh-ja-e2044', game: 'bug-hunter', language: 'java', difficulty: 'easy',
    code: 'final List<String> list = new ArrayList<>();\nlist.add("item");',
    options: ['This works — final prevents reassignment, not mutation of the list', 'This throws UnsupportedOperationException', 'list is immutable', 'Cannot add to final list'],
    correctIndex: 0, explanation: 'final on a reference variable means the variable cannot be reassigned to point to a different object. The object itself can still be modified. Use Collections.unmodifiableList() for true immutability.' },
{ id: 'bh-ja-e2045', game: 'bug-hunter', language: 'java', difficulty: 'easy',
    code: 'public class Singleton {\n  private static Singleton instance;\n  private Singleton() {}\n  public static Singleton getInstance() {\n    if (instance == null) {\n      instance = new Singleton();\n    }\n    return instance;\n  }\n}',
    options: ['Not thread-safe — use synchronized or eager initialization', 'This is a correct singleton', 'Need volatile keyword', 'Both A and C — double-checked locking needs volatile'],
    correctIndex: 3, explanation: 'Without synchronization, two threads can both see instance as null and create two instances. Add synchronized to getInstance() or use eager initialization. For double-checked locking, instance must be volatile.' },
