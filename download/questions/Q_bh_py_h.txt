{ id: 'bh-py-h2001', game: 'bug-hunter', language: 'python', difficulty: 'hard',
    code: 'class Singleton:\n    instance = None\n    def __new__(cls):\n        if cls.instance is None:\n            cls.instance = super().__new__(cls)\n        return cls.instance\n    def __init__(self):\n        self.data = []',
    options: ['__init__ runs every time, resetting data — check instance first', 'Use @classmethod instead of __new__', 'Remove __init__ entirely', 'Add __del__ method'],
    correctIndex: 0, explanation: 'While __new__ returns the same instance, __init__ runs every time Singleton() is called, resetting self.data. Check if instance exists in __init__ too.' },
{ id: 'bh-py-h2002', game: 'bug-hunter', language: 'python', difficulty: 'hard',
    code: 'def memoize(func):\n    cache = {}\n    def wrapper(*args):\n        if args in cache:\n            return cache[args]\n        result = func(*args)\n        cache[args] = result\n        return result\n    return wrapper\n\n@memoize\ndef compute(obj): pass\n\ncompute([1, 2, 3])',
    options: ['Lists are unhashable — use args as a string key or convert to tuple', 'Add **kwargs support', 'Use functools.lru_cache instead', 'Both A and C are valid solutions'],
    correctIndex: 3, explanation: 'Lists cannot be used as dictionary keys because they are unhashable. Convert to tuple or use functools.lru_cache which handles this more robustly.' },
{ id: 'bh-py-h2003', game: 'bug-hunter', language: 'python', difficulty: 'hard',
    code: 'class Property:\n    def __init__(self, fget=None):\n        self.fget = fget\n    def __get__(self, obj, objtype=None):\n        if obj is None:\n            return self\n        return self.fget(obj)\n\nclass Circle:\n    @Property\n    def area(self):\n        return 3.14 * self.r ** 2\n\nc = Circle()\nc.r = 5\nprint(c.area)',
    options: ['Missing __set_name__ to store the attribute name, and area is called as method not property', 'Need to add __set__ method', 'Change @Property to @property', 'Both A and C are valid — the built-in @property works correctly'],
    correctIndex: 3, explanation: 'The custom Property descriptor has issues with how it stores and retrieves values. Using the built-in @property decorator is the correct and simpler approach.' },
{ id: 'bh-py-h2004', game: 'bug-hunter', language: 'python', difficulty: 'hard',
    code: 'def thread_safe_increment(counter, lock):\n    with lock:\n        temp = counter["value"]\n    temp += 1\n    with lock:\n        counter["value"] = temp\n    return counter["value"]',
    options: ['The increment must be fully inside one lock acquisition', 'Use threading.RLock instead', 'Use atomic operations only', 'Remove the lock entirely'],
    correctIndex: 0, explanation: 'The read and write are in separate lock blocks, allowing a race condition. Another thread can modify the value between the two with blocks. Both operations must be in the same lock.' },
{ id: 'bh-py-h2005', game: 'bug-hunter', language: 'python', difficulty: 'hard',
    code: 'def deep_update(target, source):\n    for key, value in source.items():\n        if isinstance(value, dict):\n            deep_update(target[key], value)\n        else:\n            target[key] = value\n    return target\n\ndeep_update({}, {"a": {"b": 1}})  # works\ndeep_update({"x": 1}, {"a": {"b": 1}})  # fails',
    options: ['Add check: if key not in target: target[key] = {} before recursing', 'Use collections.ChainMap', 'Use dict.update() recursively', 'Add isinstance check for target[key]'],
    correctIndex: 0, explanation: 'If target does not have the nested key, target[key] raises KeyError. You must create the nested dict if it does not exist before recursing.' },
{ id: 'bh-py-h2006', game: 'bug-hunter', language: 'python', difficulty: 'hard',
    code: 'class Matrix:\n    def __init__(self, data):\n        self.data = data\n    def __mul__(self, other):\n        result = []\n        for i in range(len(self.data)):\n            row = []\n            for j in range(len(other.data[0])):\n                val = sum(self.data[i][k] * other.data[k][j]\n                          for k in range(len(self.data)))\n                row.append(val)\n            result.append(row)\n        return Matrix(result)',
    options: ['Use len(other.data) instead of len(self.data) for k range', 'Add dimension validation', 'Change __mul__ to __matmul__', 'Both A and B are needed'],
    correctIndex: 3, explanation: 'The inner loop uses len(self.data) but should use len(other.data) for the shared dimension. Also, dimension validation should be added to catch incompatible matrices.' },
{ id: 'bh-py-h2007', game: 'bug-hunter', language: 'python', difficulty: 'hard',
    code: 'async def fetch_all(urls):\n    results = []\n    for url in urls:\n        response = await fetch(url)\n        results.append(response)\n    return results',
    options: ['Use asyncio.gather() to fetch concurrently instead of sequentially', 'Add async for loop', 'Remove await keyword', 'Use asyncio.wait() instead'],
    correctIndex: 0, explanation: 'Using await in a loop makes requests sequential. Use asyncio.gather(*[fetch(url) for url in urls]) to make all requests concurrently.' },
{ id: 'bh-py-h2008', game: 'bug-hunter', language: 'python', difficulty: 'hard',
    code: 'def validate_schema(data, schema):\n    for field, field_type in schema.items():\n        if field not in data:\n            raise ValueError(f"Missing {field}")\n        if type(data[field]) != field_type:\n            raise TypeError(f"{field} must be {field_type}")\n    return True\n\nvalidate_schema({"age": 25}, {"age": int})  # True\nvalidate_schema({"name": True}, {"name": str})  # fails\nvalidate_schema({"name": "yes"}, {"name": bool})',
    options: ['Use isinstance() instead of type() to support inheritance', 'Add Optional type support', 'Use typing module for Union types', 'All of the above improve the validator'],
    correctIndex: 3, explanation: 'type() does not support inheritance (bool is a subclass of int but type(True) != int). isinstance() fixes this. Adding Optional and Union support makes the validator much more useful.' },
{ id: 'bh-py-h2009', game: 'bug-hunter', language: 'python', difficulty: 'hard',
    code: 'class TreeNode:\n    def __init__(self, val=0, left=None, right=None):\n        self.val = val\n        self.left = left\n        self.right = right\n\ndef has_cycle(node, visited=None):\n    if visited is None:\n        visited = set()\n    if node in visited:\n        return True\n    visited.add(node)\n    return has_cycle(node.left, visited) or has_cycle(node.right, visited)',
    options: ['The OR short-circuits — if left has a cycle, right is never checked properly', 'Need to handle None nodes before recursion', 'Use iterative DFS with explicit stack', 'Both B and C improve the solution'],
    correctIndex: 3, explanation: 'The function does not check if node is None before recursing, causing AttributeError. An iterative approach with an explicit stack also avoids recursion depth limits.' },
{ id: 'bh-py-h2010', game: 'bug-hunter', language: 'python', difficulty: 'hard',
    code: 'from contextlib import contextmanager\n\n@contextmanager\ndef temp_file():\n    import tempfile\n    f = tempfile.NamedTemporaryFile(delete=False)\n    f.write(b"temp data")\n    f.close()\n    yield f.name\n    os.unlink(f.name)',
    options: ['Missing import os at module level or inside function', 'delete=False is wrong', 'Need to open file in write mode', 'yield should be inside try block'],
    correctIndex: 0, explanation: 'os.unlink() is called but os is never imported. Add import os either at the top or inside the function before using os.unlink().' },
{ id: 'bh-py-h2011', game: 'bug-hunter', language: 'python', difficulty: 'hard',
    code: 'def partial(func, *args, **kwargs):\n    def wrapper(*more_args, **more_kwargs):\n        merged_kwargs = {**kwargs, **more_kwargs}\n        return func(*args, *more_args, **merged_kwargs)\n    return wrapper\n\ndef greet(greeting, name, punctuation="!"):\n    return f"{greeting}, {name}{punctuation}"\n\nhello = partial(greet, "Hello")\nprint(hello(name="World", punctuation="?"))',
    options: ['This works correctly — partial application with keyword args', 'merged_kwargs overwrites incorrectly', 'Need functools.partial instead', 'args should come after more_args'],
    correctIndex: 0, explanation: 'The custom partial function correctly merges positional and keyword arguments. The kwargs are merged with more_kwargs taking priority, which is the expected behavior.' },
{ id: 'bh-py-h2012', game: 'bug-hunter', language: 'python', difficulty: 'hard',
    code: 'def lru_cache(maxsize=128):\n    def decorator(func):\n        cache = {}\n        order = []\n        def wrapper(*args):\n            if args in cache:\n                order.remove(args)\n                order.append(args)\n                return cache[args]\n            result = func(*args)\n            cache[args] = result\n            order.append(args)\n            if len(cache) > maxsize:\n                oldest = order.pop(0)\n                del cache[oldest]\n            return result\n        return wrapper\n    return decorator',
    options: ['order.remove(args) is O(n) — use collections.OrderedDict for O(1)', 'The cache never expires', 'Need thread safety', 'Use weakref for cache values'],
    correctIndex: 0, explanation: 'Using a list for order tracking makes remove() O(n), defeating the purpose of an LRU cache. Use OrderedDict with move_to_end() for O(1) operations.' },
{ id: 'bh-py-h2013', game: 'bug-hunter', language: 'python', difficulty: 'hard',
    code: 'class Observable:\n    def __init__(self):\n        self._observers = []\n    def subscribe(self, observer):\n        self._observers.append(observer)\n    def notify(self, event):\n        for observer in self._observers:\n            observer(event)\n\nclass EventBus(Observable):\n    def __init__(self):\n        pass\n\nbus = EventBus()\nbus.subscribe(print)',
    options: ['Call super().__init__() in EventBus to initialize _observers', 'Add @property decorator', 'Use class methods instead', 'Change pass to self._observers = []'],
    correctIndex: 0, explanation: 'EventBus.__init__ overrides the parent without calling super().__init__(), so _observers is never initialized. This causes AttributeError when subscribe is called.' },
{ id: 'bh-py-h2014', game: 'bug-hunter', language: 'python', difficulty: 'hard',
    code: 'def retry_with_backoff(func, max_retries=3, base_delay=1):\n    import time\n    for attempt in range(max_retries):\n        try:\n            return func()\n        except Exception as e:\n            if attempt == max_retries - 1:\n                raise e\n            time.sleep(base_delay)\n    return None',
    options: ['The delay should increase exponentially: base_delay * (2 ** attempt)', 'Remove the time.sleep call', 'Use base_delay * attempt for linear backoff', 'The sleep should be before func()'],
    correctIndex: 0, explanation: 'The current code always sleeps base_delay seconds. Exponential backoff should increase the delay: base_delay * (2 ** attempt), giving 1s, 2s, 4s, etc.' },
{ id: 'bh-py-h2015', game: 'bug-hunter', language: 'python', difficulty: 'hard',
    code: 'class ImmutableList:\n    def __init__(self, items):\n        self._items = items\n    def __getitem__(self, index):\n        return self._items[index]\n    def __setitem__(self, index, value):\n        raise TypeError("ImmutableList does not support item assignment")\n\nlst = ImmutableList([1, 2, 3])\nlst._items.append(4)',
    options: ['Store a copy: self._items = list(items) and use __getattr__ to prevent modification', 'Add __slots__ to the class', 'Use @property on _items', 'Make _items a tuple: self._items = tuple(items)'],
    correctIndex: 3, explanation: 'Even though __setitem__ is blocked, _items is still a regular list accessible via ._items. Using tuple(items) makes it truly immutable since tuples cannot be modified.' },
{ id: 'bh-py-h2016', game: 'bug-hunter', language: 'python', difficulty: 'hard',
    code: 'def binary_search(arr, target):\n    left, right = 0, len(arr)\n    while left < right:\n        mid = (left + right) // 2\n        if arr[mid] < target:\n            left = mid\n        elif arr[mid] > target:\n            right = mid\n        else:\n            return mid\n    return -1',
    options: ['Set left = mid + 1 and right = mid to avoid infinite loop', 'Change while to left <= right', 'Use bitwise shift for mid calculation', 'Both A and B are needed'],
    correctIndex: 0, explanation: 'When arr[mid] < target, setting left = mid can cause an infinite loop if mid equals left. Use left = mid + 1. Similarly, right = mid is fine for the upper bound with left < right.' },
{ id: 'bh-py-h2017', game: 'bug-hunter', language: 'python', difficulty: 'hard',
    code: 'import sqlite3\n\ndef get_user(user_id):\n    conn = sqlite3.connect("db.sqlite")\n    cursor = conn.execute(f"SELECT * FROM users WHERE id = {user_id}")\n    result = cursor.fetchone()\n    conn.close()\n    return result',
    options: ['Use parameterized query: conn.execute("SELECT ... WHERE id = ?", (user_id,))', 'Add try/finally for conn.close()', 'Use context manager: with sqlite3.connect()', 'Both A and C are needed for security and reliability'],
    correctIndex: 3, explanation: 'f-string interpolation in SQL queries enables SQL injection. Use parameterized queries with ?. Also, use the connection as a context manager to ensure proper cleanup.' },
{ id: 'bh-py-h2018', game: 'bug-hunter', language: 'python', difficulty: 'hard',
    code: 'def merge_sort(arr):\n    if len(arr) <= 1:\n        return arr\n    mid = len(arr) // 2\n    left = merge_sort(arr[:mid])\n    right = merge_sort(arr[mid:])\n    return merge(left, right)\n\ndef merge(left, right):\n    result = []\n    i = j = 0\n    while i < len(left) and j < len(right):\n        if left[i] <= right[j]:\n            result.append(left[i])\n            i += 1\n        else:\n            result.append(right[j])\n            j += 1\n    result.extend(left[i:])\n    result.extend(right[j:])\n    return result\n\nprint(merge_sort([3, 1]))',
    options: ['This works correctly — merge sort implementation is complete', 'Missing base case for length 2', 'Need to handle duplicate values', 'extend should be append instead'],
    correctIndex: 0, explanation: 'The merge sort implementation is correct. It properly divides, recursively sorts, and merges. The extend calls add remaining elements from both halves.' },
{ id: 'bh-py-h2019', game: 'bug-hunter', language: 'python', difficulty: 'hard',
    code: 'def rate_limiter(max_calls, period):\n    calls = []\n    def decorator(func):\n        def wrapper(*args, **kwargs):\n            now = time.time()\n            calls[:] = [t for t in calls if now - t < period]\n            if len(calls) >= max_calls:\n                raise RuntimeError("Rate limit exceeded")\n            calls.append(now)\n            return func(*args, **kwargs)\n        return wrapper\n    return decorator',
    options: ['import time is missing — add it at the top', 'calls list is shared across all decorated functions', 'Need to use threading.Lock for thread safety', 'Both B and C are issues'],
    correctIndex: 3, explanation: 'The calls list is shared between all functions using this decorator, so different functions share the same rate limit. Also, without a lock, concurrent access to the calls list is unsafe.' },
{ id: 'bh-py-h2020', game: 'bug-hunter', language: 'python', difficulty: 'hard',
    code: 'def walk_directory(path, pattern="*.py"):\n    import glob\n    files = glob.glob(f"{path}/{pattern}")\n    for root, dirs, filenames in os.walk(path):\n        for filename in filenames:\n            if filename.endswith(pattern[1:]):\n                files.append(os.path.join(root, filename))\n    return files',
    options: ['Import os is missing, and glob.glob + os.walk duplicates results', 'Use pathlib.Path.rglob() instead', 'pattern[1:] strips the * but fails for complex patterns', 'All of the above are valid improvements'],
    correctIndex: 3, explanation: 'os is not imported, glob.glob and os.walk produce overlapping results, and pattern slicing is fragile. Using Path.rglob() from pathlib is cleaner and more reliable.' },
{ id: 'bh-py-h2021', game: 'bug-hunter', language: 'python', difficulty: 'hard',
    code: 'class Meta(type):\n    def __new__(cls, name, bases, namespace):\n        if "required_method" not in namespace:\n            raise TypeError(f"{name} must implement required_method")\n        return super().__new__(cls, name, bases, namespace)\n\nclass Base(metaclass=Meta):\n    pass\n\nclass Child(Base):\n    def required_method(self):\n        pass',
    options: ['The check fails for inherited methods — use dir() or check all bases', 'This works correctly for abstract-like enforcement', 'Need to use abc.ABC instead', 'Both A and C are better approaches'],
    correctIndex: 3, explanation: 'namespace only contains methods defined directly in the class, not inherited ones. Using abc.ABC with @abstractmethod is the standard Python way to enforce method implementation.' },
{ id: 'bh-py-h2022', game: 'bug-hunter', language: 'python', difficulty: 'hard',
    code: 'def parallel_map(func, items, workers=4):\n    from threading import Thread\n    results = [None] * len(items)\n    def worker(idx):\n        results[idx] = func(items[idx])\n    threads = [Thread(target=worker, args=(i,)) for i in range(len(items))]\n    for t in threads:\n        t.start()\n    for t in threads:\n        t.join()\n    return results',
    options: ['Creates len(items) threads, not workers — use a ThreadPoolExecutor or queue', 'Thread safety issue with results list', 'Use multiprocessing for CPU-bound work', 'Both A and C are improvements'],
    correctIndex: 3, explanation: 'The code creates one thread per item, ignoring the workers parameter. For CPU-bound tasks, multiprocessing is better than threading due to the GIL. ThreadPoolExecutor properly limits concurrency.' },
{ id: 'bh-py-h2023', game: 'bug-hunter', language: 'python', difficulty: 'hard',
    code: 'def profile(func):\n    import time\n    def wrapper(*args, **kwargs):\n        start = time.time()\n        result = func(*args, **kwargs)\n        end = time.time()\n        print(f"{func.__name__} took {end - start:.2f}s")\n        return result\n    return wrapper',
    options: ['wrapper loses func metadata — use @functools.wraps(func)', 'Use time.perf_counter() for more precision', 'Add memory profiling', 'Both A and B are improvements'],
    correctIndex: 3, explanation: 'Without @functools.wraps, the wrapper loses __name__, __doc__, etc. Also, time.perf_counter() provides higher resolution timing than time.time().' },
{ id: 'bh-py-h2024', game: 'bug-hunter', language: 'python', difficulty: 'hard',
    code: 'class LazyProperty:\n    def __init__(self, func):\n        self.func = func\n        self.attr_name = func.__name__\n    def __get__(self, obj, objtype=None):\n        if obj is None:\n            return self\n        value = self.func(obj)\n        setattr(obj, self.attr_name, value)\n        return value\n\nclass Config:\n    @LazyProperty\n    def database_url(self):\n        return os.environ.get("DATABASE_URL", "localhost")',
    options: ['import os is missing', 'The lazy property recomputes on every access because setattr overwrites', 'Need to add __set__ descriptor method', 'This works correctly for lazy evaluation'],
    correctIndex: 0, explanation: 'os is used but never imported. The code needs import os at the top of the module. The lazy property pattern itself is correct — setattr replaces the descriptor on the instance.' },
{ id: 'bh-py-h2025', game: 'bug-hunter', language: 'python', difficulty: 'hard',
    code: 'def chunked(iterable, size):\n    iterator = iter(iterable)\n    while True:\n        chunk = list(itertools.islice(iterator, size))\n        if not chunk:\n            break\n        yield chunk',
    options: ['import itertools is missing', 'This works correctly for any iterable', 'Use yield from instead of yield', 'Need to handle size=0 case'],
    correctIndex: 0, explanation: 'itertools.islice is used but itertools is never imported. Add import itertools at the top of the module.' },
{ id: 'bh-py-h2026', game: 'bug-hunter', language: 'python', difficulty: 'hard',
    code: 'def composed(*funcs):\n    def inner(data):\n        result = data\n        for func in funcs:\n            result = func(result)\n        return result\n    return inner\n\ntransform = composed(str.strip, str.upper, str.title)\nprint(transform("  hello world  "))',
    options: ['Functions are applied left-to-right but composition should be right-to-left', 'str.title after str.upper has no effect', 'Need to reverse the function order', 'Both A and B — and the composition order should be reversed'],
    correctIndex: 3, explanation: 'Mathematical function composition applies right-to-left, but this applies left-to-right. Also, calling str.title() after str.upper() does nothing since the string is already uppercase. Reverse the order.' },
{ id: 'bh-py-h2027', game: 'bug-hunter', language: 'python', difficulty: 'hard',
    code: 'class PluginManager:\n    _plugins = {}\n    @classmethod\n    def register(cls, name):\n        def decorator(func):\n            cls._plugins[name] = func\n            return func\n        return decorator\n    @classmethod\n    def get(cls, name):\n        return cls._plugins.get(name)\n\n@PluginManager.register("greet")\ndef greet_user():\n    return "Hello!"\n\n# In another module:\n@PluginManager.register("greet")\ndef greet_formal():\n    return "Good day!"',
    options: ['The second register silently overwrites the first "greet" plugin', 'Need to use a list for each name', 'Add validation for duplicate names', 'Both B and C improve robustness'],
    correctIndex: 3, explanation: 'Duplicate plugin names silently overwrite each other. Storing plugins in a list per name or raising an error on duplicates would make the system more robust.' },
{ id: 'bh-py-h2028', game: 'bug-hunter', language: 'python', difficulty: 'hard',
    code: 'def debounce(wait):\n    def decorator(func):\n        last_called = 0\n        def wrapper(*args, **kwargs):\n            nonlocal last_called\n            now = time.time()\n            if now - last_called >= wait:\n                last_called = now\n                return func(*args, **kwargs)\n        return wrapper\n    return decorator',
    options: ['import time is missing, and this is throttling not debouncing', 'This is correct debounce implementation', 'Need threading.Timer for real debounce', 'Both A and C — real debounce delays the call'],
    correctIndex: 3, explanation: 'import time is missing. More importantly, this implements throttling (limiting call rate) not debouncing (delaying until calls stop). Real debouncing requires a timer that resets on each call.' },
{ id: 'bh-py-h2029', game: 'bug-hunter', language: 'python', difficulty: 'hard',
    code: 'def tree_map(func, tree):\n    if isinstance(tree, dict):\n        return {k: tree_map(func, v) for k, v in tree.items()}\n    elif isinstance(tree, list):\n        return [tree_map(func, item) for item in tree]\n    else:\n        return func(tree)\n\ndata = {"a": 1, "b": [2, {"c": 3}], "d": "hello"}\nprint(tree_map(lambda x: x * 2, data))',
    options: ['String "hello" gets doubled to "hellohello" — need type checking in func', 'This works correctly for all types', 'Need to handle tuples', 'Use recursion limit protection'],
    correctIndex: 0, explanation: 'The lambda x: x * 2 doubles numbers but also repeats strings. "hello" becomes "hellohello". Add type checking in the transformation function or the tree_map itself.' },
{ id: 'bh-py-h2030', game: 'bug-hunter', language: 'python', difficulty: 'hard',
    code: 'def curried(add):\n    def curry(x):\n        def inner(y):\n            return add(x, y)\n        return inner\n    return curry\n\n@curried\ndef add(x, y):\n    return x + y\n\nprint(add(3)(4))',
    options: ['This works correctly — add is curried to take one arg at a time', 'add(3) returns a function, add(3)(4) returns 7', 'Need to use functools.partial instead', 'The decorator changes the calling convention unexpectedly'],
    correctIndex: 0, explanation: 'The currying decorator works correctly. After decoration, add(3) returns a function that waits for the second argument. add(3)(4) correctly returns 7.' },
{ id: 'bh-py-h2031', game: 'bug-hunter', language: 'python', difficulty: 'hard',
    code: 'class EventEmitter:\n    def __init__(self):\n        self._events = {}\n    def on(self, event, listener):\n        if event not in self._events:\n            self._events[event] = []\n        self._events[event].append(listener)\n    def emit(self, event, *args):\n        for listener in self._events[event]:\n            listener(*args)\n\nee = EventEmitter()\nee.emit("click")',
    options: ['emit() raises KeyError for unregistered events — use .get(event, [])', 'Need to add off() method', 'Listeners should be called asynchronously', 'Add once() method for one-time listeners'],
    correctIndex: 0, explanation: 'When emitting an event that has no listeners, self._events[event] raises KeyError. Use self._events.get(event, []) to handle unregistered events gracefully.' },
{ id: 'bh-py-h2032', game: 'bug-hunter', language: 'python', difficulty: 'hard',
    code: 'def validate_email(email):\n    import re\n    pattern = r"^[a-zA-Z0-9]+@[a-zA-Z0-9]+.com$"\n    return bool(re.match(pattern, email))',
    options: ['The dot in .com should be escaped: \\.com', 'The pattern is too restrictive for valid emails', 'Need to add re.IGNORECASE flag', 'Both A and B — the unescaped dot matches any char and the pattern rejects valid emails'],
    correctIndex: 3, explanation: 'The unescaped . matches any character, not just a literal dot. Also, the pattern only accepts .com domains, rejecting valid .org, .net, etc. Escape the dot and broaden the TLD pattern.' },
{ id: 'bh-py-h2033', game: 'bug-hunter', language: 'python', difficulty: 'hard',
    code: 'class FrozenDict(dict):\n    def __hash__(self):\n        return hash(tuple(sorted(self.items())))\n    def __setitem__(self, key, value):\n        raise TypeError("FrozenDict is immutable")\n\nfd = FrozenDict({"a": 1})\nfd.update({"b": 2})\nprint(fd)',
    options: ['update() bypasses __setitem__ — override update, pop, delitem too', 'Use types.MappingProxyType instead', 'Add __delitem__ override', 'Both A and B — MappingProxyType is the standard solution'],
    correctIndex: 3, explanation: 'dict.update() does not go through __setitem__, so the immutability is broken. Overriding all mutating methods is fragile. types.MappingProxyType provides a proper read-only view.' },
{ id: 'bh-py-h2034', game: 'bug-hunter', language: 'python', difficulty: 'hard',
    code: 'def async_pool(coro_func, items, max_concurrent=10):\n    async def runner():\n        sem = asyncio.Semaphore(max_concurrent)\n        async def bounded(item):\n            async with sem:\n                return await coro_func(item)\n        return await asyncio.gather(*[bounded(item) for item in items])\n    return runner()',
    options: ['import asyncio is missing', 'This works correctly with proper imports', 'Need to handle exceptions in gather', 'Both A and C — missing import and no error handling'],
    correctIndex: 3, explanation: 'asyncio is used but not imported. Also, asyncio.gather without return_exceptions=True will fail fast on the first error, losing other results.' },
{ id: 'bh-py-h2035', game: 'bug-hunter', language: 'python', difficulty: 'hard',
    code: 'def make_class(name, **attrs):\n    return type(name, (object,), attrs)\n\nPerson = make_class("Person", greet=lambda self: f"Hi, I am {self.name}")\np = Person()\nprint(p.greet())',
    options: ['Add __init__ via attrs or set p.name before calling p.greet()', 'Change the lambda to not use self.name', 'Use type() with different syntax', 'This works correctly'],
    correctIndex: 0, explanation: 'The class has no __init__ to set self.name and the instance has no name attribute. Either add an __init__ method to the class or set p.name before calling greet().' },
{ id: 'bh-py-h2036', game: 'bug-hunter', language: 'python', difficulty: 'hard',
    code: 'def type_check(**expected):\n    def decorator(func):\n        def wrapper(**kwargs):\n            for name, expected_type in expected.items():\n                if name in kwargs and not isinstance(kwargs[name], expected_type):\n                    raise TypeError(f"{name} must be {expected_type.__name__}")\n            return func(**kwargs)\n        return wrapper\n    return decorator\n\n@type_check(age=int, name=str)\ndef create_user(name, age):\n    return {"name": name, "age": age}\n\ncreate_user("Alice", 30)',
    options: ['create_user("Alice", 30) uses positional args, but wrapper only accepts **kwargs', 'Change isinstance to type() comparison', 'Add *args support to wrapper', 'Both A and C — wrapper needs to handle positional args'],
    correctIndex: 3, explanation: 'The wrapper only accepts **kwargs, but create_user is called with positional arguments. The type checking never runs. Modify wrapper to accept *args and **kwargs and map them to parameter names.' },
{ id: 'bh-py-h2037', game: 'bug-hunter', language: 'python', difficulty: 'hard',
    code: 'class BitField:\n    def __init__(self, value=0):\n        self.value = value\n    def set_bit(self, pos):\n        self.value |= (1 << pos)\n    def clear_bit(self, pos):\n        self.value &= ~(1 << pos)\n    def toggle_bit(self, pos):\n        self.value ^= (1 << pos)\n    def is_set(self, pos):\n        return self.value & (1 << pos)\n\nbf = BitField()\nbf.set_bit(3)\nprint(bf.is_set(3))',
    options: ['is_set returns int not bool — use bool(self.value & (1 << pos))', 'set_bit does not work for pos > 31', 'Need to handle negative positions', 'This works correctly in all cases'],
    correctIndex: 0, explanation: 'self.value & (1 << pos) returns an integer (8), not a boolean. While truthy, explicit bool() is clearer and avoids bugs like if bf.is_set(3) == True failing.' },
{ id: 'bh-py-h2038', game: 'bug-hunter', language: 'python', difficulty: 'hard',
    code: 'def stream_lines(filename):\n    with open(filename) as f:\n        while True:\n            line = f.readline()\n            if not line:\n                break\n            yield line.strip()',
    options: ['This works correctly for streaming lines from a file', 'Need to add buffering', 'strip() removes trailing newlines needed for detection', 'Use f.read() instead'],
    correctIndex: 0, explanation: 'The generator correctly reads lines one at a time, checking for EOF with if not line. The strip() removes the newline. This is a valid lazy file reading pattern.' },
{ id: 'bh-py-h2039', game: 'bug-hunter', language: 'python', difficulty: 'hard',
    code: 'class WeakRefCache:\n    def __init__(self):\n        self._cache = weakref.WeakValueDictionary()\n    def get_or_compute(self, key, compute_func):\n        if key in self._cache:\n            return self._cache[key]\n        value = compute_func()\n        self._cache[key] = value\n        return value',
    options: ['import weakref is missing', 'WeakValueDictionary may lose entries before they are returned', 'Need to use WeakKeyDictionary instead', 'This is a valid cache pattern with proper import'],
    correctIndex: 0, explanation: 'weakref is used but not imported. Add import weakref at the top. Note that WeakValueDictionary can lose entries if no strong reference exists, but that is by design for memory management.' },
{ id: 'bh-py-h2040', game: 'bug-hunter', language: 'python', difficulty: 'hard',
    code: 'def safe_execute(func, *args, fallback=None, log_errors=True, **kwargs):\n    try:\n        return func(*args, **kwargs)\n    except Exception as e:\n        if log_errors:\n            logging.error(f"Error in {func.__name__}: {e}")\n        return fallback',
    options: ['import logging is missing', 'fallback=None masks errors silently', 'Need to handle specific exceptions', 'Both A and B — missing import and silent failure risk'],
    correctIndex: 3, explanation: 'logging is used but not imported. Also, returning None as a default fallback can mask serious errors in calling code. Consider raising by default or logging at WARNING level.' },
{ id: 'bh-py-h2041', game: 'bug-hunter', language: 'python', difficulty: 'hard',
    code: 'def sliding_window(iterable, size):\n    iters = tee(iterable, size)\n    for i, it in enumerate(iters):\n        for _ in range(i):\n            next(it, None)\n    return zip(*iters)',
    options: ['import itertools and use itertools.tee', 'tee with large size is memory intensive', 'This works correctly for fixed windows', 'Both A and B — missing import and memory concern'],
    correctIndex: 3, explanation: 'itertools.tee is used but itertools is not imported. Also, tee with a large window size creates many independent iterators, which is memory-inefficient. Consider using a deque for large windows.' },
{ id: 'bh-py-h2042', game: 'bug-hunter', language: 'python', difficulty: 'hard',
    code: 'def flatten_json(data, parent_key="", sep="_"):\n    items = []\n    for k, v in data.items():\n        new_key = f"{parent_key}{sep}{k}" if parent_key else k\n        if isinstance(v, dict):\n            items.extend(flatten_json(v, new_key, sep).items())\n        else:\n            items.append((new_key, v))\n    return dict(items)',
    options: ['This works correctly for nested dict flattening', 'Need to handle lists inside the dict', 'Missing base case for empty dict', 'sep should be configurable'],
    correctIndex: 0, explanation: 'The function correctly flattens nested dictionaries by concatenating keys with a separator. It handles the root level (no prefix) and nested levels recursively.' },
{ id: 'bh-py-h2043', game: 'bug-hunter', language: 'python', difficulty: 'hard',
    code: 'class ORMModel:\n    _table = None\n    _fields = {}\n    def __init__(self, **kwargs):\n        for field, field_type in self._fields.items():\n            value = kwargs.get(field)\n            if value is not None and not isinstance(value, field_type):\n                raise TypeError(f"{field} must be {field_type}")\n            setattr(self, field, value)\n    def save(self):\n        columns = ", ".join(self._fields.keys())\n        values = ", ".join(f"\'{getattr(self, f)}\'" for f in self._fields)\n        query = f"INSERT INTO {self._table} ({columns}) VALUES ({values})"\n        return query',
    options: ['SQL injection via f-string values — use parameterized queries', 'None values cause "None" string in SQL', 'Need to handle auto-increment IDs', 'All of the above are issues'],
    correctIndex: 3, explanation: 'Using f-strings for SQL values is vulnerable to injection. None becomes the string "None" instead of NULL. Auto-increment fields should be excluded from INSERT statements.' },
{ id: 'bh-py-h2044', game: 'bug-hunter', language: 'python', difficulty: 'hard',
    code: 'def circular_buffer(size):\n    buffer = [None] * size\n    index = 0\n    def append(item):\n        buffer[index] = item\n        index = (index + 1) % size\n    def get_all():\n        return buffer[index:] + buffer[:index]\n    return append, get_all\n\nappend, get_all = circular_buffer(3)\nappend(1)\nprint(get_all())',
    options: ['index needs nonlocal declaration in append function', 'buffer needs nonlocal too', 'This works correctly', 'Need to use collections.deque instead'],
    correctIndex: 0, explanation: 'index is assigned inside append(), so Python treats it as a local variable. Add nonlocal index inside append() to modify the closure variable.' },
{ id: 'bh-py-h2045', game: 'bug-hunter', language: 'python', difficulty: 'hard',
    code: 'class Graph:\n    def __init__(self):\n        self.adj = {}\n    def add_edge(self, u, v, weight=1):\n        self.adj.setdefault(u, []).append((v, weight))\n        self.adj.setdefault(v, [])\n    def shortest_path(self, start, end):\n        heap = [(0, start, [])]\n        visited = set()\n        while heap:\n            cost, node, path = heapq.heappop(heap)\n            if node in visited:\n                continue\n            visited.add(node)\n            if node == end:\n                return path + [node], cost\n            for neighbor, w in self.adj.get(node, []):\n                heapq.heappush(heap, (cost + w, neighbor, path + [node]))\n        return None, float("inf")',
    options: ['import heapq is missing', 'The algorithm works correctly for Dijkstra', 'Need to handle negative weights', 'Both A and C — missing import and negative weight limitation'],
    correctIndex: 3, explanation: 'heapq is used but not imported. Also, Dijkstra does not work with negative edge weights — for that, use the Bellman-Ford algorithm.' },
{ id: 'bh-py-h2047', game: 'bug-hunter', language: 'python', difficulty: 'hard',
    code: 'def merge_sorted(a, b):\n    result = []\n    i = j = 0\n    while i < len(a) or j < len(b):\n        if i < len(a) and (j >= len(b) or a[i] <= b[j]):\n            result.append(a[i])\n            i += 1\n        else:\n            result.append(b[j])\n            j += 1\n    return result',
    options: ['This works correctly — merge of two sorted lists', 'The OR condition should be AND', 'Need to use heapq.merge instead', 'Should check a[i] < b[j] without equality on one side'],
    correctIndex: 0, explanation: 'The merge logic is correct. The condition handles all cases: when one list is exhausted, when both have elements, and preserves stability by preferring a when equal. heapq.merge is an alternative but this implementation is also correct.' },
{ id: 'bh-py-h2048', game: 'bug-hunter', language: 'python', difficulty: 'hard',
    code: 'class BloomFilter:\n    def __init__(self, size=1000):\n        self.size = size\n        self.bit_array = [False] * size\n    def add(self, item):\n        for seed in range(3):\n            index = hash(str(seed) + str(item)) % self.size\n            self.bit_array[index] = True\n    def contains(self, item):\n        return all(\n            self.bit_array[hash(str(seed) + str(item)) % self.size]\n            for seed in range(3)\n        )',
    options: ['hash() can return negative values — use abs() before modulo, and hash is not deterministic across runs', 'This is a correct bloom filter', 'Use bitarray library instead', 'Need more hash functions'],
    correctIndex: 0, explanation: 'Python hash() can return negative values, causing negative indices. Also, hash() is randomized across Python sessions (PYTHONHASHSEED), making the filter non-deterministic. Use abs() and a stable hash function like hashlib.' },
