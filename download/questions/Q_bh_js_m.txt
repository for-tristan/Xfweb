{ id: 'bh-js-m2001', game: 'bug-hunter', language: 'javascript', difficulty: 'medium',
    code: 'for (var i = 0; i < 3; i++) {\n  setTimeout(() => console.log(i), 100);\n}',
    options: ['Change var to let to create block-scoped i per iteration', 'Add a closure wrapper', 'Use setInterval instead', 'Change i++ to i+=1'],
    correctIndex: 0, explanation: 'var is function-scoped, so all callbacks share the same i, which is 3 when they execute. Using let creates a new i for each iteration.' },
{ id: 'bh-js-m2002', game: 'bug-hunter', language: 'javascript', difficulty: 'medium',
    code: 'const obj = { a: 1, b: 2 };\nconst keys = Object.keys(obj);\nfor (const key of obj) {\n  console.log(key);\n}',
    options: ['Use for...in or for...of Object.keys(obj) to iterate objects', 'Use obj.forEach()', 'Use Object.entries(obj) instead', 'Both A and C work correctly'],
    correctIndex: 3, explanation: 'Objects are not iterable with for...of. Use for...in for keys, or for...of Object.entries(obj) for key-value pairs.' },
{ id: 'bh-js-m2003', game: 'bug-hunter', language: 'javascript', difficulty: 'medium',
    code: 'const double = (x) => {\n  x * 2;\n};\nconsole.log(double(5));',
    options: ['Add return keyword: return x * 2', 'Remove the curly braces', 'Change arrow function to regular function', 'Use x ** 2 instead'],
    correctIndex: 0, explanation: 'Arrow functions with curly braces require an explicit return. Without it, the function returns undefined. Either add return or remove the braces.' },
{ id: 'bh-js-m2004', game: 'bug-hunter', language: 'javascript', difficulty: 'medium',
    code: 'async function getData() {\n  const response = fetch("/api/data");\n  const data = response.json();\n  return data;\n}',
    options: ['Add await before fetch() and before response.json()', 'Add return before fetch', 'Use .then() chain instead', 'Change async to sync'],
    correctIndex: 0, explanation: 'Without await, fetch() returns a Promise, not the response. Similarly, response.json() needs await. Add await before both async operations.' },
{ id: 'bh-js-m2005', game: 'bug-hunter', language: 'javascript', difficulty: 'medium',
    code: 'const user = { name: "Alice", age: 25 };\nconst { Name, Age } = user;\nconsole.log(Name);',
    options: ['Match variable names to property names: { name, age } or use alias { name: Name }', 'Use user.Name instead', 'Change const to let', 'Add default values'],
    correctIndex: 0, explanation: 'Destructuring is case-sensitive. { Name } does not match the property name. Use { name: Name } to alias, or match the exact property name { name }.' },
{ id: 'bh-js-m2006', game: 'bug-hunter', language: 'javascript', difficulty: 'medium',
    code: 'const arr = [1, 2, 3];\nconst [first, second, third, fourth] = arr;\nconsole.log(fourth);',
    options: ['fourth is undefined — arr only has 3 elements', 'fourth is null', 'This throws an error', 'fourth is 0'],
    correctIndex: 0, explanation: 'Destructuring more variables than array elements assigns undefined to the extras. fourth will be undefined. Add a default value: const [..., fourth = 0] = arr.' },
{ id: 'bh-js-m2007', game: 'bug-hunter', language: 'javascript', difficulty: 'medium',
    code: 'class Animal {\n  constructor(name) {\n    this.name = name;\n  }\n  speak() {\n    return this.name + " makes a sound";\n  }\n}\n\nclass Dog extends Animal {\n  speak() {\n    return name + " barks";\n  }\n}',
    options: ['Use this.name instead of name in Dog.speak()', 'Add super.speak() call', 'Change extends to implements', 'Add constructor to Dog'],
    correctIndex: 0, explanation: 'name without this refers to a variable, not the instance property. Use this.name to access the inherited property from the Animal class.' },
{ id: 'bh-js-m2008', game: 'bug-hunter', language: 'javascript', difficulty: 'medium',
    code: 'const promise = new Promise((resolve, reject) => {\n  setTimeout(() => {\n    resolve("done");\n  });\n}).then(result => {\n  console.log(result);\n});\nconsole.log("first");',
    options: ['This is correct — "first" prints first, then "done"', 'resolve should be called with reject', 'setTimeout needs a delay parameter', 'then() should be await instead'],
    correctIndex: 0, explanation: 'The Promise resolves asynchronously after setTimeout. The synchronous console.log("first") executes first, then "done" appears after the microtask completes. This is the expected behavior.' },
