{ id: 'bh-js-e2001', game: 'bug-hunter', language: 'javascript', difficulty: 'easy',
    code: 'if (x = 5) {\n  console.log("x is five");\n}',
    options: ['Change = to === for comparison', 'Add let before x', 'Change console.log to print', 'Remove the if statement'],
    correctIndex: 0, explanation: 'Single = is assignment, not comparison. x = 5 always evaluates to 5 (truthy). Use === for strict equality comparison.' },
{ id: 'bh-js-e2002', game: 'bug-hunter', language: 'javascript', difficulty: 'easy',
    code: 'const numbers = [1, 2, 3];\nnumbers.push(4);\nnumbers = [5, 6];',
    options: ['Cannot reassign const — use let if reassignment is needed', 'Use numbers.add(4) instead', 'Change push to append', 'Remove the const keyword'],
    correctIndex: 0, explanation: 'const prevents reassignment of the variable. While push() works to mutate the array, reassigning numbers = [5, 6] throws TypeError. Use let if reassignment is needed.' },
{ id: 'bh-js-e2003', game: 'bug-hunter', language: 'javascript', difficulty: 'easy',
    code: 'function greet(name) {\n  return "Hello, " + name;\n}\n\nconsole.log(greet());',
    options: ['Pass an argument: greet("World") or add a default parameter', 'Remove the return statement', 'Change + to comma', 'Add a second parameter'],
    correctIndex: 0, explanation: 'The function expects a name argument but is called without one. name will be undefined, printing "Hello, undefined". Add a default: function greet(name = "World").' },
{ id: 'bh-js-e2004', game: 'bug-hunter', language: 'javascript', difficulty: 'easy',
    code: 'const obj = { name: "Alice" };\nconsole.log(obj.age.length);',
    options: ['Check if age exists first: obj.age?.length', 'Use obj.get("age") instead', 'Add age property to the object', 'Use obj["age"].length'],
    correctIndex: 0, explanation: 'obj.age is undefined, and accessing .length on undefined throws TypeError. Use optional chaining obj.age?.length to safely check.' },
{ id: 'bh-js-e2005', game: 'bug-hunter', language: 'javascript', difficulty: 'easy',
    code: 'const arr = [1, 2, 3];\nfor (let i = 0; i <= arr.length; i++) {\n  console.log(arr[i]);\n}',
    options: ['Change <= to < in the loop condition', 'Change i++ to i+=2', 'Use arr.forEach instead', 'Change let to var'],
    correctIndex: 0, explanation: 'Using <= makes the loop go one step too far, accessing arr[3] which is undefined. Use < to stay within valid indices 0 to arr.length - 1.' },
{ id: 'bh-js-e2006', game: 'bug-hunter', language: 'javascript', difficulty: 'easy',
    code: 'const result = parseInt("10.5");\nconsole.log(result);',
    options: ['Use parseFloat("10.5") to get 10.5 instead of 10', 'Use Number("10.5") instead', 'Add radix parameter: parseInt("10.5", 10)', 'This is correct — parseInt returns 10'],
    correctIndex: 0, explanation: 'parseInt("10.5") truncates the decimal, returning 10. Use parseFloat() if you need the decimal portion, which returns 10.5.' },
{ id: 'bh-js-e2007', game: 'bug-hunter', language: 'javascript', difficulty: 'easy',
    code: 'switch (color) {\n  case "red":\n    console.log("Red");\n  case "blue":\n    console.log("Blue");\n    break;\n}',
    options: ['Add break after the "red" case to prevent fall-through', 'Remove the break from "blue"', 'Change switch to if/else', 'Add a default case'],
    correctIndex: 0, explanation: 'Without break after "red", execution falls through to "blue". Both "Red" and "Blue" will be printed when color is "red". Add break after each case.' },
{ id: 'bh-js-e2008', game: 'bug-hunter', language: 'javascript', difficulty: 'easy',
    code: 'const str = "Hello World";\nconsole.log(str.substring(6, 0));',
    options: ['Swap arguments: str.substring(0, 6) gives "Hello "', 'This returns an empty string', 'Use slice instead', 'This works correctly'],
    correctIndex: 0, explanation: 'substring() swaps the arguments if start > end, so substring(6, 0) is treated as substring(0, 6) returning "Hello ". But the intent is unclear — use slice(0, 6) for predictable behavior.' },
{ id: 'bh-js-e2009', game: 'bug-hunter', language: 'javascript', difficulty: 'easy',
    code: 'const a = [1, 2, 3];\nconst b = a;\nb.push(4);\nconsole.log(a.length);',
    options: ['a.length is 4 — b = a creates a reference, not a copy', 'a.length is 3', 'This throws an error', 'b.push does not affect a'],
    correctIndex: 0, explanation: 'b = a does not create a copy — both variables reference the same array. After b.push(4), both a and b have length 4. Use [...a] or a.slice() to copy.' },
{ id: 'bh-js-e2010', game: 'bug-hunter', language: 'javascript', difficulty: 'easy',
    code: 'console.log(typeof null);',
    options: ['Returns "object" — this is a well-known JS bug, not "null"', 'Returns "null"', 'Returns "undefined"', 'Throws TypeError'],
    correctIndex: 0, explanation: 'typeof null returns "object", which is a historical bug in JavaScript. To check for null, use === null instead of typeof.' },
{ id: 'bh-js-e2011', game: 'bug-hunter', language: 'javascript', difficulty: 'easy',
    code: 'const nums = [1, 2, 3, 4, 5];\ndelete nums[2];\nconsole.log(nums.length);',
    options: ['Length is still 5 — delete creates a hole, use splice instead', 'Length becomes 4', 'Length becomes 3', 'This throws an error'],
    correctIndex: 0, explanation: 'delete removes the value but leaves a hole (empty slot). The array length remains 5. Use nums.splice(2, 1) to properly remove the element and shift indices.' },
{ id: 'bh-js-e2012', game: 'bug-hunter', language: 'javascript', difficulty: 'easy',
    code: 'const x = "5";\nconst y = 3;\nconsole.log(x + y);',
    options: ['This concatenates to "53" — convert x to number first: Number(x) + y', 'This returns 8', 'This returns 53 as a number', 'This throws TypeError'],
    correctIndex: 0, explanation: 'When one operand is a string, + performs concatenation. "5" + 3 gives "53", not 8. Use Number(x) or parseInt(x) to convert before adding.' },
