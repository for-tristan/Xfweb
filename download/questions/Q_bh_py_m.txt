{ id: 'bh-py-m2001', game: 'bug-hunter', language: 'python', difficulty: 'medium',
    code: 'def greet(name):\n    return "Hello, " + name\n\nprint(greet())',
    options: ['Add name argument to greet() call', 'Remove the name parameter', 'Add a default value to name', 'Change return to print'],
    correctIndex: 2, explanation: 'The function expects a name argument but is called without one. Adding a default value like name="World" makes the function work without arguments.' },
{ id: 'bh-py-m2002', game: 'bug-hunter', language: 'python', difficulty: 'medium',
    code: 'my_list = [1, 2, 3]\nmy_list.add(4)\nprint(my_list)',
    options: ['Use my_list.append(4) instead', 'Use my_list.push(4) instead', 'Use my_list.insert(4) instead', 'Use my_list.add(4, end=True)'],
    correctIndex: 0, explanation: 'Python lists use append() to add elements, not add(). The add() method belongs to sets, not lists.' },
{ id: 'bh-py-m2003', game: 'bug-hunter', language: 'python', difficulty: 'medium',
    code: 'def count_items(items):\n    count = 0\n    for item in items\n        count += 1\n    return count',
    options: ['Add a colon after items', 'Change for to while', 'Change count += 1 to count++', 'Remove the return statement'],
    correctIndex: 0, explanation: 'Python requires a colon after the for statement. Without it, this is a syntax error.' },
{ id: 'bh-py-m2004', game: 'bug-hunter', language: 'python', difficulty: 'medium',
    code: 'x = [1, 2, 3, 4, 5]\nprint(x[5])',
    options: ['Use x[4] instead', 'Use x[-1] instead', 'Use x[6] instead', 'Use x.len() instead'],
    correctIndex: 1, explanation: 'x has indices 0-4. x[5] is out of range. x[-1] accesses the last element (5), which is valid.' },
{ id: 'bh-py-m2005', game: 'bug-hunter', language: 'python', difficulty: 'medium',
    code: 'def add(a, b):\n    result = a + b\n\nprint(add(3, 5))',
    options: ['Add return result before the function ends', 'Change result to return', 'Add print(result) inside the function', 'Change def to lambda'],
    correctIndex: 0, explanation: 'The function computes result but never returns it. Without a return statement, it returns None.' },
{ id: 'bh-py-m2006', game: 'bug-hunter', language: 'python', difficulty: 'medium',
    code: 'my_dict = {"a": 1, "b": 2}\nprint(my_dict["c"])',
    options: ['Use my_dict.get("c") instead', 'Add "c" key to the dictionary', 'Use my_dict.find("c")', 'Use my_dict["c"] with default'],
    correctIndex: 0, explanation: 'Accessing a non-existent key with [] raises KeyError. Using .get("c") returns None instead of raising an error.' },
{ id: 'bh-py-m2007', game: 'bug-hunter', language: 'python', difficulty: 'medium',
    code: 'def factorial(n):\n    if n == 0:\n        return 1\n    return n * factorial(n)',
    options: ['Change factorial(n) to factorial(n-1)', 'Change n == 0 to n == 1', 'Add elif n < 0 check', 'Change return 1 to return 0'],
    correctIndex: 0, explanation: 'The recursive call must be factorial(n-1), not factorial(n). Without decrementing n, this causes infinite recursion.' },
{ id: 'bh-py-m2008', game: 'bug-hunter', language: 'python', difficulty: 'medium',
    code: 'numbers = [1, 2, 3, 4, 5]\nsquared = list(map(lambda x: x * 2, numbers))\nprint(squared)',
    options: ['Change x * 2 to x ** 2 for squaring', 'Change map to filter', 'Change list to tuple', 'Add range around numbers'],
    correctIndex: 0, explanation: 'The variable name "squared" implies x**2 (x to the power of 2), but the code uses x*2 (doubling). Change to x**2 for correct squaring.' },
{ id: 'bh-py-m2009', game: 'bug-hunter', language: 'python', difficulty: 'medium',
    code: 'class Dog:\n    def __init__(self, name, age):\n        name = name\n        age = age\n\nd = Dog("Rex", 5)\nprint(d.name)',
    options: ['Add self. prefix: self.name = name', 'Change __init__ to __new__', 'Make name and age class variables', 'Add @property decorator'],
    correctIndex: 0, explanation: 'Without self., the assignments name = name just rebind local variables. Instance attributes must use self.name = name.' },
{ id: 'bh-py-m2010', game: 'bug-hunter', language: 'python', difficulty: 'medium',
    code: 'with open("data.txt", "r") as f:\n    lines = f.readline()\n    for line in lines:\n        print(line.strip())',
    options: ['Use f.readlines() instead of f.readline()', 'Use f.read() instead', 'Change "r" to "rb"', 'Add encoding parameter'],
    correctIndex: 0, explanation: 'f.readline() reads only one line as a string. Iterating over a string iterates character by character. Use f.readlines() or iterate f directly.' },
{ id: 'bh-py-m2011', game: 'bug-hunter', language: 'python', difficulty: 'medium',
    code: 'def remove_duplicates(lst):\n    return list(set(lst))\n\nprint(remove_duplicates([3, 1, 2, 3, 2]))',
    options: ['This loses the original order — use dict.fromkeys(lst)', 'This raises TypeError', 'set() does not work on lists', 'There is no bug in this code'],
    correctIndex: 0, explanation: 'Converting to a set removes duplicates but loses insertion order. Use dict.fromkeys(lst) to preserve order while removing duplicates.' },
{ id: 'bh-py-m2012', game: 'bug-hunter', language: 'python', difficulty: 'medium',
    code: 'def merge_dicts(a, b):\n    return a.update(b)\n\nresult = merge_dicts({"x": 1}, {"y": 2})\nprint(result)',
    options: ['Use {**a, **b} instead of a.update(b)', 'Change update to merge', 'Add a.copy() before update', 'Use dict.concat(a, b)'],
    correctIndex: 0, explanation: 'dict.update() modifies a in-place and returns None. Use {**a, **b} to create a new merged dict without mutating the originals.' },
{ id: 'bh-py-m2013', game: 'bug-hunter', language: 'python', difficulty: 'medium',
    code: 'nums = [1, 2, 3, 4, 5]\nevens = filter(lambda x: x % 2 == 0, nums)\nprint(evens[0])',
    options: ['Wrap with list(): list(filter(...))[0]', 'Use next() with iterator', 'Change filter to comprehension', 'Both A and C work'],
    correctIndex: 3, explanation: 'filter() returns an iterator, not a list. You cannot index it. Either convert with list() or use a list comprehension like [x for x in nums if x % 2 == 0].' },
{ id: 'bh-py-m2014', game: 'bug-hunter', language: 'python', difficulty: 'medium',
    code: 'def create_counters():\n    counters = []\n    for i in range(3):\n        counters.append(lambda: i)\n    return counters\n\nfor c in create_counters():\n    print(c())',
    options: ['Use lambda i=i: i to capture current value', 'Change range(3) to range(4)', 'Add global i declaration', 'Use def instead of lambda'],
    correctIndex: 0, explanation: 'All lambdas share the same variable i. By the time they are called, i is 2. Use default argument lambda i=i: i to capture each value.' },
{ id: 'bh-py-m2015', game: 'bug-hunter', language: 'python', difficulty: 'medium',
    code: 'def safe_divide(a, b):\n    try:\n        return a / b\n    except:\n        return 0\n\nprint(safe_divide("10", 2))',
    options: ['Add type conversion: float(a) / float(b)', 'Change except to except ZeroDivisionError', 'Change return 0 to return None', 'Add isinstance check before division'],
    correctIndex: 0, explanation: 'The bare except catches all errors including TypeError from string division, masking the real issue. Convert inputs to float() to handle string arguments.' },
{ id: 'bh-py-m2016', game: 'bug-hunter', language: 'python', difficulty: 'medium',
    code: 'data = [("Alice", 90), ("Bob", 85), ("Charlie", 92)]\ndata.sort(key=lambda x: x[1])\nprint(data[0])',
    options: ['This works correctly — sorts by score ascending', 'Use reverse=True for descending', 'Change x[1] to x[0]', 'Use sorted() instead of sort()'],
    correctIndex: 0, explanation: 'The code sorts by the second element (score) in ascending order. The result is correct — ("Bob", 85) comes first as the lowest score.' },
{ id: 'bh-py-m2017', game: 'bug-hunter', language: 'python', difficulty: 'medium',
    code: 'class Counter:\n    count = 0\n    def increment(self):\n        count += 1\n\nc = Counter()\nc.increment()',
    options: ['Add self. prefix: self.count += 1', 'Add global count declaration', 'Change count to Counter.count', 'Make increment a @classmethod'],
    correctIndex: 0, explanation: 'count += 1 tries to assign to a local variable, not the class attribute. Use self.count += 1 to modify the instance attribute.' },
{ id: 'bh-py-m2018', game: 'bug-hunter', language: 'python', difficulty: 'medium',
    code: 'def flatten(lst):\n    return [item for sublist in lst for item in sublist]\n\nprint(flatten([[1, 2], [3], 4]))',
    options: ['Handle non-iterable items: use isinstance check', 'Add try/except around item', 'Change flatten to use sum()', 'Use itertools.chain instead'],
    correctIndex: 0, explanation: 'The comprehension fails when it encounters 4 (not iterable). Add an isinstance check or handle mixed flat/nested items gracefully.' },
{ id: 'bh-py-m2019', game: 'bug-hunter', language: 'python', difficulty: 'medium',
    code: 'import datetime\ndate_str = "2024-01-15"\nd = datetime.datetime.strptime(date_str, "%d-%m-%Y")\nprint(d)',
    options: ['Swap format to "%Y-%m-%d" to match the date string', 'Change strptime to strftime', 'Add timezone info', 'Use datetime.date instead of datetime.datetime'],
    correctIndex: 0, explanation: 'The format string "%d-%m-%Y" expects day-month-year, but the date string "2024-01-15" is year-month-day. Use "%Y-%m-%d".' },
{ id: 'bh-py-m2020', game: 'bug-hunter', language: 'python', difficulty: 'medium',
    code: 'def chunk_list(lst, size):\n    return [lst[i:i+size] for i in range(0, len(lst), size)]\n\nprint(chunk_list([1, 2, 3, 4, 5], 0))',
    options: ['Add a guard: if size <= 0: return []', 'Change range step to size or 1', 'Use while loop instead', 'Add try/except for ValueError'],
    correctIndex: 0, explanation: 'When size is 0, range(0, 5, 0) raises ValueError because the step cannot be zero. Add a guard clause to handle size <= 0.' },
{ id: 'bh-py-m2021', game: 'bug-hunter', language: 'python', difficulty: 'medium',
    code: 'names = ["Alice", "Bob", "Charlie"]\nfor i in range(len(names)):\n    if names[i] == "Bob":\n        names.pop(i)\nprint(names)',
    options: ['Use list comprehension: [n for n in names if n != "Bob"]', 'Iterate backwards', 'Use names.remove("Bob") instead', 'Both A and C work correctly'],
    correctIndex: 3, explanation: 'Modifying a list while iterating by index can skip elements. Using names.remove("Bob") or a list comprehension avoids this issue.' },
{ id: 'bh-py-m2022', game: 'bug-hunter', language: 'python', difficulty: 'medium',
    code: 'def greet(name="World"):\n    return f"Hello, {name}!"\n\nprint(greet(name="Alice", title="Ms"))',
    options: ['Remove the title keyword argument from the call', 'Add title parameter to the function', 'Use **kwargs in function signature', 'Change f-string to .format()'],
    correctIndex: 0, explanation: 'The function does not accept a title parameter. Passing title="Ms" raises a TypeError. Remove the extra keyword argument.' },
{ id: 'bh-py-m2023', game: 'bug-hunter', language: 'python', difficulty: 'medium',
    code: 'def get_grade(score):\n    if score >= 90:\n        return "A"\n    elif score >= 80:\n        return "B"\n    elif score >= 70\n        return "C"\n    else:\n        return "F"',
    options: ['Add a colon after score >= 70', 'Change elif to else if', 'Add return "D" between C and F', 'Remove the else clause'],
    correctIndex: 0, explanation: 'Python requires a colon after each elif condition. The elif score >= 70 line is missing its colon, causing a syntax error.' },
{ id: 'bh-py-m2024', game: 'bug-hunter', language: 'python', difficulty: 'medium',
    code: 'def average(nums):\n    return sum(nums) / len(nums)\n\nprint(average([]))',
    options: ['Add a check for empty list before dividing', 'Use statistics.mean() instead', 'Wrap in try/except', 'Change len() to max(len(), 1)'],
    correctIndex: 0, explanation: 'When nums is empty, len(nums) is 0, causing ZeroDivisionError. Add a guard: if not nums: return 0 or raise a meaningful exception.' },
{ id: 'bh-py-m2025', game: 'bug-hunter', language: 'python', difficulty: 'medium',
    code: 'data = {"name": "Alice", "scores": [90, 85, 92]}\ncopy = data\ncopy["scores"].append(88)\nprint(data["scores"])',
    options: ['Use copy.deepcopy(data) instead of direct assignment', 'Use data.copy() for shallow copy', 'Change append to extend', 'Use dict(data) for the copy'],
    correctIndex: 0, explanation: 'copy = data creates a reference, not a copy. Modifying copy also modifies data. Use copy.deepcopy() to create a fully independent copy.' },
{ id: 'bh-py-m2026', game: 'bug-hunter', language: 'python', difficulty: 'medium',
    code: 'def find_max(numbers):\n    max_val = 0\n    for n in numbers:\n        if n > max_val:\n            max_val = n\n    return max_val\n\nprint(find_max([-5, -2, -10]))',
    options: ['Initialize max_val with numbers[0] instead of 0', 'Use max() built-in function', 'Change > to >=', 'Add abs() around n'],
    correctIndex: 0, explanation: 'Initializing max_val to 0 fails when all numbers are negative. Use numbers[0] as the initial value or just use the built-in max().' },
{ id: 'bh-py-m2027', game: 'bug-hunter', language: 'python', difficulty: 'medium',
    code: 'def is_palindrome(s):\n    return s == s[::-1]\n\nprint(is_palindrome("Racecar"))',
    options: ['Normalize case: s.lower() == s.lower()[::-1]', 'Use reversed(s) instead of slicing', 'Strip whitespace first', 'Add s.strip() before comparison'],
    correctIndex: 0, explanation: '"Racecar" is a palindrome, but "R" != "r" in the reversed string. Normalize case with s.lower() before comparing.' },
{ id: 'bh-py-m2028', game: 'bug-hunter', language: 'python', difficulty: 'medium',
    code: 'def read_config():\n    with open("config.json") as f:\n        config = eval(f.read())\n    return config',
    options: ['Use json.load(f) instead of eval(f.read())', 'Add encoding="utf-8" to open', 'Use ast.literal_eval instead', 'Both A and C are safer alternatives'],
    correctIndex: 3, explanation: 'eval() is dangerous — it can execute arbitrary code. json.load() is safe for JSON data, and ast.literal_eval() safely evaluates Python literals.' },
{ id: 'bh-py-m2029', game: 'bug-hunter', language: 'python', difficulty: 'medium',
    code: 'class Stack:\n    def __init__(self):\n        self.items = []\n    def push(self, item):\n        self.items.insert(0, item)\n    def pop(self):\n        return self.items.pop(0)',
    options: ['Use append() and pop() for O(1) operations', 'Use collections.deque instead', 'Change insert(0) to append()', 'Both A and C are equivalent fixes'],
    correctIndex: 3, explanation: 'insert(0) and pop(0) are O(n) operations. Using append() and pop() (from the end) gives O(1) performance for a stack.' },
{ id: 'bh-py-m2030', game: 'bug-hunter', language: 'python', difficulty: 'medium',
    code: 'def matrix_sum(matrix):\n    total = 0\n    for row in matrix:\n        for col in row:\n            total += col\n    return total\n\nprint(matrix_sum([[1, 2], [3, "4"]]))',
    options: ['Add type check or convert: total += int(col)', 'Change "4" to 4 in the data', 'Use sum(map(sum, matrix))', 'Add try/except around total += col'],
    correctIndex: 0, explanation: 'The string "4" causes a TypeError when added to an integer. Convert col to int() or validate the matrix data before summing.' },
{ id: 'bh-py-m2031', game: 'bug-hunter', language: 'python', difficulty: 'medium',
    code: 'def process(data=None):\n    if data is None:\n        data = []\n    data.append(1)\n    return data\n\nprint(process())\nprint(process())',
    options: ['This is correct — each call gets a fresh list', 'data should be [] in the parameter', 'Use tuple instead of list', 'Add data = list(data) at start'],
    correctIndex: 0, explanation: 'This code is actually correct! Using None as the default and creating a new list inside the function avoids the mutable default argument trap.' },
{ id: 'bh-py-m2032', game: 'bug-hunter', language: 'python', difficulty: 'medium',
    code: 'def search(items, target):\n    for i in range(len(items)):\n        if items[i] == target:\n            return i\n        return -1\n\nprint(search([1, 2, 3], 2))',
    options: ['Dedent return -1 to be outside the for loop', 'Change range to enumerate', 'Use items.index(target)', 'Add break after return i'],
    correctIndex: 0, explanation: 'The return -1 is incorrectly indented inside the for loop, so it returns -1 after checking only the first element. Move it outside the loop.' },
{ id: 'bh-py-m2033', game: 'bug-hunter', language: 'python', difficulty: 'medium',
    code: 'def fibonacci(n):\n    a, b = 0, 1\n    for _ in range(n):\n        a, b = b, a + b\n    return a\n\nprint(fibonacci(-1))',
    options: ['Add guard: if n < 0: raise ValueError', 'Change range to range(max(0, n))', 'Use while loop instead', 'Add try/except for negative input'],
    correctIndex: 0, explanation: 'range(-1) produces an empty sequence, so fibonacci(-1) returns 0 which is incorrect. Add validation for negative inputs.' },
{ id: 'bh-py-m2034', game: 'bug-hunter', language: 'python', difficulty: 'medium',
    code: 'result = "hello".replace("l", "L", 1)\nprint(result.count("l"))',
    options: ['This prints 1 — only one "l" was replaced, one remains', 'This prints 0', 'This prints 2', 'This raises an error'],
    correctIndex: 0, explanation: 'replace("l", "L", 1) only replaces the first occurrence. "hello" becomes "heLlo", which still has one "l". count("l") returns 1.' },
{ id: 'bh-py-m2035', game: 'bug-hunter', language: 'python', difficulty: 'medium',
    code: 'def split_pairs(s):\n    return [s[i:i+2] for i in range(0, len(s), 2)]\n\nprint(split_pairs("abc"))',
    options: ['This works: returns ["ab", "c"] — the last char gets its own string', 'This raises an IndexError', 'This returns ["ab"] only', 'Need to pad with space: s.ljust(len(s)+1)'],
    correctIndex: 0, explanation: 'The code works correctly. Slice s[2:4] on "abc" returns "c" without error. Python slicing does not raise IndexError for out-of-range slices.' },
{ id: 'bh-py-m2036', game: 'bug-hunter', language: 'python', difficulty: 'medium',
    code: 'def capitalize_words(text):\n    return " ".join(w.capitalize() for w in text.split(","))\n\nprint(capitalize_words("hello world"))',
    options: ['Split on spaces: text.split() instead of text.split(",")', 'Use title() instead', 'Add .strip() after split', 'Change join separator to ","'],
    correctIndex: 0, explanation: 'split(",") splits on commas but the string uses spaces. Use text.split() to split on whitespace, producing ["hello", "world"].' },
{ id: 'bh-py-m2037', game: 'bug-hunter', language: 'python', difficulty: 'medium',
    code: 'import re\npattern = re.compile("[0-9+")\nprint(pattern.match("123"))',
    options: ['Close the bracket: "[0-9]+" with closing ]', 'Use match() instead of compile()', 'Escape the + sign', 'Change [0-9+] to \\d+'],
    correctIndex: 0, explanation: 'The regex "[0-9+" has an unclosed character class bracket. It should be "[0-9]+" with the closing bracket, or simply "\d+".' },
{ id: 'bh-py-m2038', game: 'bug-hunter', language: 'python', difficulty: 'medium',
    code: 'class Animal:\n    def speak(self):\n        return "Some sound"\n\nclass Dog(Animal):\n    def speak():\n        return "Woof!"\n\nd = Dog()\nprint(d.speak())',
    options: ['Add self parameter to Dog.speak(self)', 'Add @staticmethod decorator', 'Change speak() to __speak()', 'Use super().speak() inside Dog'],
    correctIndex: 0, explanation: 'Dog.speak() is missing the self parameter. Python instance methods must include self as the first parameter.' },
{ id: 'bh-py-m2039', game: 'bug-hunter', language: 'python', difficulty: 'medium',
    code: 'def count_word(text, word):\n    return text.count(word)\n\nprint(count_word("the cat and the dog", "the"))',
    options: ['This returns 2 — correct for non-overlapping occurrences', 'This returns 1', 'This counts overlapping occurrences', 'Need to use regex instead'],
    correctIndex: 0, explanation: 'str.count() correctly counts non-overlapping occurrences. "the" appears twice in the string, so it returns 2.' },
{ id: 'bh-py-m2040', game: 'bug-hunter', language: 'python', difficulty: 'medium',
    code: 'def make_pair(a, b):\n    return a, b\n\nx, y = make_pair(1, 2, 3)',
    options: ['Pass only 2 arguments or change function to accept *args', 'Add a third return value', 'Change make_pair to return a list', 'Use *x instead of x, y'],
    correctIndex: 0, explanation: 'make_pair returns a 2-tuple but the caller tries to unpack 3 values. Either pass 2 arguments or change the function to accept variable arguments.' },
{ id: 'bh-py-m2041', game: 'bug-hunter', language: 'python', difficulty: 'medium',
    code: 'items = [1, 2, 3]\nresult = items * 2\nprint(result)',
    options: ['This returns [1, 2, 3, 1, 2, 3] — list repetition, not multiplication', 'This returns [2, 4, 6]', 'This raises TypeError', 'This returns [1, 2, 3, 2]'],
    correctIndex: 0, explanation: 'The * operator on lists performs repetition, not element-wise multiplication. items * 2 produces [1, 2, 3, 1, 2, 3]. Use a list comprehension for element-wise math.' },
{ id: 'bh-py-m2042', game: 'bug-hunter', language: 'python', difficulty: 'medium',
    code: 'def first_non_repeating(s):\n    for char in s:\n        if s.count(char) == 1:\n            return char\n    return None\n\nprint(first_non_repeating("aabbc"))',
    options: ['This returns "c" correctly — it is the first non-repeating char', 'This returns "a"', 'This returns None', 'This raises an error'],
    correctIndex: 0, explanation: 'The code correctly finds the first character with count 1. "a" has count 2, "b" has count 2, "c" has count 1 — so it returns "c".' },
{ id: 'bh-py-m2043', game: 'bug-hunter', language: 'python', difficulty: 'medium',
    code: 'def retry(func, max_attempts=3):\n    for attempt in range(max_attempts):\n        try:\n            return func()\n        except Exception:\n            continue\n    return None\n\nprint(retry(lambda: 1/0))',
    options: ['Add attempt logging and raise after max attempts instead of returning None', 'Change continue to pass', 'Remove the try/except', 'Use while loop instead'],
    correctIndex: 0, explanation: 'Silently returning None after all retries fail hides the error. It should raise the last exception or at least log which attempts failed.' },
{ id: 'bh-py-m2044', game: 'bug-hunter', language: 'python', difficulty: 'medium',
    code: 'd = {"x": 1, "y": 2}\nfor key in d:\n    d[key + "_copy"] = d[key]\nprint(d)',
    options: ['This causes RuntimeError: dictionary changed size during iteration', 'This works and adds _copy keys', 'This only adds one key', 'This raises KeyError'],
    correctIndex: 0, explanation: 'Modifying a dictionary while iterating over it raises RuntimeError. Create a list of keys first: for key in list(d): to avoid this.' },
{ id: 'bh-py-m2045', game: 'bug-hunter', language: 'python', difficulty: 'medium',
    code: 'def parse_int(s):\n    try:\n        return int(s)\n    except:\n        return float(s)\n\nprint(parse_int("3.14"))',
    options: ['The bare except catches int() failure, then float("3.14") returns 3.14, not an int', 'This returns 3', 'This raises ValueError', 'Need to add except ValueError specifically'],
    correctIndex: 0, explanation: 'The bare except catches the ValueError from int("3.14"), then float("3.14") returns 3.14. The function name "parse_int" is misleading since it can return a float.' },
{ id: 'bh-py-m2046', game: 'bug-hunter', language: 'python', difficulty: 'medium',
    code: 'def batch_process(items, batch_size=100):\n    results = []\n    for i in range(0, len(items), batch_size):\n        batch = items[i:i + batch_size]\n        results.append(process(batch))\n    return results',
    options: ['process() is not defined — missing function or import', 'batch_size is too large', 'Need to use enumerate instead', 'range step should be 1'],
    correctIndex: 0, explanation: 'The function calls process(batch) but process is never defined or imported. This will raise a NameError at runtime.' },
{ id: 'bh-py-m2047', game: 'bug-hunter', language: 'python', difficulty: 'medium',
    code: 'squares = {x: x*x for x in range(5)}\nprint(squares[5])',
    options: ['Use squares.get(5) or access an existing key like squares[4]', 'Change range(5) to range(6)', 'Use squares.keys() to check first', 'Both A and B are valid fixes'],
    correctIndex: 3, explanation: 'The dictionary has keys 0-4. Accessing key 5 raises KeyError. Either expand the range to include 5, or use .get(5) with a default value.' },
{ id: 'bh-py-m2048', game: 'bug-hunter', language: 'python', difficulty: 'medium',
    code: 'def parse_config(lines):\n    config = {}\n    for line in lines:\n        key, value = line.split("=")\n        config[key.strip()] = value.strip()\n    return config',
    options: ['split("=") fails on lines without "=" — add if "=" in line check', 'Use yaml.safe_load instead', 'Add try/except around split', 'Change split to partition'],
    correctIndex: 0, explanation: 'Lines without an "=" character cause split("=") to raise ValueError. Add a guard: if "=" in line before splitting, or use partition("=") which always returns 3 values.' },
