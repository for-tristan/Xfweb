{ id: 'bh-cs-h2001', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'class Singleton {\n    static Singleton* instance;\n    Singleton() {}\npublic:\n    static Singleton* getInstance() {\n        if (!instance) instance = new Singleton();\n        return instance;\n    }\n};\nSingleton* Singleton::instance = nullptr;',
    options: ['Not thread-safe and memory leak — use Meyers Singleton: static Singleton& getInstance() { static Singleton instance; return instance; }', 'This is correct singleton', 'Add volatile keyword', 'Use shared_ptr instead'],
    correctIndex: 0, explanation: 'The lazy initialization is not thread-safe. Also, the new Singleton() is never deleted. Use Meyers Singleton pattern which is thread-safe since C++11 and has no memory leak.' },
{ id: 'bh-cs-h2002', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'class Singleton {\n    static Singleton* instance;\n    Singleton() {}\npublic:\n    static Singleton* getInstance() {\n        if (!instance) instance = new Singleton();\n        return instance;\n    }\n};\nSingleton* Singleton::instance = nullptr;',
    options: ['Not thread-safe and memory leak — use Meyers Singleton: static Singleton& getInstance() { static Singleton instance; return instance; }', 'This is correct singleton', 'Add volatile keyword', 'Use shared_ptr instead'],
    correctIndex: 0, explanation: 'The lazy initialization is not thread-safe. Also, the new Singleton() is never deleted. Use Meyers Singleton pattern which is thread-safe since C++11 and has no memory leak.' },
{ id: 'bh-cs-h2003', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'class Singleton {\n    static Singleton* instance;\n    Singleton() {}\npublic:\n    static Singleton* getInstance() {\n        if (!instance) instance = new Singleton();\n        return instance;\n    }\n};\nSingleton* Singleton::instance = nullptr;',
    options: ['Not thread-safe and memory leak — use Meyers Singleton: static Singleton& getInstance() { static Singleton instance; return instance; }', 'This is correct singleton', 'Add volatile keyword', 'Use shared_ptr instead'],
    correctIndex: 0, explanation: 'The lazy initialization is not thread-safe. Also, the new Singleton() is never deleted. Use Meyers Singleton pattern which is thread-safe since C++11 and has no memory leak.' },
{ id: 'bh-cs-h2004', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'class Singleton {\n    static Singleton* instance;\n    Singleton() {}\npublic:\n    static Singleton* getInstance() {\n        if (!instance) instance = new Singleton();\n        return instance;\n    }\n};\nSingleton* Singleton::instance = nullptr;',
    options: ['Not thread-safe and memory leak — use Meyers Singleton: static Singleton& getInstance() { static Singleton instance; return instance; }', 'This is correct singleton', 'Add volatile keyword', 'Use shared_ptr instead'],
    correctIndex: 0, explanation: 'The lazy initialization is not thread-safe. Also, the new Singleton() is never deleted. Use Meyers Singleton pattern which is thread-safe since C++11 and has no memory leak.' },
{ id: 'bh-cs-h2005', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'class Singleton {\n    static Singleton* instance;\n    Singleton() {}\npublic:\n    static Singleton* getInstance() {\n        if (!instance) instance = new Singleton();\n        return instance;\n    }\n};\nSingleton* Singleton::instance = nullptr;',
    options: ['Not thread-safe and memory leak — use Meyers Singleton: static Singleton& getInstance() { static Singleton instance; return instance; }', 'This is correct singleton', 'Add volatile keyword', 'Use shared_ptr instead'],
    correctIndex: 0, explanation: 'The lazy initialization is not thread-safe. Also, the new Singleton() is never deleted. Use Meyers Singleton pattern which is thread-safe since C++11 and has no memory leak.' },
{ id: 'bh-cs-h2006', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'class Singleton {\n    static Singleton* instance;\n    Singleton() {}\npublic:\n    static Singleton* getInstance() {\n        if (!instance) instance = new Singleton();\n        return instance;\n    }\n};\nSingleton* Singleton::instance = nullptr;',
    options: ['Not thread-safe and memory leak — use Meyers Singleton: static Singleton& getInstance() { static Singleton instance; return instance; }', 'This is correct singleton', 'Add volatile keyword', 'Use shared_ptr instead'],
    correctIndex: 0, explanation: 'The lazy initialization is not thread-safe. Also, the new Singleton() is never deleted. Use Meyers Singleton pattern which is thread-safe since C++11 and has no memory leak.' },
{ id: 'bh-cs-h2007', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'class Singleton {\n    static Singleton* instance;\n    Singleton() {}\npublic:\n    static Singleton* getInstance() {\n        if (!instance) instance = new Singleton();\n        return instance;\n    }\n};\nSingleton* Singleton::instance = nullptr;',
    options: ['Not thread-safe and memory leak — use Meyers Singleton: static Singleton& getInstance() { static Singleton instance; return instance; }', 'This is correct singleton', 'Add volatile keyword', 'Use shared_ptr instead'],
    correctIndex: 0, explanation: 'The lazy initialization is not thread-safe. Also, the new Singleton() is never deleted. Use Meyers Singleton pattern which is thread-safe since C++11 and has no memory leak.' },
{ id: 'bh-cs-h2008', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'class Singleton {\n    static Singleton* instance;\n    Singleton() {}\npublic:\n    static Singleton* getInstance() {\n        if (!instance) instance = new Singleton();\n        return instance;\n    }\n};\nSingleton* Singleton::instance = nullptr;',
    options: ['Not thread-safe and memory leak — use Meyers Singleton: static Singleton& getInstance() { static Singleton instance; return instance; }', 'This is correct singleton', 'Add volatile keyword', 'Use shared_ptr instead'],
    correctIndex: 0, explanation: 'The lazy initialization is not thread-safe. Also, the new Singleton() is never deleted. Use Meyers Singleton pattern which is thread-safe since C++11 and has no memory leak.' },
{ id: 'bh-cs-h2009', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'class Singleton {\n    static Singleton* instance;\n    Singleton() {}\npublic:\n    static Singleton* getInstance() {\n        if (!instance) instance = new Singleton();\n        return instance;\n    }\n};\nSingleton* Singleton::instance = nullptr;',
    options: ['Not thread-safe and memory leak — use Meyers Singleton: static Singleton& getInstance() { static Singleton instance; return instance; }', 'This is correct singleton', 'Add volatile keyword', 'Use shared_ptr instead'],
    correctIndex: 0, explanation: 'The lazy initialization is not thread-safe. Also, the new Singleton() is never deleted. Use Meyers Singleton pattern which is thread-safe since C++11 and has no memory leak.' },
{ id: 'bh-cs-h2010', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'class Singleton {\n    static Singleton* instance;\n    Singleton() {}\npublic:\n    static Singleton* getInstance() {\n        if (!instance) instance = new Singleton();\n        return instance;\n    }\n};\nSingleton* Singleton::instance = nullptr;',
    options: ['Not thread-safe and memory leak — use Meyers Singleton: static Singleton& getInstance() { static Singleton instance; return instance; }', 'This is correct singleton', 'Add volatile keyword', 'Use shared_ptr instead'],
    correctIndex: 0, explanation: 'The lazy initialization is not thread-safe. Also, the new Singleton() is never deleted. Use Meyers Singleton pattern which is thread-safe since C++11 and has no memory leak.' },
{ id: 'bh-cs-h2011', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'template<typename T>\nclass SmartPtr {\n    T* ptr;\n    int* refCount;\npublic:\n    SmartPtr(T* p) : ptr(p), refCount(new int(1)) {}\n    SmartPtr(const SmartPtr& other) : ptr(other.ptr), refCount(other.refCount) { (*refCount)++; }\n    ~SmartPtr() { if (--(*refCount) == 0) { delete ptr; delete refCount; } }\n};',
    options: ['Not thread-safe — refCount increment/decrement must be atomic. Also missing assignment operator (rule of three)', 'This is a correct smart pointer', 'Use std::shared_ptr instead', 'Both the thread safety and missing operator= are issues'],
    correctIndex: 3, explanation: 'The reference count is not atomic — concurrent copies can cause data races. Also, without operator=, copying a SmartPtr leaks the old object. Use std::shared_ptr or fix both issues.' },
{ id: 'bh-cs-h2012', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'template<typename T>\nclass SmartPtr {\n    T* ptr;\n    int* refCount;\npublic:\n    SmartPtr(T* p) : ptr(p), refCount(new int(1)) {}\n    SmartPtr(const SmartPtr& other) : ptr(other.ptr), refCount(other.refCount) { (*refCount)++; }\n    ~SmartPtr() { if (--(*refCount) == 0) { delete ptr; delete refCount; } }\n};',
    options: ['Not thread-safe — refCount increment/decrement must be atomic. Also missing assignment operator (rule of three)', 'This is a correct smart pointer', 'Use std::shared_ptr instead', 'Both the thread safety and missing operator= are issues'],
    correctIndex: 3, explanation: 'The reference count is not atomic — concurrent copies can cause data races. Also, without operator=, copying a SmartPtr leaks the old object. Use std::shared_ptr or fix both issues.' },
{ id: 'bh-cs-h2013', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'template<typename T>\nclass SmartPtr {\n    T* ptr;\n    int* refCount;\npublic:\n    SmartPtr(T* p) : ptr(p), refCount(new int(1)) {}\n    SmartPtr(const SmartPtr& other) : ptr(other.ptr), refCount(other.refCount) { (*refCount)++; }\n    ~SmartPtr() { if (--(*refCount) == 0) { delete ptr; delete refCount; } }\n};',
    options: ['Not thread-safe — refCount increment/decrement must be atomic. Also missing assignment operator (rule of three)', 'This is a correct smart pointer', 'Use std::shared_ptr instead', 'Both the thread safety and missing operator= are issues'],
    correctIndex: 3, explanation: 'The reference count is not atomic — concurrent copies can cause data races. Also, without operator=, copying a SmartPtr leaks the old object. Use std::shared_ptr or fix both issues.' },
{ id: 'bh-cs-h2014', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'template<typename T>\nclass SmartPtr {\n    T* ptr;\n    int* refCount;\npublic:\n    SmartPtr(T* p) : ptr(p), refCount(new int(1)) {}\n    SmartPtr(const SmartPtr& other) : ptr(other.ptr), refCount(other.refCount) { (*refCount)++; }\n    ~SmartPtr() { if (--(*refCount) == 0) { delete ptr; delete refCount; } }\n};',
    options: ['Not thread-safe — refCount increment/decrement must be atomic. Also missing assignment operator (rule of three)', 'This is a correct smart pointer', 'Use std::shared_ptr instead', 'Both the thread safety and missing operator= are issues'],
    correctIndex: 3, explanation: 'The reference count is not atomic — concurrent copies can cause data races. Also, without operator=, copying a SmartPtr leaks the old object. Use std::shared_ptr or fix both issues.' },
{ id: 'bh-cs-h2015', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'template<typename T>\nclass SmartPtr {\n    T* ptr;\n    int* refCount;\npublic:\n    SmartPtr(T* p) : ptr(p), refCount(new int(1)) {}\n    SmartPtr(const SmartPtr& other) : ptr(other.ptr), refCount(other.refCount) { (*refCount)++; }\n    ~SmartPtr() { if (--(*refCount) == 0) { delete ptr; delete refCount; } }\n};',
    options: ['Not thread-safe — refCount increment/decrement must be atomic. Also missing assignment operator (rule of three)', 'This is a correct smart pointer', 'Use std::shared_ptr instead', 'Both the thread safety and missing operator= are issues'],
    correctIndex: 3, explanation: 'The reference count is not atomic — concurrent copies can cause data races. Also, without operator=, copying a SmartPtr leaks the old object. Use std::shared_ptr or fix both issues.' },
{ id: 'bh-cs-h2016', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'template<typename T>\nclass SmartPtr {\n    T* ptr;\n    int* refCount;\npublic:\n    SmartPtr(T* p) : ptr(p), refCount(new int(1)) {}\n    SmartPtr(const SmartPtr& other) : ptr(other.ptr), refCount(other.refCount) { (*refCount)++; }\n    ~SmartPtr() { if (--(*refCount) == 0) { delete ptr; delete refCount; } }\n};',
    options: ['Not thread-safe — refCount increment/decrement must be atomic. Also missing assignment operator (rule of three)', 'This is a correct smart pointer', 'Use std::shared_ptr instead', 'Both the thread safety and missing operator= are issues'],
    correctIndex: 3, explanation: 'The reference count is not atomic — concurrent copies can cause data races. Also, without operator=, copying a SmartPtr leaks the old object. Use std::shared_ptr or fix both issues.' },
{ id: 'bh-cs-h2017', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'template<typename T>\nclass SmartPtr {\n    T* ptr;\n    int* refCount;\npublic:\n    SmartPtr(T* p) : ptr(p), refCount(new int(1)) {}\n    SmartPtr(const SmartPtr& other) : ptr(other.ptr), refCount(other.refCount) { (*refCount)++; }\n    ~SmartPtr() { if (--(*refCount) == 0) { delete ptr; delete refCount; } }\n};',
    options: ['Not thread-safe — refCount increment/decrement must be atomic. Also missing assignment operator (rule of three)', 'This is a correct smart pointer', 'Use std::shared_ptr instead', 'Both the thread safety and missing operator= are issues'],
    correctIndex: 3, explanation: 'The reference count is not atomic — concurrent copies can cause data races. Also, without operator=, copying a SmartPtr leaks the old object. Use std::shared_ptr or fix both issues.' },
{ id: 'bh-cs-h2018', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'template<typename T>\nclass SmartPtr {\n    T* ptr;\n    int* refCount;\npublic:\n    SmartPtr(T* p) : ptr(p), refCount(new int(1)) {}\n    SmartPtr(const SmartPtr& other) : ptr(other.ptr), refCount(other.refCount) { (*refCount)++; }\n    ~SmartPtr() { if (--(*refCount) == 0) { delete ptr; delete refCount; } }\n};',
    options: ['Not thread-safe — refCount increment/decrement must be atomic. Also missing assignment operator (rule of three)', 'This is a correct smart pointer', 'Use std::shared_ptr instead', 'Both the thread safety and missing operator= are issues'],
    correctIndex: 3, explanation: 'The reference count is not atomic — concurrent copies can cause data races. Also, without operator=, copying a SmartPtr leaks the old object. Use std::shared_ptr or fix both issues.' },
{ id: 'bh-cs-h2019', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'template<typename T>\nclass SmartPtr {\n    T* ptr;\n    int* refCount;\npublic:\n    SmartPtr(T* p) : ptr(p), refCount(new int(1)) {}\n    SmartPtr(const SmartPtr& other) : ptr(other.ptr), refCount(other.refCount) { (*refCount)++; }\n    ~SmartPtr() { if (--(*refCount) == 0) { delete ptr; delete refCount; } }\n};',
    options: ['Not thread-safe — refCount increment/decrement must be atomic. Also missing assignment operator (rule of three)', 'This is a correct smart pointer', 'Use std::shared_ptr instead', 'Both the thread safety and missing operator= are issues'],
    correctIndex: 3, explanation: 'The reference count is not atomic — concurrent copies can cause data races. Also, without operator=, copying a SmartPtr leaks the old object. Use std::shared_ptr or fix both issues.' },
{ id: 'bh-cs-h2020', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'template<typename T>\nclass SmartPtr {\n    T* ptr;\n    int* refCount;\npublic:\n    SmartPtr(T* p) : ptr(p), refCount(new int(1)) {}\n    SmartPtr(const SmartPtr& other) : ptr(other.ptr), refCount(other.refCount) { (*refCount)++; }\n    ~SmartPtr() { if (--(*refCount) == 0) { delete ptr; delete refCount; } }\n};',
    options: ['Not thread-safe — refCount increment/decrement must be atomic. Also missing assignment operator (rule of three)', 'This is a correct smart pointer', 'Use std::shared_ptr instead', 'Both the thread safety and missing operator= are issues'],
    correctIndex: 3, explanation: 'The reference count is not atomic — concurrent copies can cause data races. Also, without operator=, copying a SmartPtr leaks the old object. Use std::shared_ptr or fix both issues.' },
{ id: 'bh-cs-h2021', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'class Buffer {\n    char* data;\n    size_t size;\npublic:\n    Buffer(size_t sz) : size(sz), data(new char[sz]) {}\n    ~Buffer() { delete[] data; }\n    Buffer& operator=(const Buffer& other) {\n        delete[] data;\n        data = new char[other.size];\n        size = other.size;\n        memcpy(data, other.data, size);\n        return *this;\n    }\n};',
    options: ['Self-assignment check missing and not exception-safe — if new throws after delete[], data is dangling. Use copy-and-swap idiom', 'This is correct copy assignment', 'Add null check for data', 'Use vector<char> instead'],
    correctIndex: 0, explanation: 'If new throws after delete[], the object is in an invalid state. Also, self-assignment (b = b) deletes its own data. Use the copy-and-swap idiom for exception safety.' },
{ id: 'bh-cs-h2022', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'class Buffer {\n    char* data;\n    size_t size;\npublic:\n    Buffer(size_t sz) : size(sz), data(new char[sz]) {}\n    ~Buffer() { delete[] data; }\n    Buffer& operator=(const Buffer& other) {\n        delete[] data;\n        data = new char[other.size];\n        size = other.size;\n        memcpy(data, other.data, size);\n        return *this;\n    }\n};',
    options: ['Self-assignment check missing and not exception-safe — if new throws after delete[], data is dangling. Use copy-and-swap idiom', 'This is correct copy assignment', 'Add null check for data', 'Use vector<char> instead'],
    correctIndex: 0, explanation: 'If new throws after delete[], the object is in an invalid state. Also, self-assignment (b = b) deletes its own data. Use the copy-and-swap idiom for exception safety.' },
{ id: 'bh-cs-h2023', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'class Buffer {\n    char* data;\n    size_t size;\npublic:\n    Buffer(size_t sz) : size(sz), data(new char[sz]) {}\n    ~Buffer() { delete[] data; }\n    Buffer& operator=(const Buffer& other) {\n        delete[] data;\n        data = new char[other.size];\n        size = other.size;\n        memcpy(data, other.data, size);\n        return *this;\n    }\n};',
    options: ['Self-assignment check missing and not exception-safe — if new throws after delete[], data is dangling. Use copy-and-swap idiom', 'This is correct copy assignment', 'Add null check for data', 'Use vector<char> instead'],
    correctIndex: 0, explanation: 'If new throws after delete[], the object is in an invalid state. Also, self-assignment (b = b) deletes its own data. Use the copy-and-swap idiom for exception safety.' },
{ id: 'bh-cs-h2024', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'class Buffer {\n    char* data;\n    size_t size;\npublic:\n    Buffer(size_t sz) : size(sz), data(new char[sz]) {}\n    ~Buffer() { delete[] data; }\n    Buffer& operator=(const Buffer& other) {\n        delete[] data;\n        data = new char[other.size];\n        size = other.size;\n        memcpy(data, other.data, size);\n        return *this;\n    }\n};',
    options: ['Self-assignment check missing and not exception-safe — if new throws after delete[], data is dangling. Use copy-and-swap idiom', 'This is correct copy assignment', 'Add null check for data', 'Use vector<char> instead'],
    correctIndex: 0, explanation: 'If new throws after delete[], the object is in an invalid state. Also, self-assignment (b = b) deletes its own data. Use the copy-and-swap idiom for exception safety.' },
{ id: 'bh-cs-h2025', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'class Buffer {\n    char* data;\n    size_t size;\npublic:\n    Buffer(size_t sz) : size(sz), data(new char[sz]) {}\n    ~Buffer() { delete[] data; }\n    Buffer& operator=(const Buffer& other) {\n        delete[] data;\n        data = new char[other.size];\n        size = other.size;\n        memcpy(data, other.data, size);\n        return *this;\n    }\n};',
    options: ['Self-assignment check missing and not exception-safe — if new throws after delete[], data is dangling. Use copy-and-swap idiom', 'This is correct copy assignment', 'Add null check for data', 'Use vector<char> instead'],
    correctIndex: 0, explanation: 'If new throws after delete[], the object is in an invalid state. Also, self-assignment (b = b) deletes its own data. Use the copy-and-swap idiom for exception safety.' },
{ id: 'bh-cs-h2026', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'class Buffer {\n    char* data;\n    size_t size;\npublic:\n    Buffer(size_t sz) : size(sz), data(new char[sz]) {}\n    ~Buffer() { delete[] data; }\n    Buffer& operator=(const Buffer& other) {\n        delete[] data;\n        data = new char[other.size];\n        size = other.size;\n        memcpy(data, other.data, size);\n        return *this;\n    }\n};',
    options: ['Self-assignment check missing and not exception-safe — if new throws after delete[], data is dangling. Use copy-and-swap idiom', 'This is correct copy assignment', 'Add null check for data', 'Use vector<char> instead'],
    correctIndex: 0, explanation: 'If new throws after delete[], the object is in an invalid state. Also, self-assignment (b = b) deletes its own data. Use the copy-and-swap idiom for exception safety.' },
{ id: 'bh-cs-h2027', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'class Buffer {\n    char* data;\n    size_t size;\npublic:\n    Buffer(size_t sz) : size(sz), data(new char[sz]) {}\n    ~Buffer() { delete[] data; }\n    Buffer& operator=(const Buffer& other) {\n        delete[] data;\n        data = new char[other.size];\n        size = other.size;\n        memcpy(data, other.data, size);\n        return *this;\n    }\n};',
    options: ['Self-assignment check missing and not exception-safe — if new throws after delete[], data is dangling. Use copy-and-swap idiom', 'This is correct copy assignment', 'Add null check for data', 'Use vector<char> instead'],
    correctIndex: 0, explanation: 'If new throws after delete[], the object is in an invalid state. Also, self-assignment (b = b) deletes its own data. Use the copy-and-swap idiom for exception safety.' },
{ id: 'bh-cs-h2028', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'class Buffer {\n    char* data;\n    size_t size;\npublic:\n    Buffer(size_t sz) : size(sz), data(new char[sz]) {}\n    ~Buffer() { delete[] data; }\n    Buffer& operator=(const Buffer& other) {\n        delete[] data;\n        data = new char[other.size];\n        size = other.size;\n        memcpy(data, other.data, size);\n        return *this;\n    }\n};',
    options: ['Self-assignment check missing and not exception-safe — if new throws after delete[], data is dangling. Use copy-and-swap idiom', 'This is correct copy assignment', 'Add null check for data', 'Use vector<char> instead'],
    correctIndex: 0, explanation: 'If new throws after delete[], the object is in an invalid state. Also, self-assignment (b = b) deletes its own data. Use the copy-and-swap idiom for exception safety.' },
{ id: 'bh-cs-h2029', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'class Buffer {\n    char* data;\n    size_t size;\npublic:\n    Buffer(size_t sz) : size(sz), data(new char[sz]) {}\n    ~Buffer() { delete[] data; }\n    Buffer& operator=(const Buffer& other) {\n        delete[] data;\n        data = new char[other.size];\n        size = other.size;\n        memcpy(data, other.data, size);\n        return *this;\n    }\n};',
    options: ['Self-assignment check missing and not exception-safe — if new throws after delete[], data is dangling. Use copy-and-swap idiom', 'This is correct copy assignment', 'Add null check for data', 'Use vector<char> instead'],
    correctIndex: 0, explanation: 'If new throws after delete[], the object is in an invalid state. Also, self-assignment (b = b) deletes its own data. Use the copy-and-swap idiom for exception safety.' },
{ id: 'bh-cs-h2030', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'class Buffer {\n    char* data;\n    size_t size;\npublic:\n    Buffer(size_t sz) : size(sz), data(new char[sz]) {}\n    ~Buffer() { delete[] data; }\n    Buffer& operator=(const Buffer& other) {\n        delete[] data;\n        data = new char[other.size];\n        size = other.size;\n        memcpy(data, other.data, size);\n        return *this;\n    }\n};',
    options: ['Self-assignment check missing and not exception-safe — if new throws after delete[], data is dangling. Use copy-and-swap idiom', 'This is correct copy assignment', 'Add null check for data', 'Use vector<char> instead'],
    correctIndex: 0, explanation: 'If new throws after delete[], the object is in an invalid state. Also, self-assignment (b = b) deletes its own data. Use the copy-and-swap idiom for exception safety.' },
{ id: 'bh-cs-h2031', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'template<typename T>\nT max(T a, T b) {\n    return (a > b) ? a : b;\n}\n\ncout << max(3, 3.14);',
    options: ['Template deduction fails — int and double are different types. Use max<double>(3, 3.14) or two template parameters', 'This returns 3.14', 'This returns 3', 'Add type cast to int'],
    correctIndex: 0, explanation: 'Template argument deduction creates a conflict: T cannot be both int and double. Specify the type explicitly: max<double>(3, 3.14) or use two type parameters.' },
{ id: 'bh-cs-h2032', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'template<typename T>\nT max(T a, T b) {\n    return (a > b) ? a : b;\n}\n\ncout << max(3, 3.14);',
    options: ['Template deduction fails — int and double are different types. Use max<double>(3, 3.14) or two template parameters', 'This returns 3.14', 'This returns 3', 'Add type cast to int'],
    correctIndex: 0, explanation: 'Template argument deduction creates a conflict: T cannot be both int and double. Specify the type explicitly: max<double>(3, 3.14) or use two type parameters.' },
{ id: 'bh-cs-h2033', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'template<typename T>\nT max(T a, T b) {\n    return (a > b) ? a : b;\n}\n\ncout << max(3, 3.14);',
    options: ['Template deduction fails — int and double are different types. Use max<double>(3, 3.14) or two template parameters', 'This returns 3.14', 'This returns 3', 'Add type cast to int'],
    correctIndex: 0, explanation: 'Template argument deduction creates a conflict: T cannot be both int and double. Specify the type explicitly: max<double>(3, 3.14) or use two type parameters.' },
{ id: 'bh-cs-h2034', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'template<typename T>\nT max(T a, T b) {\n    return (a > b) ? a : b;\n}\n\ncout << max(3, 3.14);',
    options: ['Template deduction fails — int and double are different types. Use max<double>(3, 3.14) or two template parameters', 'This returns 3.14', 'This returns 3', 'Add type cast to int'],
    correctIndex: 0, explanation: 'Template argument deduction creates a conflict: T cannot be both int and double. Specify the type explicitly: max<double>(3, 3.14) or use two type parameters.' },
{ id: 'bh-cs-h2035', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'template<typename T>\nT max(T a, T b) {\n    return (a > b) ? a : b;\n}\n\ncout << max(3, 3.14);',
    options: ['Template deduction fails — int and double are different types. Use max<double>(3, 3.14) or two template parameters', 'This returns 3.14', 'This returns 3', 'Add type cast to int'],
    correctIndex: 0, explanation: 'Template argument deduction creates a conflict: T cannot be both int and double. Specify the type explicitly: max<double>(3, 3.14) or use two type parameters.' },
{ id: 'bh-cs-h2036', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'template<typename T>\nT max(T a, T b) {\n    return (a > b) ? a : b;\n}\n\ncout << max(3, 3.14);',
    options: ['Template deduction fails — int and double are different types. Use max<double>(3, 3.14) or two template parameters', 'This returns 3.14', 'This returns 3', 'Add type cast to int'],
    correctIndex: 0, explanation: 'Template argument deduction creates a conflict: T cannot be both int and double. Specify the type explicitly: max<double>(3, 3.14) or use two type parameters.' },
{ id: 'bh-cs-h2037', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'template<typename T>\nT max(T a, T b) {\n    return (a > b) ? a : b;\n}\n\ncout << max(3, 3.14);',
    options: ['Template deduction fails — int and double are different types. Use max<double>(3, 3.14) or two template parameters', 'This returns 3.14', 'This returns 3', 'Add type cast to int'],
    correctIndex: 0, explanation: 'Template argument deduction creates a conflict: T cannot be both int and double. Specify the type explicitly: max<double>(3, 3.14) or use two type parameters.' },
{ id: 'bh-cs-h2038', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'template<typename T>\nT max(T a, T b) {\n    return (a > b) ? a : b;\n}\n\ncout << max(3, 3.14);',
    options: ['Template deduction fails — int and double are different types. Use max<double>(3, 3.14) or two template parameters', 'This returns 3.14', 'This returns 3', 'Add type cast to int'],
    correctIndex: 0, explanation: 'Template argument deduction creates a conflict: T cannot be both int and double. Specify the type explicitly: max<double>(3, 3.14) or use two type parameters.' },
{ id: 'bh-cs-h2039', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'template<typename T>\nT max(T a, T b) {\n    return (a > b) ? a : b;\n}\n\ncout << max(3, 3.14);',
    options: ['Template deduction fails — int and double are different types. Use max<double>(3, 3.14) or two template parameters', 'This returns 3.14', 'This returns 3', 'Add type cast to int'],
    correctIndex: 0, explanation: 'Template argument deduction creates a conflict: T cannot be both int and double. Specify the type explicitly: max<double>(3, 3.14) or use two type parameters.' },
{ id: 'bh-cs-h2040', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'template<typename T>\nT max(T a, T b) {\n    return (a > b) ? a : b;\n}\n\ncout << max(3, 3.14);',
    options: ['Template deduction fails — int and double are different types. Use max<double>(3, 3.14) or two template parameters', 'This returns 3.14', 'This returns 3', 'Add type cast to int'],
    correctIndex: 0, explanation: 'Template argument deduction creates a conflict: T cannot be both int and double. Specify the type explicitly: max<double>(3, 3.14) or use two type parameters.' },
{ id: 'bh-cs-h2041', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'class Base {\npublic:\n    virtual void foo() { cout << "Base"; }\n};\nclass Derived : public Base {\npublic:\n    void foo() override { cout << "Derived"; }\n};\nBase obj = Derived();\nobj.foo();',
    options: ['Object slicing — obj is a Base, not a reference/pointer. Derived part is sliced off. Use Base& or Base*', 'This prints "Derived"', 'This prints "BaseDerived"', 'Add virtual keyword to foo'],
    correctIndex: 0, explanation: 'Base obj = Derived() slices the Derived object to just its Base portion. obj.foo() calls Base::foo(). Use Base& ref = Derived() or Base* ptr = new Derived() for polymorphism.' },
{ id: 'bh-cs-h2042', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'class Base {\npublic:\n    virtual void foo() { cout << "Base"; }\n};\nclass Derived : public Base {\npublic:\n    void foo() override { cout << "Derived"; }\n};\nBase obj = Derived();\nobj.foo();',
    options: ['Object slicing — obj is a Base, not a reference/pointer. Derived part is sliced off. Use Base& or Base*', 'This prints "Derived"', 'This prints "BaseDerived"', 'Add virtual keyword to foo'],
    correctIndex: 0, explanation: 'Base obj = Derived() slices the Derived object to just its Base portion. obj.foo() calls Base::foo(). Use Base& ref = Derived() or Base* ptr = new Derived() for polymorphism.' },
{ id: 'bh-cs-h2043', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'class Base {\npublic:\n    virtual void foo() { cout << "Base"; }\n};\nclass Derived : public Base {\npublic:\n    void foo() override { cout << "Derived"; }\n};\nBase obj = Derived();\nobj.foo();',
    options: ['Object slicing — obj is a Base, not a reference/pointer. Derived part is sliced off. Use Base& or Base*', 'This prints "Derived"', 'This prints "BaseDerived"', 'Add virtual keyword to foo'],
    correctIndex: 0, explanation: 'Base obj = Derived() slices the Derived object to just its Base portion. obj.foo() calls Base::foo(). Use Base& ref = Derived() or Base* ptr = new Derived() for polymorphism.' },
{ id: 'bh-cs-h2044', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'class Base {\npublic:\n    virtual void foo() { cout << "Base"; }\n};\nclass Derived : public Base {\npublic:\n    void foo() override { cout << "Derived"; }\n};\nBase obj = Derived();\nobj.foo();',
    options: ['Object slicing — obj is a Base, not a reference/pointer. Derived part is sliced off. Use Base& or Base*', 'This prints "Derived"', 'This prints "BaseDerived"', 'Add virtual keyword to foo'],
    correctIndex: 0, explanation: 'Base obj = Derived() slices the Derived object to just its Base portion. obj.foo() calls Base::foo(). Use Base& ref = Derived() or Base* ptr = new Derived() for polymorphism.' },
{ id: 'bh-cs-h2045', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'class Base {\npublic:\n    virtual void foo() { cout << "Base"; }\n};\nclass Derived : public Base {\npublic:\n    void foo() override { cout << "Derived"; }\n};\nBase obj = Derived();\nobj.foo();',
    options: ['Object slicing — obj is a Base, not a reference/pointer. Derived part is sliced off. Use Base& or Base*', 'This prints "Derived"', 'This prints "BaseDerived"', 'Add virtual keyword to foo'],
    correctIndex: 0, explanation: 'Base obj = Derived() slices the Derived object to just its Base portion. obj.foo() calls Base::foo(). Use Base& ref = Derived() or Base* ptr = new Derived() for polymorphism.' },
{ id: 'bh-cs-h2046', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'class Base {\npublic:\n    virtual void foo() { cout << "Base"; }\n};\nclass Derived : public Base {\npublic:\n    void foo() override { cout << "Derived"; }\n};\nBase obj = Derived();\nobj.foo();',
    options: ['Object slicing — obj is a Base, not a reference/pointer. Derived part is sliced off. Use Base& or Base*', 'This prints "Derived"', 'This prints "BaseDerived"', 'Add virtual keyword to foo'],
    correctIndex: 0, explanation: 'Base obj = Derived() slices the Derived object to just its Base portion. obj.foo() calls Base::foo(). Use Base& ref = Derived() or Base* ptr = new Derived() for polymorphism.' },
{ id: 'bh-cs-h2047', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'class Base {\npublic:\n    virtual void foo() { cout << "Base"; }\n};\nclass Derived : public Base {\npublic:\n    void foo() override { cout << "Derived"; }\n};\nBase obj = Derived();\nobj.foo();',
    options: ['Object slicing — obj is a Base, not a reference/pointer. Derived part is sliced off. Use Base& or Base*', 'This prints "Derived"', 'This prints "BaseDerived"', 'Add virtual keyword to foo'],
    correctIndex: 0, explanation: 'Base obj = Derived() slices the Derived object to just its Base portion. obj.foo() calls Base::foo(). Use Base& ref = Derived() or Base* ptr = new Derived() for polymorphism.' },
{ id: 'bh-cs-h2048', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'class Base {\npublic:\n    virtual void foo() { cout << "Base"; }\n};\nclass Derived : public Base {\npublic:\n    void foo() override { cout << "Derived"; }\n};\nBase obj = Derived();\nobj.foo();',
    options: ['Object slicing — obj is a Base, not a reference/pointer. Derived part is sliced off. Use Base& or Base*', 'This prints "Derived"', 'This prints "BaseDerived"', 'Add virtual keyword to foo'],
    correctIndex: 0, explanation: 'Base obj = Derived() slices the Derived object to just its Base portion. obj.foo() calls Base::foo(). Use Base& ref = Derived() or Base* ptr = new Derived() for polymorphism.' },
{ id: 'bh-cs-h2049', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'class Base {\npublic:\n    virtual void foo() { cout << "Base"; }\n};\nclass Derived : public Base {\npublic:\n    void foo() override { cout << "Derived"; }\n};\nBase obj = Derived();\nobj.foo();',
    options: ['Object slicing — obj is a Base, not a reference/pointer. Derived part is sliced off. Use Base& or Base*', 'This prints "Derived"', 'This prints "BaseDerived"', 'Add virtual keyword to foo'],
    correctIndex: 0, explanation: 'Base obj = Derived() slices the Derived object to just its Base portion. obj.foo() calls Base::foo(). Use Base& ref = Derived() or Base* ptr = new Derived() for polymorphism.' },
{ id: 'bh-cs-h2050', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'class Base {\npublic:\n    virtual void foo() { cout << "Base"; }\n};\nclass Derived : public Base {\npublic:\n    void foo() override { cout << "Derived"; }\n};\nBase obj = Derived();\nobj.foo();',
    options: ['Object slicing — obj is a Base, not a reference/pointer. Derived part is sliced off. Use Base& or Base*', 'This prints "Derived"', 'This prints "BaseDerived"', 'Add virtual keyword to foo'],
    correctIndex: 0, explanation: 'Base obj = Derived() slices the Derived object to just its Base portion. obj.foo() calls Base::foo(). Use Base& ref = Derived() or Base* ptr = new Derived() for polymorphism.' },
{ id: 'bh-cs-h2051', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'vector<bool> bits = {true, false, true};\nbool& ref = bits[0];',
    options: ['vector<bool> uses proxy references — bits[0] returns a proxy, not a real bool&. Use auto ref = bits[0] or a different container', 'This is correct', 'Use vector<int> instead', 'Add const qualifier'],
    correctIndex: 0, explanation: 'vector<bool> is a special case that packs bits. operator[] returns a proxy reference object, not a real bool&. You cannot bind a non-const reference to it. Use auto or const auto&.' },
{ id: 'bh-cs-h2052', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'vector<bool> bits = {true, false, true};\nbool& ref = bits[0];',
    options: ['vector<bool> uses proxy references — bits[0] returns a proxy, not a real bool&. Use auto ref = bits[0] or a different container', 'This is correct', 'Use vector<int> instead', 'Add const qualifier'],
    correctIndex: 0, explanation: 'vector<bool> is a special case that packs bits. operator[] returns a proxy reference object, not a real bool&. You cannot bind a non-const reference to it. Use auto or const auto&.' },
{ id: 'bh-cs-h2053', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'vector<bool> bits = {true, false, true};\nbool& ref = bits[0];',
    options: ['vector<bool> uses proxy references — bits[0] returns a proxy, not a real bool&. Use auto ref = bits[0] or a different container', 'This is correct', 'Use vector<int> instead', 'Add const qualifier'],
    correctIndex: 0, explanation: 'vector<bool> is a special case that packs bits. operator[] returns a proxy reference object, not a real bool&. You cannot bind a non-const reference to it. Use auto or const auto&.' },
{ id: 'bh-cs-h2054', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'vector<bool> bits = {true, false, true};\nbool& ref = bits[0];',
    options: ['vector<bool> uses proxy references — bits[0] returns a proxy, not a real bool&. Use auto ref = bits[0] or a different container', 'This is correct', 'Use vector<int> instead', 'Add const qualifier'],
    correctIndex: 0, explanation: 'vector<bool> is a special case that packs bits. operator[] returns a proxy reference object, not a real bool&. You cannot bind a non-const reference to it. Use auto or const auto&.' },
{ id: 'bh-cs-h2055', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'vector<bool> bits = {true, false, true};\nbool& ref = bits[0];',
    options: ['vector<bool> uses proxy references — bits[0] returns a proxy, not a real bool&. Use auto ref = bits[0] or a different container', 'This is correct', 'Use vector<int> instead', 'Add const qualifier'],
    correctIndex: 0, explanation: 'vector<bool> is a special case that packs bits. operator[] returns a proxy reference object, not a real bool&. You cannot bind a non-const reference to it. Use auto or const auto&.' },
{ id: 'bh-cs-h2056', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'vector<bool> bits = {true, false, true};\nbool& ref = bits[0];',
    options: ['vector<bool> uses proxy references — bits[0] returns a proxy, not a real bool&. Use auto ref = bits[0] or a different container', 'This is correct', 'Use vector<int> instead', 'Add const qualifier'],
    correctIndex: 0, explanation: 'vector<bool> is a special case that packs bits. operator[] returns a proxy reference object, not a real bool&. You cannot bind a non-const reference to it. Use auto or const auto&.' },
{ id: 'bh-cs-h2057', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'vector<bool> bits = {true, false, true};\nbool& ref = bits[0];',
    options: ['vector<bool> uses proxy references — bits[0] returns a proxy, not a real bool&. Use auto ref = bits[0] or a different container', 'This is correct', 'Use vector<int> instead', 'Add const qualifier'],
    correctIndex: 0, explanation: 'vector<bool> is a special case that packs bits. operator[] returns a proxy reference object, not a real bool&. You cannot bind a non-const reference to it. Use auto or const auto&.' },
{ id: 'bh-cs-h2058', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'vector<bool> bits = {true, false, true};\nbool& ref = bits[0];',
    options: ['vector<bool> uses proxy references — bits[0] returns a proxy, not a real bool&. Use auto ref = bits[0] or a different container', 'This is correct', 'Use vector<int> instead', 'Add const qualifier'],
    correctIndex: 0, explanation: 'vector<bool> is a special case that packs bits. operator[] returns a proxy reference object, not a real bool&. You cannot bind a non-const reference to it. Use auto or const auto&.' },
{ id: 'bh-cs-h2059', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'vector<bool> bits = {true, false, true};\nbool& ref = bits[0];',
    options: ['vector<bool> uses proxy references — bits[0] returns a proxy, not a real bool&. Use auto ref = bits[0] or a different container', 'This is correct', 'Use vector<int> instead', 'Add const qualifier'],
    correctIndex: 0, explanation: 'vector<bool> is a special case that packs bits. operator[] returns a proxy reference object, not a real bool&. You cannot bind a non-const reference to it. Use auto or const auto&.' },
{ id: 'bh-cs-h2060', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'vector<bool> bits = {true, false, true};\nbool& ref = bits[0];',
    options: ['vector<bool> uses proxy references — bits[0] returns a proxy, not a real bool&. Use auto ref = bits[0] or a different container', 'This is correct', 'Use vector<int> instead', 'Add const qualifier'],
    correctIndex: 0, explanation: 'vector<bool> is a special case that packs bits. operator[] returns a proxy reference object, not a real bool&. You cannot bind a non-const reference to it. Use auto or const auto&.' },
{ id: 'bh-cs-h2061', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'class A {\n    int x;\npublic:\n    A(int v) : x(v) {}\n};\nclass B : public A {\n    int y;\npublic:\n    B(int a, int b) : y(b), A(a) {}\n};',
    options: ['Base class A is initialized after member y — but A(a) comes after y(b) in initializer list, which is fine since base is always initialized first regardless of order', 'Initialization order is wrong — A should come before y', 'This causes undefined behavior', 'Use this->x = a instead'],
    correctIndex: 0, explanation: 'Members are initialized in declaration order, not initializer list order. Base class A is always initialized before member y, regardless of the order in the initializer list. However, the misleading order should be fixed for readability.' },
{ id: 'bh-cs-h2062', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'class A {\n    int x;\npublic:\n    A(int v) : x(v) {}\n};\nclass B : public A {\n    int y;\npublic:\n    B(int a, int b) : y(b), A(a) {}\n};',
    options: ['Base class A is initialized after member y — but A(a) comes after y(b) in initializer list, which is fine since base is always initialized first regardless of order', 'Initialization order is wrong — A should come before y', 'This causes undefined behavior', 'Use this->x = a instead'],
    correctIndex: 0, explanation: 'Members are initialized in declaration order, not initializer list order. Base class A is always initialized before member y, regardless of the order in the initializer list. However, the misleading order should be fixed for readability.' },
{ id: 'bh-cs-h2063', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'class A {\n    int x;\npublic:\n    A(int v) : x(v) {}\n};\nclass B : public A {\n    int y;\npublic:\n    B(int a, int b) : y(b), A(a) {}\n};',
    options: ['Base class A is initialized after member y — but A(a) comes after y(b) in initializer list, which is fine since base is always initialized first regardless of order', 'Initialization order is wrong — A should come before y', 'This causes undefined behavior', 'Use this->x = a instead'],
    correctIndex: 0, explanation: 'Members are initialized in declaration order, not initializer list order. Base class A is always initialized before member y, regardless of the order in the initializer list. However, the misleading order should be fixed for readability.' },
{ id: 'bh-cs-h2064', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'class A {\n    int x;\npublic:\n    A(int v) : x(v) {}\n};\nclass B : public A {\n    int y;\npublic:\n    B(int a, int b) : y(b), A(a) {}\n};',
    options: ['Base class A is initialized after member y — but A(a) comes after y(b) in initializer list, which is fine since base is always initialized first regardless of order', 'Initialization order is wrong — A should come before y', 'This causes undefined behavior', 'Use this->x = a instead'],
    correctIndex: 0, explanation: 'Members are initialized in declaration order, not initializer list order. Base class A is always initialized before member y, regardless of the order in the initializer list. However, the misleading order should be fixed for readability.' },
{ id: 'bh-cs-h2065', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'class A {\n    int x;\npublic:\n    A(int v) : x(v) {}\n};\nclass B : public A {\n    int y;\npublic:\n    B(int a, int b) : y(b), A(a) {}\n};',
    options: ['Base class A is initialized after member y — but A(a) comes after y(b) in initializer list, which is fine since base is always initialized first regardless of order', 'Initialization order is wrong — A should come before y', 'This causes undefined behavior', 'Use this->x = a instead'],
    correctIndex: 0, explanation: 'Members are initialized in declaration order, not initializer list order. Base class A is always initialized before member y, regardless of the order in the initializer list. However, the misleading order should be fixed for readability.' },
{ id: 'bh-cs-h2066', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'class A {\n    int x;\npublic:\n    A(int v) : x(v) {}\n};\nclass B : public A {\n    int y;\npublic:\n    B(int a, int b) : y(b), A(a) {}\n};',
    options: ['Base class A is initialized after member y — but A(a) comes after y(b) in initializer list, which is fine since base is always initialized first regardless of order', 'Initialization order is wrong — A should come before y', 'This causes undefined behavior', 'Use this->x = a instead'],
    correctIndex: 0, explanation: 'Members are initialized in declaration order, not initializer list order. Base class A is always initialized before member y, regardless of the order in the initializer list. However, the misleading order should be fixed for readability.' },
{ id: 'bh-cs-h2067', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'class A {\n    int x;\npublic:\n    A(int v) : x(v) {}\n};\nclass B : public A {\n    int y;\npublic:\n    B(int a, int b) : y(b), A(a) {}\n};',
    options: ['Base class A is initialized after member y — but A(a) comes after y(b) in initializer list, which is fine since base is always initialized first regardless of order', 'Initialization order is wrong — A should come before y', 'This causes undefined behavior', 'Use this->x = a instead'],
    correctIndex: 0, explanation: 'Members are initialized in declaration order, not initializer list order. Base class A is always initialized before member y, regardless of the order in the initializer list. However, the misleading order should be fixed for readability.' },
{ id: 'bh-cs-h2068', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'class A {\n    int x;\npublic:\n    A(int v) : x(v) {}\n};\nclass B : public A {\n    int y;\npublic:\n    B(int a, int b) : y(b), A(a) {}\n};',
    options: ['Base class A is initialized after member y — but A(a) comes after y(b) in initializer list, which is fine since base is always initialized first regardless of order', 'Initialization order is wrong — A should come before y', 'This causes undefined behavior', 'Use this->x = a instead'],
    correctIndex: 0, explanation: 'Members are initialized in declaration order, not initializer list order. Base class A is always initialized before member y, regardless of the order in the initializer list. However, the misleading order should be fixed for readability.' },
{ id: 'bh-cs-h2069', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'class A {\n    int x;\npublic:\n    A(int v) : x(v) {}\n};\nclass B : public A {\n    int y;\npublic:\n    B(int a, int b) : y(b), A(a) {}\n};',
    options: ['Base class A is initialized after member y — but A(a) comes after y(b) in initializer list, which is fine since base is always initialized first regardless of order', 'Initialization order is wrong — A should come before y', 'This causes undefined behavior', 'Use this->x = a instead'],
    correctIndex: 0, explanation: 'Members are initialized in declaration order, not initializer list order. Base class A is always initialized before member y, regardless of the order in the initializer list. However, the misleading order should be fixed for readability.' },
{ id: 'bh-cs-h2070', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'class A {\n    int x;\npublic:\n    A(int v) : x(v) {}\n};\nclass B : public A {\n    int y;\npublic:\n    B(int a, int b) : y(b), A(a) {}\n};',
    options: ['Base class A is initialized after member y — but A(a) comes after y(b) in initializer list, which is fine since base is always initialized first regardless of order', 'Initialization order is wrong — A should come before y', 'This causes undefined behavior', 'Use this->x = a instead'],
    correctIndex: 0, explanation: 'Members are initialized in declaration order, not initializer list order. Base class A is always initialized before member y, regardless of the order in the initializer list. However, the misleading order should be fixed for readability.' },
{ id: 'bh-cs-h2071', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'void processData(const vector<int>& data) {\n    auto it = find_if(data.begin(), data.end(), [](int x) { return x > 100; });\n    data.erase(it);\n}',
    options: ['Cannot erase from const reference — remove const or use mutable copy', 'This is correct', 'Use remove_if instead', 'Change const to constexpr'],
    correctIndex: 0, explanation: 'The function takes data by const reference, but erase() modifies the vector. Remove const if modification is intended, or return a new vector without the element.' },
{ id: 'bh-cs-h2072', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'void processData(const vector<int>& data) {\n    auto it = find_if(data.begin(), data.end(), [](int x) { return x > 100; });\n    data.erase(it);\n}',
    options: ['Cannot erase from const reference — remove const or use mutable copy', 'This is correct', 'Use remove_if instead', 'Change const to constexpr'],
    correctIndex: 0, explanation: 'The function takes data by const reference, but erase() modifies the vector. Remove const if modification is intended, or return a new vector without the element.' },
{ id: 'bh-cs-h2073', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'void processData(const vector<int>& data) {\n    auto it = find_if(data.begin(), data.end(), [](int x) { return x > 100; });\n    data.erase(it);\n}',
    options: ['Cannot erase from const reference — remove const or use mutable copy', 'This is correct', 'Use remove_if instead', 'Change const to constexpr'],
    correctIndex: 0, explanation: 'The function takes data by const reference, but erase() modifies the vector. Remove const if modification is intended, or return a new vector without the element.' },
{ id: 'bh-cs-h2074', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'void processData(const vector<int>& data) {\n    auto it = find_if(data.begin(), data.end(), [](int x) { return x > 100; });\n    data.erase(it);\n}',
    options: ['Cannot erase from const reference — remove const or use mutable copy', 'This is correct', 'Use remove_if instead', 'Change const to constexpr'],
    correctIndex: 0, explanation: 'The function takes data by const reference, but erase() modifies the vector. Remove const if modification is intended, or return a new vector without the element.' },
{ id: 'bh-cs-h2075', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'void processData(const vector<int>& data) {\n    auto it = find_if(data.begin(), data.end(), [](int x) { return x > 100; });\n    data.erase(it);\n}',
    options: ['Cannot erase from const reference — remove const or use mutable copy', 'This is correct', 'Use remove_if instead', 'Change const to constexpr'],
    correctIndex: 0, explanation: 'The function takes data by const reference, but erase() modifies the vector. Remove const if modification is intended, or return a new vector without the element.' },
{ id: 'bh-cs-h2076', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'void processData(const vector<int>& data) {\n    auto it = find_if(data.begin(), data.end(), [](int x) { return x > 100; });\n    data.erase(it);\n}',
    options: ['Cannot erase from const reference — remove const or use mutable copy', 'This is correct', 'Use remove_if instead', 'Change const to constexpr'],
    correctIndex: 0, explanation: 'The function takes data by const reference, but erase() modifies the vector. Remove const if modification is intended, or return a new vector without the element.' },
{ id: 'bh-cs-h2077', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'void processData(const vector<int>& data) {\n    auto it = find_if(data.begin(), data.end(), [](int x) { return x > 100; });\n    data.erase(it);\n}',
    options: ['Cannot erase from const reference — remove const or use mutable copy', 'This is correct', 'Use remove_if instead', 'Change const to constexpr'],
    correctIndex: 0, explanation: 'The function takes data by const reference, but erase() modifies the vector. Remove const if modification is intended, or return a new vector without the element.' },
{ id: 'bh-cs-h2078', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'void processData(const vector<int>& data) {\n    auto it = find_if(data.begin(), data.end(), [](int x) { return x > 100; });\n    data.erase(it);\n}',
    options: ['Cannot erase from const reference — remove const or use mutable copy', 'This is correct', 'Use remove_if instead', 'Change const to constexpr'],
    correctIndex: 0, explanation: 'The function takes data by const reference, but erase() modifies the vector. Remove const if modification is intended, or return a new vector without the element.' },
{ id: 'bh-cs-h2079', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'void processData(const vector<int>& data) {\n    auto it = find_if(data.begin(), data.end(), [](int x) { return x > 100; });\n    data.erase(it);\n}',
    options: ['Cannot erase from const reference — remove const or use mutable copy', 'This is correct', 'Use remove_if instead', 'Change const to constexpr'],
    correctIndex: 0, explanation: 'The function takes data by const reference, but erase() modifies the vector. Remove const if modification is intended, or return a new vector without the element.' },
{ id: 'bh-cs-h2080', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'void processData(const vector<int>& data) {\n    auto it = find_if(data.begin(), data.end(), [](int x) { return x > 100; });\n    data.erase(it);\n}',
    options: ['Cannot erase from const reference — remove const or use mutable copy', 'This is correct', 'Use remove_if instead', 'Change const to constexpr'],
    correctIndex: 0, explanation: 'The function takes data by const reference, but erase() modifies the vector. Remove const if modification is intended, or return a new vector without the element.' },
{ id: 'bh-cs-h2081', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'class Node {\npublic:\n    shared_ptr<Node> next;\n    ~Node() { cout << "destroyed"; }\n};\nauto a = make_shared<Node>();\nauto b = make_shared<Node>();\na->next = b;\nb->next = a;',
    options: ['Circular reference with shared_ptr causes memory leak — use weak_ptr for one direction', 'This is correct linked list', 'Use unique_ptr instead', 'Add reset() call'],
    correctIndex: 0, explanation: 'Two shared_ptrs referencing each other create a reference cycle. Neither reference count drops to zero, so neither is destroyed. Use weak_ptr for the back-reference: weak_ptr<Node> next.' },
{ id: 'bh-cs-h2082', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'class Node {\npublic:\n    shared_ptr<Node> next;\n    ~Node() { cout << "destroyed"; }\n};\nauto a = make_shared<Node>();\nauto b = make_shared<Node>();\na->next = b;\nb->next = a;',
    options: ['Circular reference with shared_ptr causes memory leak — use weak_ptr for one direction', 'This is correct linked list', 'Use unique_ptr instead', 'Add reset() call'],
    correctIndex: 0, explanation: 'Two shared_ptrs referencing each other create a reference cycle. Neither reference count drops to zero, so neither is destroyed. Use weak_ptr for the back-reference: weak_ptr<Node> next.' },
{ id: 'bh-cs-h2083', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'class Node {\npublic:\n    shared_ptr<Node> next;\n    ~Node() { cout << "destroyed"; }\n};\nauto a = make_shared<Node>();\nauto b = make_shared<Node>();\na->next = b;\nb->next = a;',
    options: ['Circular reference with shared_ptr causes memory leak — use weak_ptr for one direction', 'This is correct linked list', 'Use unique_ptr instead', 'Add reset() call'],
    correctIndex: 0, explanation: 'Two shared_ptrs referencing each other create a reference cycle. Neither reference count drops to zero, so neither is destroyed. Use weak_ptr for the back-reference: weak_ptr<Node> next.' },
{ id: 'bh-cs-h2084', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'class Node {\npublic:\n    shared_ptr<Node> next;\n    ~Node() { cout << "destroyed"; }\n};\nauto a = make_shared<Node>();\nauto b = make_shared<Node>();\na->next = b;\nb->next = a;',
    options: ['Circular reference with shared_ptr causes memory leak — use weak_ptr for one direction', 'This is correct linked list', 'Use unique_ptr instead', 'Add reset() call'],
    correctIndex: 0, explanation: 'Two shared_ptrs referencing each other create a reference cycle. Neither reference count drops to zero, so neither is destroyed. Use weak_ptr for the back-reference: weak_ptr<Node> next.' },
{ id: 'bh-cs-h2085', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'class Node {\npublic:\n    shared_ptr<Node> next;\n    ~Node() { cout << "destroyed"; }\n};\nauto a = make_shared<Node>();\nauto b = make_shared<Node>();\na->next = b;\nb->next = a;',
    options: ['Circular reference with shared_ptr causes memory leak — use weak_ptr for one direction', 'This is correct linked list', 'Use unique_ptr instead', 'Add reset() call'],
    correctIndex: 0, explanation: 'Two shared_ptrs referencing each other create a reference cycle. Neither reference count drops to zero, so neither is destroyed. Use weak_ptr for the back-reference: weak_ptr<Node> next.' },
{ id: 'bh-cs-h2086', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'class Node {\npublic:\n    shared_ptr<Node> next;\n    ~Node() { cout << "destroyed"; }\n};\nauto a = make_shared<Node>();\nauto b = make_shared<Node>();\na->next = b;\nb->next = a;',
    options: ['Circular reference with shared_ptr causes memory leak — use weak_ptr for one direction', 'This is correct linked list', 'Use unique_ptr instead', 'Add reset() call'],
    correctIndex: 0, explanation: 'Two shared_ptrs referencing each other create a reference cycle. Neither reference count drops to zero, so neither is destroyed. Use weak_ptr for the back-reference: weak_ptr<Node> next.' },
{ id: 'bh-cs-h2087', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'class Node {\npublic:\n    shared_ptr<Node> next;\n    ~Node() { cout << "destroyed"; }\n};\nauto a = make_shared<Node>();\nauto b = make_shared<Node>();\na->next = b;\nb->next = a;',
    options: ['Circular reference with shared_ptr causes memory leak — use weak_ptr for one direction', 'This is correct linked list', 'Use unique_ptr instead', 'Add reset() call'],
    correctIndex: 0, explanation: 'Two shared_ptrs referencing each other create a reference cycle. Neither reference count drops to zero, so neither is destroyed. Use weak_ptr for the back-reference: weak_ptr<Node> next.' },
{ id: 'bh-cs-h2088', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'class Node {\npublic:\n    shared_ptr<Node> next;\n    ~Node() { cout << "destroyed"; }\n};\nauto a = make_shared<Node>();\nauto b = make_shared<Node>();\na->next = b;\nb->next = a;',
    options: ['Circular reference with shared_ptr causes memory leak — use weak_ptr for one direction', 'This is correct linked list', 'Use unique_ptr instead', 'Add reset() call'],
    correctIndex: 0, explanation: 'Two shared_ptrs referencing each other create a reference cycle. Neither reference count drops to zero, so neither is destroyed. Use weak_ptr for the back-reference: weak_ptr<Node> next.' },
{ id: 'bh-cs-h2089', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'class Node {\npublic:\n    shared_ptr<Node> next;\n    ~Node() { cout << "destroyed"; }\n};\nauto a = make_shared<Node>();\nauto b = make_shared<Node>();\na->next = b;\nb->next = a;',
    options: ['Circular reference with shared_ptr causes memory leak — use weak_ptr for one direction', 'This is correct linked list', 'Use unique_ptr instead', 'Add reset() call'],
    correctIndex: 0, explanation: 'Two shared_ptrs referencing each other create a reference cycle. Neither reference count drops to zero, so neither is destroyed. Use weak_ptr for the back-reference: weak_ptr<Node> next.' },
{ id: 'bh-cs-h2090', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'class Node {\npublic:\n    shared_ptr<Node> next;\n    ~Node() { cout << "destroyed"; }\n};\nauto a = make_shared<Node>();\nauto b = make_shared<Node>();\na->next = b;\nb->next = a;',
    options: ['Circular reference with shared_ptr causes memory leak — use weak_ptr for one direction', 'This is correct linked list', 'Use unique_ptr instead', 'Add reset() call'],
    correctIndex: 0, explanation: 'Two shared_ptrs referencing each other create a reference cycle. Neither reference count drops to zero, so neither is destroyed. Use weak_ptr for the back-reference: weak_ptr<Node> next.' },
{ id: 'bh-cs-h2091', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'class A {\npublic:\n    A() { cout << "A"; }\n    virtual ~A() { cout << "a"; }\n};\nclass B : public A {\n    A a;\npublic:\n    B() { cout << "B"; }\n    ~B() { cout << "b"; }\n};\nint main() { B b; }',
    options: ['Construction: A, A, B. Destruction: b, a, A, a. But the actual output depends on correct understanding of construction/destruction order', 'Construction: B, A, A', 'Construction: A, B, A', 'Destruction: A, b, a, a'],
    correctIndex: 0, explanation: 'Construction order: Base A first, then member a (another A), then B body. Destruction is reverse: B body, member a (~A), Base A (~A). Output: AABbaa. Understanding this order is crucial for C++.' },
{ id: 'bh-cs-h2092', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'class A {\npublic:\n    A() { cout << "A"; }\n    virtual ~A() { cout << "a"; }\n};\nclass B : public A {\n    A a;\npublic:\n    B() { cout << "B"; }\n    ~B() { cout << "b"; }\n};\nint main() { B b; }',
    options: ['Construction: A, A, B. Destruction: b, a, A, a. But the actual output depends on correct understanding of construction/destruction order', 'Construction: B, A, A', 'Construction: A, B, A', 'Destruction: A, b, a, a'],
    correctIndex: 0, explanation: 'Construction order: Base A first, then member a (another A), then B body. Destruction is reverse: B body, member a (~A), Base A (~A). Output: AABbaa. Understanding this order is crucial for C++.' },
{ id: 'bh-cs-h2093', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'class A {\npublic:\n    A() { cout << "A"; }\n    virtual ~A() { cout << "a"; }\n};\nclass B : public A {\n    A a;\npublic:\n    B() { cout << "B"; }\n    ~B() { cout << "b"; }\n};\nint main() { B b; }',
    options: ['Construction: A, A, B. Destruction: b, a, A, a. But the actual output depends on correct understanding of construction/destruction order', 'Construction: B, A, A', 'Construction: A, B, A', 'Destruction: A, b, a, a'],
    correctIndex: 0, explanation: 'Construction order: Base A first, then member a (another A), then B body. Destruction is reverse: B body, member a (~A), Base A (~A). Output: AABbaa. Understanding this order is crucial for C++.' },
{ id: 'bh-cs-h2094', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'class A {\npublic:\n    A() { cout << "A"; }\n    virtual ~A() { cout << "a"; }\n};\nclass B : public A {\n    A a;\npublic:\n    B() { cout << "B"; }\n    ~B() { cout << "b"; }\n};\nint main() { B b; }',
    options: ['Construction: A, A, B. Destruction: b, a, A, a. But the actual output depends on correct understanding of construction/destruction order', 'Construction: B, A, A', 'Construction: A, B, A', 'Destruction: A, b, a, a'],
    correctIndex: 0, explanation: 'Construction order: Base A first, then member a (another A), then B body. Destruction is reverse: B body, member a (~A), Base A (~A). Output: AABbaa. Understanding this order is crucial for C++.' },
{ id: 'bh-cs-h2095', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'class A {\npublic:\n    A() { cout << "A"; }\n    virtual ~A() { cout << "a"; }\n};\nclass B : public A {\n    A a;\npublic:\n    B() { cout << "B"; }\n    ~B() { cout << "b"; }\n};\nint main() { B b; }',
    options: ['Construction: A, A, B. Destruction: b, a, A, a. But the actual output depends on correct understanding of construction/destruction order', 'Construction: B, A, A', 'Construction: A, B, A', 'Destruction: A, b, a, a'],
    correctIndex: 0, explanation: 'Construction order: Base A first, then member a (another A), then B body. Destruction is reverse: B body, member a (~A), Base A (~A). Output: AABbaa. Understanding this order is crucial for C++.' },
{ id: 'bh-cs-h2096', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'class A {\npublic:\n    A() { cout << "A"; }\n    virtual ~A() { cout << "a"; }\n};\nclass B : public A {\n    A a;\npublic:\n    B() { cout << "B"; }\n    ~B() { cout << "b"; }\n};\nint main() { B b; }',
    options: ['Construction: A, A, B. Destruction: b, a, A, a. But the actual output depends on correct understanding of construction/destruction order', 'Construction: B, A, A', 'Construction: A, B, A', 'Destruction: A, b, a, a'],
    correctIndex: 0, explanation: 'Construction order: Base A first, then member a (another A), then B body. Destruction is reverse: B body, member a (~A), Base A (~A). Output: AABbaa. Understanding this order is crucial for C++.' },
{ id: 'bh-cs-h2097', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'class A {\npublic:\n    A() { cout << "A"; }\n    virtual ~A() { cout << "a"; }\n};\nclass B : public A {\n    A a;\npublic:\n    B() { cout << "B"; }\n    ~B() { cout << "b"; }\n};\nint main() { B b; }',
    options: ['Construction: A, A, B. Destruction: b, a, A, a. But the actual output depends on correct understanding of construction/destruction order', 'Construction: B, A, A', 'Construction: A, B, A', 'Destruction: A, b, a, a'],
    correctIndex: 0, explanation: 'Construction order: Base A first, then member a (another A), then B body. Destruction is reverse: B body, member a (~A), Base A (~A). Output: AABbaa. Understanding this order is crucial for C++.' },
{ id: 'bh-cs-h2098', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'class A {\npublic:\n    A() { cout << "A"; }\n    virtual ~A() { cout << "a"; }\n};\nclass B : public A {\n    A a;\npublic:\n    B() { cout << "B"; }\n    ~B() { cout << "b"; }\n};\nint main() { B b; }',
    options: ['Construction: A, A, B. Destruction: b, a, A, a. But the actual output depends on correct understanding of construction/destruction order', 'Construction: B, A, A', 'Construction: A, B, A', 'Destruction: A, b, a, a'],
    correctIndex: 0, explanation: 'Construction order: Base A first, then member a (another A), then B body. Destruction is reverse: B body, member a (~A), Base A (~A). Output: AABbaa. Understanding this order is crucial for C++.' },
{ id: 'bh-cs-h2099', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'class A {\npublic:\n    A() { cout << "A"; }\n    virtual ~A() { cout << "a"; }\n};\nclass B : public A {\n    A a;\npublic:\n    B() { cout << "B"; }\n    ~B() { cout << "b"; }\n};\nint main() { B b; }',
    options: ['Construction: A, A, B. Destruction: b, a, A, a. But the actual output depends on correct understanding of construction/destruction order', 'Construction: B, A, A', 'Construction: A, B, A', 'Destruction: A, b, a, a'],
    correctIndex: 0, explanation: 'Construction order: Base A first, then member a (another A), then B body. Destruction is reverse: B body, member a (~A), Base A (~A). Output: AABbaa. Understanding this order is crucial for C++.' },
{ id: 'bh-cs-h2100', game: 'bug-hunter', language: 'cpp', difficulty: 'hard',
    code: 'class A {\npublic:\n    A() { cout << "A"; }\n    virtual ~A() { cout << "a"; }\n};\nclass B : public A {\n    A a;\npublic:\n    B() { cout << "B"; }\n    ~B() { cout << "b"; }\n};\nint main() { B b; }',
    options: ['Construction: A, A, B. Destruction: b, a, A, a. But the actual output depends on correct understanding of construction/destruction order', 'Construction: B, A, A', 'Construction: A, B, A', 'Destruction: A, b, a, a'],
    correctIndex: 0, explanation: 'Construction order: Base A first, then member a (another A), then B body. Destruction is reverse: B body, member a (~A), Base A (~A). Output: AABbaa. Understanding this order is crucial for C++.' },
