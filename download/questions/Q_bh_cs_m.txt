{ id: 'bh-cs-m2001', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'class StringList {\n    vector<string*> items;\npublic:\n    void add(const string& s) {\n        items.push_back(new string(s));\n    }\n    ~StringList() {}\n};',
    options: ['Memory leak — destructor must delete each pointer: for (auto p : items) delete p;', 'Use unique_ptr<string> instead', 'Use vector<string> instead of vector<string*>', 'All of the above are valid solutions'],
    correctIndex: 3, explanation: 'Raw pointers in the vector are never deleted. Add proper cleanup in the destructor, or better yet, use smart pointers or store strings by value.' },
{ id: 'bh-cs-m2002', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'class StringList {\n    vector<string*> items;\npublic:\n    void add(const string& s) {\n        items.push_back(new string(s));\n    }\n    ~StringList() {}\n};',
    options: ['Memory leak — destructor must delete each pointer: for (auto p : items) delete p;', 'Use unique_ptr<string> instead', 'Use vector<string> instead of vector<string*>', 'All of the above are valid solutions'],
    correctIndex: 3, explanation: 'Raw pointers in the vector are never deleted. Add proper cleanup in the destructor, or better yet, use smart pointers or store strings by value.' },
{ id: 'bh-cs-m2003', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'class StringList {\n    vector<string*> items;\npublic:\n    void add(const string& s) {\n        items.push_back(new string(s));\n    }\n    ~StringList() {}\n};',
    options: ['Memory leak — destructor must delete each pointer: for (auto p : items) delete p;', 'Use unique_ptr<string> instead', 'Use vector<string> instead of vector<string*>', 'All of the above are valid solutions'],
    correctIndex: 3, explanation: 'Raw pointers in the vector are never deleted. Add proper cleanup in the destructor, or better yet, use smart pointers or store strings by value.' },
{ id: 'bh-cs-m2004', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'class StringList {\n    vector<string*> items;\npublic:\n    void add(const string& s) {\n        items.push_back(new string(s));\n    }\n    ~StringList() {}\n};',
    options: ['Memory leak — destructor must delete each pointer: for (auto p : items) delete p;', 'Use unique_ptr<string> instead', 'Use vector<string> instead of vector<string*>', 'All of the above are valid solutions'],
    correctIndex: 3, explanation: 'Raw pointers in the vector are never deleted. Add proper cleanup in the destructor, or better yet, use smart pointers or store strings by value.' },
{ id: 'bh-cs-m2005', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'class StringList {\n    vector<string*> items;\npublic:\n    void add(const string& s) {\n        items.push_back(new string(s));\n    }\n    ~StringList() {}\n};',
    options: ['Memory leak — destructor must delete each pointer: for (auto p : items) delete p;', 'Use unique_ptr<string> instead', 'Use vector<string> instead of vector<string*>', 'All of the above are valid solutions'],
    correctIndex: 3, explanation: 'Raw pointers in the vector are never deleted. Add proper cleanup in the destructor, or better yet, use smart pointers or store strings by value.' },
{ id: 'bh-cs-m2006', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'class StringList {\n    vector<string*> items;\npublic:\n    void add(const string& s) {\n        items.push_back(new string(s));\n    }\n    ~StringList() {}\n};',
    options: ['Memory leak — destructor must delete each pointer: for (auto p : items) delete p;', 'Use unique_ptr<string> instead', 'Use vector<string> instead of vector<string*>', 'All of the above are valid solutions'],
    correctIndex: 3, explanation: 'Raw pointers in the vector are never deleted. Add proper cleanup in the destructor, or better yet, use smart pointers or store strings by value.' },
{ id: 'bh-cs-m2007', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'class StringList {\n    vector<string*> items;\npublic:\n    void add(const string& s) {\n        items.push_back(new string(s));\n    }\n    ~StringList() {}\n};',
    options: ['Memory leak — destructor must delete each pointer: for (auto p : items) delete p;', 'Use unique_ptr<string> instead', 'Use vector<string> instead of vector<string*>', 'All of the above are valid solutions'],
    correctIndex: 3, explanation: 'Raw pointers in the vector are never deleted. Add proper cleanup in the destructor, or better yet, use smart pointers or store strings by value.' },
{ id: 'bh-cs-m2008', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'class StringList {\n    vector<string*> items;\npublic:\n    void add(const string& s) {\n        items.push_back(new string(s));\n    }\n    ~StringList() {}\n};',
    options: ['Memory leak — destructor must delete each pointer: for (auto p : items) delete p;', 'Use unique_ptr<string> instead', 'Use vector<string> instead of vector<string*>', 'All of the above are valid solutions'],
    correctIndex: 3, explanation: 'Raw pointers in the vector are never deleted. Add proper cleanup in the destructor, or better yet, use smart pointers or store strings by value.' },
{ id: 'bh-cs-m2009', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'void process(vector<int>& v) {\n    for (auto it = v.begin(); it != v.end(); it++) {\n        if (*it < 0) v.erase(it);\n    }\n}',
    options: ['erase() invalidates the iterator — use it = v.erase(it) and only increment otherwise', 'Use remove_if and erase idiom', 'Copy the vector first', 'Both A and B are valid fixes'],
    correctIndex: 3, explanation: 'erase() invalidates the current iterator. Use it = v.erase(it) which returns the next valid iterator, or use the erase-remove idiom: v.erase(remove_if(v.begin(), v.end(), [](int x){return x<0;}), v.end()).' },
{ id: 'bh-cs-m2010', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'void process(vector<int>& v) {\n    for (auto it = v.begin(); it != v.end(); it++) {\n        if (*it < 0) v.erase(it);\n    }\n}',
    options: ['erase() invalidates the iterator — use it = v.erase(it) and only increment otherwise', 'Use remove_if and erase idiom', 'Copy the vector first', 'Both A and B are valid fixes'],
    correctIndex: 3, explanation: 'erase() invalidates the current iterator. Use it = v.erase(it) which returns the next valid iterator, or use the erase-remove idiom: v.erase(remove_if(v.begin(), v.end(), [](int x){return x<0;}), v.end()).' },
{ id: 'bh-cs-m2011', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'void process(vector<int>& v) {\n    for (auto it = v.begin(); it != v.end(); it++) {\n        if (*it < 0) v.erase(it);\n    }\n}',
    options: ['erase() invalidates the iterator — use it = v.erase(it) and only increment otherwise', 'Use remove_if and erase idiom', 'Copy the vector first', 'Both A and B are valid fixes'],
    correctIndex: 3, explanation: 'erase() invalidates the current iterator. Use it = v.erase(it) which returns the next valid iterator, or use the erase-remove idiom: v.erase(remove_if(v.begin(), v.end(), [](int x){return x<0;}), v.end()).' },
{ id: 'bh-cs-m2012', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'void process(vector<int>& v) {\n    for (auto it = v.begin(); it != v.end(); it++) {\n        if (*it < 0) v.erase(it);\n    }\n}',
    options: ['erase() invalidates the iterator — use it = v.erase(it) and only increment otherwise', 'Use remove_if and erase idiom', 'Copy the vector first', 'Both A and B are valid fixes'],
    correctIndex: 3, explanation: 'erase() invalidates the current iterator. Use it = v.erase(it) which returns the next valid iterator, or use the erase-remove idiom: v.erase(remove_if(v.begin(), v.end(), [](int x){return x<0;}), v.end()).' },
{ id: 'bh-cs-m2013', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'void process(vector<int>& v) {\n    for (auto it = v.begin(); it != v.end(); it++) {\n        if (*it < 0) v.erase(it);\n    }\n}',
    options: ['erase() invalidates the iterator — use it = v.erase(it) and only increment otherwise', 'Use remove_if and erase idiom', 'Copy the vector first', 'Both A and B are valid fixes'],
    correctIndex: 3, explanation: 'erase() invalidates the current iterator. Use it = v.erase(it) which returns the next valid iterator, or use the erase-remove idiom: v.erase(remove_if(v.begin(), v.end(), [](int x){return x<0;}), v.end()).' },
{ id: 'bh-cs-m2014', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'void process(vector<int>& v) {\n    for (auto it = v.begin(); it != v.end(); it++) {\n        if (*it < 0) v.erase(it);\n    }\n}',
    options: ['erase() invalidates the iterator — use it = v.erase(it) and only increment otherwise', 'Use remove_if and erase idiom', 'Copy the vector first', 'Both A and B are valid fixes'],
    correctIndex: 3, explanation: 'erase() invalidates the current iterator. Use it = v.erase(it) which returns the next valid iterator, or use the erase-remove idiom: v.erase(remove_if(v.begin(), v.end(), [](int x){return x<0;}), v.end()).' },
{ id: 'bh-cs-m2015', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'void process(vector<int>& v) {\n    for (auto it = v.begin(); it != v.end(); it++) {\n        if (*it < 0) v.erase(it);\n    }\n}',
    options: ['erase() invalidates the iterator — use it = v.erase(it) and only increment otherwise', 'Use remove_if and erase idiom', 'Copy the vector first', 'Both A and B are valid fixes'],
    correctIndex: 3, explanation: 'erase() invalidates the current iterator. Use it = v.erase(it) which returns the next valid iterator, or use the erase-remove idiom: v.erase(remove_if(v.begin(), v.end(), [](int x){return x<0;}), v.end()).' },
{ id: 'bh-cs-m2016', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'void process(vector<int>& v) {\n    for (auto it = v.begin(); it != v.end(); it++) {\n        if (*it < 0) v.erase(it);\n    }\n}',
    options: ['erase() invalidates the iterator — use it = v.erase(it) and only increment otherwise', 'Use remove_if and erase idiom', 'Copy the vector first', 'Both A and B are valid fixes'],
    correctIndex: 3, explanation: 'erase() invalidates the current iterator. Use it = v.erase(it) which returns the next valid iterator, or use the erase-remove idiom: v.erase(remove_if(v.begin(), v.end(), [](int x){return x<0;}), v.end()).' },
{ id: 'bh-cs-m2017', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'shared_ptr<int> p1 = make_shared<int>(42);\nshared_ptr<int> p2(p1.get());',
    options: ['p2 created from raw pointer — when both shared_ptrs are destroyed, double-free occurs. Use shared_ptr<int> p2(p1) instead', 'This is correct shared_ptr usage', 'Use weak_ptr instead', 'Use unique_ptr instead'],
    correctIndex: 0, explanation: 'Constructing shared_ptr from get() creates a second control block. Both shared_ptrs think they own the memory exclusively, causing double-free. Copy the shared_ptr directly: shared_ptr<int> p2(p1).' },
{ id: 'bh-cs-m2018', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'shared_ptr<int> p1 = make_shared<int>(42);\nshared_ptr<int> p2(p1.get());',
    options: ['p2 created from raw pointer — when both shared_ptrs are destroyed, double-free occurs. Use shared_ptr<int> p2(p1) instead', 'This is correct shared_ptr usage', 'Use weak_ptr instead', 'Use unique_ptr instead'],
    correctIndex: 0, explanation: 'Constructing shared_ptr from get() creates a second control block. Both shared_ptrs think they own the memory exclusively, causing double-free. Copy the shared_ptr directly: shared_ptr<int> p2(p1).' },
{ id: 'bh-cs-m2019', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'shared_ptr<int> p1 = make_shared<int>(42);\nshared_ptr<int> p2(p1.get());',
    options: ['p2 created from raw pointer — when both shared_ptrs are destroyed, double-free occurs. Use shared_ptr<int> p2(p1) instead', 'This is correct shared_ptr usage', 'Use weak_ptr instead', 'Use unique_ptr instead'],
    correctIndex: 0, explanation: 'Constructing shared_ptr from get() creates a second control block. Both shared_ptrs think they own the memory exclusively, causing double-free. Copy the shared_ptr directly: shared_ptr<int> p2(p1).' },
{ id: 'bh-cs-m2020', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'shared_ptr<int> p1 = make_shared<int>(42);\nshared_ptr<int> p2(p1.get());',
    options: ['p2 created from raw pointer — when both shared_ptrs are destroyed, double-free occurs. Use shared_ptr<int> p2(p1) instead', 'This is correct shared_ptr usage', 'Use weak_ptr instead', 'Use unique_ptr instead'],
    correctIndex: 0, explanation: 'Constructing shared_ptr from get() creates a second control block. Both shared_ptrs think they own the memory exclusively, causing double-free. Copy the shared_ptr directly: shared_ptr<int> p2(p1).' },
{ id: 'bh-cs-m2021', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'shared_ptr<int> p1 = make_shared<int>(42);\nshared_ptr<int> p2(p1.get());',
    options: ['p2 created from raw pointer — when both shared_ptrs are destroyed, double-free occurs. Use shared_ptr<int> p2(p1) instead', 'This is correct shared_ptr usage', 'Use weak_ptr instead', 'Use unique_ptr instead'],
    correctIndex: 0, explanation: 'Constructing shared_ptr from get() creates a second control block. Both shared_ptrs think they own the memory exclusively, causing double-free. Copy the shared_ptr directly: shared_ptr<int> p2(p1).' },
{ id: 'bh-cs-m2022', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'shared_ptr<int> p1 = make_shared<int>(42);\nshared_ptr<int> p2(p1.get());',
    options: ['p2 created from raw pointer — when both shared_ptrs are destroyed, double-free occurs. Use shared_ptr<int> p2(p1) instead', 'This is correct shared_ptr usage', 'Use weak_ptr instead', 'Use unique_ptr instead'],
    correctIndex: 0, explanation: 'Constructing shared_ptr from get() creates a second control block. Both shared_ptrs think they own the memory exclusively, causing double-free. Copy the shared_ptr directly: shared_ptr<int> p2(p1).' },
{ id: 'bh-cs-m2023', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'shared_ptr<int> p1 = make_shared<int>(42);\nshared_ptr<int> p2(p1.get());',
    options: ['p2 created from raw pointer — when both shared_ptrs are destroyed, double-free occurs. Use shared_ptr<int> p2(p1) instead', 'This is correct shared_ptr usage', 'Use weak_ptr instead', 'Use unique_ptr instead'],
    correctIndex: 0, explanation: 'Constructing shared_ptr from get() creates a second control block. Both shared_ptrs think they own the memory exclusively, causing double-free. Copy the shared_ptr directly: shared_ptr<int> p2(p1).' },
{ id: 'bh-cs-m2024', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'class A {\n    int x;\npublic:\n    A(int val) : x(val) {}\n    A operator+(A other) {\n        A result(x + other.x);\n        return result;\n    }\n};',
    options: ['Pass other by const reference: const A& other to avoid unnecessary copy', 'Return by reference instead', 'Add static keyword', 'Make operator+ a friend function'],
    correctIndex: 0, explanation: 'Passing by value copies the entire object. Use const A& other to avoid the copy, which is the standard practice for binary operators.' },
{ id: 'bh-cs-m2025', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'class A {\n    int x;\npublic:\n    A(int val) : x(val) {}\n    A operator+(A other) {\n        A result(x + other.x);\n        return result;\n    }\n};',
    options: ['Pass other by const reference: const A& other to avoid unnecessary copy', 'Return by reference instead', 'Add static keyword', 'Make operator+ a friend function'],
    correctIndex: 0, explanation: 'Passing by value copies the entire object. Use const A& other to avoid the copy, which is the standard practice for binary operators.' },
{ id: 'bh-cs-m2026', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'class A {\n    int x;\npublic:\n    A(int val) : x(val) {}\n    A operator+(A other) {\n        A result(x + other.x);\n        return result;\n    }\n};',
    options: ['Pass other by const reference: const A& other to avoid unnecessary copy', 'Return by reference instead', 'Add static keyword', 'Make operator+ a friend function'],
    correctIndex: 0, explanation: 'Passing by value copies the entire object. Use const A& other to avoid the copy, which is the standard practice for binary operators.' },
{ id: 'bh-cs-m2027', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'class A {\n    int x;\npublic:\n    A(int val) : x(val) {}\n    A operator+(A other) {\n        A result(x + other.x);\n        return result;\n    }\n};',
    options: ['Pass other by const reference: const A& other to avoid unnecessary copy', 'Return by reference instead', 'Add static keyword', 'Make operator+ a friend function'],
    correctIndex: 0, explanation: 'Passing by value copies the entire object. Use const A& other to avoid the copy, which is the standard practice for binary operators.' },
{ id: 'bh-cs-m2028', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'class A {\n    int x;\npublic:\n    A(int val) : x(val) {}\n    A operator+(A other) {\n        A result(x + other.x);\n        return result;\n    }\n};',
    options: ['Pass other by const reference: const A& other to avoid unnecessary copy', 'Return by reference instead', 'Add static keyword', 'Make operator+ a friend function'],
    correctIndex: 0, explanation: 'Passing by value copies the entire object. Use const A& other to avoid the copy, which is the standard practice for binary operators.' },
{ id: 'bh-cs-m2029', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'class A {\n    int x;\npublic:\n    A(int val) : x(val) {}\n    A operator+(A other) {\n        A result(x + other.x);\n        return result;\n    }\n};',
    options: ['Pass other by const reference: const A& other to avoid unnecessary copy', 'Return by reference instead', 'Add static keyword', 'Make operator+ a friend function'],
    correctIndex: 0, explanation: 'Passing by value copies the entire object. Use const A& other to avoid the copy, which is the standard practice for binary operators.' },
{ id: 'bh-cs-m2030', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'class A {\n    int x;\npublic:\n    A(int val) : x(val) {}\n    A operator+(A other) {\n        A result(x + other.x);\n        return result;\n    }\n};',
    options: ['Pass other by const reference: const A& other to avoid unnecessary copy', 'Return by reference instead', 'Add static keyword', 'Make operator+ a friend function'],
    correctIndex: 0, explanation: 'Passing by value copies the entire object. Use const A& other to avoid the copy, which is the standard practice for binary operators.' },
{ id: 'bh-cs-m2031', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'auto f = []() { return 42; };\nauto g = []() { return 42; };\nvector<function<int()>> funcs = {f, g};',
    options: ['Each lambda has a unique type — must use std::function or type erasure to store them', 'This is correct', 'Use auto for vector', 'Make lambdas static'],
    correctIndex: 0, explanation: 'While this code works with std::function, it is worth knowing that each lambda has a unique unnamed type. Direct use of auto for the vector would not work — std::function<int()> provides the needed type erasure.' },
{ id: 'bh-cs-m2032', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'auto f = []() { return 42; };\nauto g = []() { return 42; };\nvector<function<int()>> funcs = {f, g};',
    options: ['Each lambda has a unique type — must use std::function or type erasure to store them', 'This is correct', 'Use auto for vector', 'Make lambdas static'],
    correctIndex: 0, explanation: 'While this code works with std::function, it is worth knowing that each lambda has a unique unnamed type. Direct use of auto for the vector would not work — std::function<int()> provides the needed type erasure.' },
{ id: 'bh-cs-m2033', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'auto f = []() { return 42; };\nauto g = []() { return 42; };\nvector<function<int()>> funcs = {f, g};',
    options: ['Each lambda has a unique type — must use std::function or type erasure to store them', 'This is correct', 'Use auto for vector', 'Make lambdas static'],
    correctIndex: 0, explanation: 'While this code works with std::function, it is worth knowing that each lambda has a unique unnamed type. Direct use of auto for the vector would not work — std::function<int()> provides the needed type erasure.' },
{ id: 'bh-cs-m2034', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'auto f = []() { return 42; };\nauto g = []() { return 42; };\nvector<function<int()>> funcs = {f, g};',
    options: ['Each lambda has a unique type — must use std::function or type erasure to store them', 'This is correct', 'Use auto for vector', 'Make lambdas static'],
    correctIndex: 0, explanation: 'While this code works with std::function, it is worth knowing that each lambda has a unique unnamed type. Direct use of auto for the vector would not work — std::function<int()> provides the needed type erasure.' },
{ id: 'bh-cs-m2035', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'auto f = []() { return 42; };\nauto g = []() { return 42; };\nvector<function<int()>> funcs = {f, g};',
    options: ['Each lambda has a unique type — must use std::function or type erasure to store them', 'This is correct', 'Use auto for vector', 'Make lambdas static'],
    correctIndex: 0, explanation: 'While this code works with std::function, it is worth knowing that each lambda has a unique unnamed type. Direct use of auto for the vector would not work — std::function<int()> provides the needed type erasure.' },
{ id: 'bh-cs-m2036', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'auto f = []() { return 42; };\nauto g = []() { return 42; };\nvector<function<int()>> funcs = {f, g};',
    options: ['Each lambda has a unique type — must use std::function or type erasure to store them', 'This is correct', 'Use auto for vector', 'Make lambdas static'],
    correctIndex: 0, explanation: 'While this code works with std::function, it is worth knowing that each lambda has a unique unnamed type. Direct use of auto for the vector would not work — std::function<int()> provides the needed type erasure.' },
{ id: 'bh-cs-m2037', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'auto f = []() { return 42; };\nauto g = []() { return 42; };\nvector<function<int()>> funcs = {f, g};',
    options: ['Each lambda has a unique type — must use std::function or type erasure to store them', 'This is correct', 'Use auto for vector', 'Make lambdas static'],
    correctIndex: 0, explanation: 'While this code works with std::function, it is worth knowing that each lambda has a unique unnamed type. Direct use of auto for the vector would not work — std::function<int()> provides the needed type erasure.' },
{ id: 'bh-cs-m2038', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'void printAll(const vector<string>& items) {\n    for (int i = 0; i < items.size(); i++) {\n        cout << items[i];\n    }\n}',
    options: ['Comparison between signed int and unsigned size_t — use size_t i or for (const auto& item : items)', 'This is correct', 'Use int(items.size()) for comparison', 'Change to while loop'],
    correctIndex: 0, explanation: 'items.size() returns size_t (unsigned). Comparing int i < size_t triggers a signed/unsigned comparison warning. Use size_t i or a range-based for loop.' },
{ id: 'bh-cs-m2039', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'void printAll(const vector<string>& items) {\n    for (int i = 0; i < items.size(); i++) {\n        cout << items[i];\n    }\n}',
    options: ['Comparison between signed int and unsigned size_t — use size_t i or for (const auto& item : items)', 'This is correct', 'Use int(items.size()) for comparison', 'Change to while loop'],
    correctIndex: 0, explanation: 'items.size() returns size_t (unsigned). Comparing int i < size_t triggers a signed/unsigned comparison warning. Use size_t i or a range-based for loop.' },
{ id: 'bh-cs-m2040', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'void printAll(const vector<string>& items) {\n    for (int i = 0; i < items.size(); i++) {\n        cout << items[i];\n    }\n}',
    options: ['Comparison between signed int and unsigned size_t — use size_t i or for (const auto& item : items)', 'This is correct', 'Use int(items.size()) for comparison', 'Change to while loop'],
    correctIndex: 0, explanation: 'items.size() returns size_t (unsigned). Comparing int i < size_t triggers a signed/unsigned comparison warning. Use size_t i or a range-based for loop.' },
{ id: 'bh-cs-m2041', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'void printAll(const vector<string>& items) {\n    for (int i = 0; i < items.size(); i++) {\n        cout << items[i];\n    }\n}',
    options: ['Comparison between signed int and unsigned size_t — use size_t i or for (const auto& item : items)', 'This is correct', 'Use int(items.size()) for comparison', 'Change to while loop'],
    correctIndex: 0, explanation: 'items.size() returns size_t (unsigned). Comparing int i < size_t triggers a signed/unsigned comparison warning. Use size_t i or a range-based for loop.' },
{ id: 'bh-cs-m2042', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'void printAll(const vector<string>& items) {\n    for (int i = 0; i < items.size(); i++) {\n        cout << items[i];\n    }\n}',
    options: ['Comparison between signed int and unsigned size_t — use size_t i or for (const auto& item : items)', 'This is correct', 'Use int(items.size()) for comparison', 'Change to while loop'],
    correctIndex: 0, explanation: 'items.size() returns size_t (unsigned). Comparing int i < size_t triggers a signed/unsigned comparison warning. Use size_t i or a range-based for loop.' },
{ id: 'bh-cs-m2043', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'void printAll(const vector<string>& items) {\n    for (int i = 0; i < items.size(); i++) {\n        cout << items[i];\n    }\n}',
    options: ['Comparison between signed int and unsigned size_t — use size_t i or for (const auto& item : items)', 'This is correct', 'Use int(items.size()) for comparison', 'Change to while loop'],
    correctIndex: 0, explanation: 'items.size() returns size_t (unsigned). Comparing int i < size_t triggers a signed/unsigned comparison warning. Use size_t i or a range-based for loop.' },
{ id: 'bh-cs-m2044', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'void printAll(const vector<string>& items) {\n    for (int i = 0; i < items.size(); i++) {\n        cout << items[i];\n    }\n}',
    options: ['Comparison between signed int and unsigned size_t — use size_t i or for (const auto& item : items)', 'This is correct', 'Use int(items.size()) for comparison', 'Change to while loop'],
    correctIndex: 0, explanation: 'items.size() returns size_t (unsigned). Comparing int i < size_t triggers a signed/unsigned comparison warning. Use size_t i or a range-based for loop.' },
{ id: 'bh-cs-m2045', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'class MutexGuard {\n    mutex& mtx;\npublic:\n    MutexGuard(mutex& m) : mtx(m) { mtx.lock(); }\n    ~MutexGuard() { mtx.unlock(); }\n};',
    options: ['Missing copy/move prevention — delete copy constructor and assignment operator, or use lock_guard', 'This is correct RAII', 'Add try-catch in constructor', 'Use recursive_mutex instead'],
    correctIndex: 0, explanation: 'If the guard is accidentally copied, both copies try to unlock the same mutex. Delete copy operations: MutexGuard(const MutexGuard&) = delete; or just use std::lock_guard.' },
{ id: 'bh-cs-m2046', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'class MutexGuard {\n    mutex& mtx;\npublic:\n    MutexGuard(mutex& m) : mtx(m) { mtx.lock(); }\n    ~MutexGuard() { mtx.unlock(); }\n};',
    options: ['Missing copy/move prevention — delete copy constructor and assignment operator, or use lock_guard', 'This is correct RAII', 'Add try-catch in constructor', 'Use recursive_mutex instead'],
    correctIndex: 0, explanation: 'If the guard is accidentally copied, both copies try to unlock the same mutex. Delete copy operations: MutexGuard(const MutexGuard&) = delete; or just use std::lock_guard.' },
{ id: 'bh-cs-m2047', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'class MutexGuard {\n    mutex& mtx;\npublic:\n    MutexGuard(mutex& m) : mtx(m) { mtx.lock(); }\n    ~MutexGuard() { mtx.unlock(); }\n};',
    options: ['Missing copy/move prevention — delete copy constructor and assignment operator, or use lock_guard', 'This is correct RAII', 'Add try-catch in constructor', 'Use recursive_mutex instead'],
    correctIndex: 0, explanation: 'If the guard is accidentally copied, both copies try to unlock the same mutex. Delete copy operations: MutexGuard(const MutexGuard&) = delete; or just use std::lock_guard.' },
{ id: 'bh-cs-m2048', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'class MutexGuard {\n    mutex& mtx;\npublic:\n    MutexGuard(mutex& m) : mtx(m) { mtx.lock(); }\n    ~MutexGuard() { mtx.unlock(); }\n};',
    options: ['Missing copy/move prevention — delete copy constructor and assignment operator, or use lock_guard', 'This is correct RAII', 'Add try-catch in constructor', 'Use recursive_mutex instead'],
    correctIndex: 0, explanation: 'If the guard is accidentally copied, both copies try to unlock the same mutex. Delete copy operations: MutexGuard(const MutexGuard&) = delete; or just use std::lock_guard.' },
{ id: 'bh-cs-m2049', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'class MutexGuard {\n    mutex& mtx;\npublic:\n    MutexGuard(mutex& m) : mtx(m) { mtx.lock(); }\n    ~MutexGuard() { mtx.unlock(); }\n};',
    options: ['Missing copy/move prevention — delete copy constructor and assignment operator, or use lock_guard', 'This is correct RAII', 'Add try-catch in constructor', 'Use recursive_mutex instead'],
    correctIndex: 0, explanation: 'If the guard is accidentally copied, both copies try to unlock the same mutex. Delete copy operations: MutexGuard(const MutexGuard&) = delete; or just use std::lock_guard.' },
{ id: 'bh-cs-m2050', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'class MutexGuard {\n    mutex& mtx;\npublic:\n    MutexGuard(mutex& m) : mtx(m) { mtx.lock(); }\n    ~MutexGuard() { mtx.unlock(); }\n};',
    options: ['Missing copy/move prevention — delete copy constructor and assignment operator, or use lock_guard', 'This is correct RAII', 'Add try-catch in constructor', 'Use recursive_mutex instead'],
    correctIndex: 0, explanation: 'If the guard is accidentally copied, both copies try to unlock the same mutex. Delete copy operations: MutexGuard(const MutexGuard&) = delete; or just use std::lock_guard.' },
{ id: 'bh-cs-m2051', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'class MutexGuard {\n    mutex& mtx;\npublic:\n    MutexGuard(mutex& m) : mtx(m) { mtx.lock(); }\n    ~MutexGuard() { mtx.unlock(); }\n};',
    options: ['Missing copy/move prevention — delete copy constructor and assignment operator, or use lock_guard', 'This is correct RAII', 'Add try-catch in constructor', 'Use recursive_mutex instead'],
    correctIndex: 0, explanation: 'If the guard is accidentally copied, both copies try to unlock the same mutex. Delete copy operations: MutexGuard(const MutexGuard&) = delete; or just use std::lock_guard.' },
{ id: 'bh-cs-m2052', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'map<string, int> counts;\nfor (auto& [key, value] : counts) {\n    if (value < 5) counts.erase(key);\n}',
    options: ['Cannot erase from map while iterating — save keys and erase after, or use it = counts.erase(it)', 'This is correct', 'Use unordered_map instead', 'Use remove_if algorithm'],
    correctIndex: 0, explanation: 'Erasing from a map while iterating with range-based for invalidates the iterator. Use a traditional loop: for (auto it = counts.begin(); it != counts.end();) { if (it->second < 5) it = counts.erase(it); else ++it; }' },
{ id: 'bh-cs-m2053', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'map<string, int> counts;\nfor (auto& [key, value] : counts) {\n    if (value < 5) counts.erase(key);\n}',
    options: ['Cannot erase from map while iterating — save keys and erase after, or use it = counts.erase(it)', 'This is correct', 'Use unordered_map instead', 'Use remove_if algorithm'],
    correctIndex: 0, explanation: 'Erasing from a map while iterating with range-based for invalidates the iterator. Use a traditional loop: for (auto it = counts.begin(); it != counts.end();) { if (it->second < 5) it = counts.erase(it); else ++it; }' },
{ id: 'bh-cs-m2054', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'map<string, int> counts;\nfor (auto& [key, value] : counts) {\n    if (value < 5) counts.erase(key);\n}',
    options: ['Cannot erase from map while iterating — save keys and erase after, or use it = counts.erase(it)', 'This is correct', 'Use unordered_map instead', 'Use remove_if algorithm'],
    correctIndex: 0, explanation: 'Erasing from a map while iterating with range-based for invalidates the iterator. Use a traditional loop: for (auto it = counts.begin(); it != counts.end();) { if (it->second < 5) it = counts.erase(it); else ++it; }' },
{ id: 'bh-cs-m2055', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'map<string, int> counts;\nfor (auto& [key, value] : counts) {\n    if (value < 5) counts.erase(key);\n}',
    options: ['Cannot erase from map while iterating — save keys and erase after, or use it = counts.erase(it)', 'This is correct', 'Use unordered_map instead', 'Use remove_if algorithm'],
    correctIndex: 0, explanation: 'Erasing from a map while iterating with range-based for invalidates the iterator. Use a traditional loop: for (auto it = counts.begin(); it != counts.end();) { if (it->second < 5) it = counts.erase(it); else ++it; }' },
{ id: 'bh-cs-m2056', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'map<string, int> counts;\nfor (auto& [key, value] : counts) {\n    if (value < 5) counts.erase(key);\n}',
    options: ['Cannot erase from map while iterating — save keys and erase after, or use it = counts.erase(it)', 'This is correct', 'Use unordered_map instead', 'Use remove_if algorithm'],
    correctIndex: 0, explanation: 'Erasing from a map while iterating with range-based for invalidates the iterator. Use a traditional loop: for (auto it = counts.begin(); it != counts.end();) { if (it->second < 5) it = counts.erase(it); else ++it; }' },
{ id: 'bh-cs-m2057', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'map<string, int> counts;\nfor (auto& [key, value] : counts) {\n    if (value < 5) counts.erase(key);\n}',
    options: ['Cannot erase from map while iterating — save keys and erase after, or use it = counts.erase(it)', 'This is correct', 'Use unordered_map instead', 'Use remove_if algorithm'],
    correctIndex: 0, explanation: 'Erasing from a map while iterating with range-based for invalidates the iterator. Use a traditional loop: for (auto it = counts.begin(); it != counts.end();) { if (it->second < 5) it = counts.erase(it); else ++it; }' },
{ id: 'bh-cs-m2058', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'map<string, int> counts;\nfor (auto& [key, value] : counts) {\n    if (value < 5) counts.erase(key);\n}',
    options: ['Cannot erase from map while iterating — save keys and erase after, or use it = counts.erase(it)', 'This is correct', 'Use unordered_map instead', 'Use remove_if algorithm'],
    correctIndex: 0, explanation: 'Erasing from a map while iterating with range-based for invalidates the iterator. Use a traditional loop: for (auto it = counts.begin(); it != counts.end();) { if (it->second < 5) it = counts.erase(it); else ++it; }' },
{ id: 'bh-cs-m2059', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'class Resource {\n    int* data;\npublic:\n    Resource(int n) : data(new int[n]) {}\n    ~Resource() { delete data; }\n};',
    options: ['Should use delete[] data since allocated with new[] — delete only deletes one element', 'This is correct', 'Use free() instead', 'Add null check before delete'],
    correctIndex: 0, explanation: 'new int[n] allocates an array and must be freed with delete[] data, not delete data. Using delete on array allocation causes undefined behavior.' },
{ id: 'bh-cs-m2060', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'class Resource {\n    int* data;\npublic:\n    Resource(int n) : data(new int[n]) {}\n    ~Resource() { delete data; }\n};',
    options: ['Should use delete[] data since allocated with new[] — delete only deletes one element', 'This is correct', 'Use free() instead', 'Add null check before delete'],
    correctIndex: 0, explanation: 'new int[n] allocates an array and must be freed with delete[] data, not delete data. Using delete on array allocation causes undefined behavior.' },
{ id: 'bh-cs-m2061', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'class Resource {\n    int* data;\npublic:\n    Resource(int n) : data(new int[n]) {}\n    ~Resource() { delete data; }\n};',
    options: ['Should use delete[] data since allocated with new[] — delete only deletes one element', 'This is correct', 'Use free() instead', 'Add null check before delete'],
    correctIndex: 0, explanation: 'new int[n] allocates an array and must be freed with delete[] data, not delete data. Using delete on array allocation causes undefined behavior.' },
{ id: 'bh-cs-m2062', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'class Resource {\n    int* data;\npublic:\n    Resource(int n) : data(new int[n]) {}\n    ~Resource() { delete data; }\n};',
    options: ['Should use delete[] data since allocated with new[] — delete only deletes one element', 'This is correct', 'Use free() instead', 'Add null check before delete'],
    correctIndex: 0, explanation: 'new int[n] allocates an array and must be freed with delete[] data, not delete data. Using delete on array allocation causes undefined behavior.' },
{ id: 'bh-cs-m2063', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'class Resource {\n    int* data;\npublic:\n    Resource(int n) : data(new int[n]) {}\n    ~Resource() { delete data; }\n};',
    options: ['Should use delete[] data since allocated with new[] — delete only deletes one element', 'This is correct', 'Use free() instead', 'Add null check before delete'],
    correctIndex: 0, explanation: 'new int[n] allocates an array and must be freed with delete[] data, not delete data. Using delete on array allocation causes undefined behavior.' },
{ id: 'bh-cs-m2064', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'class Resource {\n    int* data;\npublic:\n    Resource(int n) : data(new int[n]) {}\n    ~Resource() { delete data; }\n};',
    options: ['Should use delete[] data since allocated with new[] — delete only deletes one element', 'This is correct', 'Use free() instead', 'Add null check before delete'],
    correctIndex: 0, explanation: 'new int[n] allocates an array and must be freed with delete[] data, not delete data. Using delete on array allocation causes undefined behavior.' },
{ id: 'bh-cs-m2065', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'class Resource {\n    int* data;\npublic:\n    Resource(int n) : data(new int[n]) {}\n    ~Resource() { delete data; }\n};',
    options: ['Should use delete[] data since allocated with new[] — delete only deletes one element', 'This is correct', 'Use free() instead', 'Add null check before delete'],
    correctIndex: 0, explanation: 'new int[n] allocates an array and must be freed with delete[] data, not delete data. Using delete on array allocation causes undefined behavior.' },
{ id: 'bh-cs-m2066', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'string getName() {\n    string name = "Alice";\n    return name;\n}\nstring& ref = getName();',
    options: ['Returning reference to temporary — getName() returns a temporary. Use string ref = getName() (by value) or const string&', 'This is correct', 'Use string&& instead', 'Add move semantics'],
    correctIndex: 0, explanation: 'getName() returns a temporary string. Binding it to a non-const reference is invalid (though some compilers allow it as an extension). Use string ref = getName() or const string& ref = getName().' },
{ id: 'bh-cs-m2067', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'string getName() {\n    string name = "Alice";\n    return name;\n}\nstring& ref = getName();',
    options: ['Returning reference to temporary — getName() returns a temporary. Use string ref = getName() (by value) or const string&', 'This is correct', 'Use string&& instead', 'Add move semantics'],
    correctIndex: 0, explanation: 'getName() returns a temporary string. Binding it to a non-const reference is invalid (though some compilers allow it as an extension). Use string ref = getName() or const string& ref = getName().' },
{ id: 'bh-cs-m2068', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'string getName() {\n    string name = "Alice";\n    return name;\n}\nstring& ref = getName();',
    options: ['Returning reference to temporary — getName() returns a temporary. Use string ref = getName() (by value) or const string&', 'This is correct', 'Use string&& instead', 'Add move semantics'],
    correctIndex: 0, explanation: 'getName() returns a temporary string. Binding it to a non-const reference is invalid (though some compilers allow it as an extension). Use string ref = getName() or const string& ref = getName().' },
{ id: 'bh-cs-m2069', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'string getName() {\n    string name = "Alice";\n    return name;\n}\nstring& ref = getName();',
    options: ['Returning reference to temporary — getName() returns a temporary. Use string ref = getName() (by value) or const string&', 'This is correct', 'Use string&& instead', 'Add move semantics'],
    correctIndex: 0, explanation: 'getName() returns a temporary string. Binding it to a non-const reference is invalid (though some compilers allow it as an extension). Use string ref = getName() or const string& ref = getName().' },
{ id: 'bh-cs-m2070', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'string getName() {\n    string name = "Alice";\n    return name;\n}\nstring& ref = getName();',
    options: ['Returning reference to temporary — getName() returns a temporary. Use string ref = getName() (by value) or const string&', 'This is correct', 'Use string&& instead', 'Add move semantics'],
    correctIndex: 0, explanation: 'getName() returns a temporary string. Binding it to a non-const reference is invalid (though some compilers allow it as an extension). Use string ref = getName() or const string& ref = getName().' },
{ id: 'bh-cs-m2071', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'string getName() {\n    string name = "Alice";\n    return name;\n}\nstring& ref = getName();',
    options: ['Returning reference to temporary — getName() returns a temporary. Use string ref = getName() (by value) or const string&', 'This is correct', 'Use string&& instead', 'Add move semantics'],
    correctIndex: 0, explanation: 'getName() returns a temporary string. Binding it to a non-const reference is invalid (though some compilers allow it as an extension). Use string ref = getName() or const string& ref = getName().' },
{ id: 'bh-cs-m2072', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'string getName() {\n    string name = "Alice";\n    return name;\n}\nstring& ref = getName();',
    options: ['Returning reference to temporary — getName() returns a temporary. Use string ref = getName() (by value) or const string&', 'This is correct', 'Use string&& instead', 'Add move semantics'],
    correctIndex: 0, explanation: 'getName() returns a temporary string. Binding it to a non-const reference is invalid (though some compilers allow it as an extension). Use string ref = getName() or const string& ref = getName().' },
{ id: 'bh-cs-m2073', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'template<typename T>\nclass Stack {\n    T items[100];\n    int top = -1;\npublic:\n    void push(T item) { items[++top] = item; }\n    T pop() { return items[top--]; }\n};',
    options: ['No bounds checking — push can overflow and pop on empty stack returns garbage', 'This is a correct stack', 'Use vector instead of array', 'Add isEmpty() and isFull() methods'],
    correctIndex: 0, explanation: 'push() does not check if top < 99, risking buffer overflow. pop() does not check if top >= 0, returning garbage when empty. Add bounds checking or use std::stack.' },
{ id: 'bh-cs-m2074', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'template<typename T>\nclass Stack {\n    T items[100];\n    int top = -1;\npublic:\n    void push(T item) { items[++top] = item; }\n    T pop() { return items[top--]; }\n};',
    options: ['No bounds checking — push can overflow and pop on empty stack returns garbage', 'This is a correct stack', 'Use vector instead of array', 'Add isEmpty() and isFull() methods'],
    correctIndex: 0, explanation: 'push() does not check if top < 99, risking buffer overflow. pop() does not check if top >= 0, returning garbage when empty. Add bounds checking or use std::stack.' },
{ id: 'bh-cs-m2075', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'template<typename T>\nclass Stack {\n    T items[100];\n    int top = -1;\npublic:\n    void push(T item) { items[++top] = item; }\n    T pop() { return items[top--]; }\n};',
    options: ['No bounds checking — push can overflow and pop on empty stack returns garbage', 'This is a correct stack', 'Use vector instead of array', 'Add isEmpty() and isFull() methods'],
    correctIndex: 0, explanation: 'push() does not check if top < 99, risking buffer overflow. pop() does not check if top >= 0, returning garbage when empty. Add bounds checking or use std::stack.' },
{ id: 'bh-cs-m2076', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'template<typename T>\nclass Stack {\n    T items[100];\n    int top = -1;\npublic:\n    void push(T item) { items[++top] = item; }\n    T pop() { return items[top--]; }\n};',
    options: ['No bounds checking — push can overflow and pop on empty stack returns garbage', 'This is a correct stack', 'Use vector instead of array', 'Add isEmpty() and isFull() methods'],
    correctIndex: 0, explanation: 'push() does not check if top < 99, risking buffer overflow. pop() does not check if top >= 0, returning garbage when empty. Add bounds checking or use std::stack.' },
{ id: 'bh-cs-m2077', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'template<typename T>\nclass Stack {\n    T items[100];\n    int top = -1;\npublic:\n    void push(T item) { items[++top] = item; }\n    T pop() { return items[top--]; }\n};',
    options: ['No bounds checking — push can overflow and pop on empty stack returns garbage', 'This is a correct stack', 'Use vector instead of array', 'Add isEmpty() and isFull() methods'],
    correctIndex: 0, explanation: 'push() does not check if top < 99, risking buffer overflow. pop() does not check if top >= 0, returning garbage when empty. Add bounds checking or use std::stack.' },
{ id: 'bh-cs-m2078', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'template<typename T>\nclass Stack {\n    T items[100];\n    int top = -1;\npublic:\n    void push(T item) { items[++top] = item; }\n    T pop() { return items[top--]; }\n};',
    options: ['No bounds checking — push can overflow and pop on empty stack returns garbage', 'This is a correct stack', 'Use vector instead of array', 'Add isEmpty() and isFull() methods'],
    correctIndex: 0, explanation: 'push() does not check if top < 99, risking buffer overflow. pop() does not check if top >= 0, returning garbage when empty. Add bounds checking or use std::stack.' },
{ id: 'bh-cs-m2079', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'template<typename T>\nclass Stack {\n    T items[100];\n    int top = -1;\npublic:\n    void push(T item) { items[++top] = item; }\n    T pop() { return items[top--]; }\n};',
    options: ['No bounds checking — push can overflow and pop on empty stack returns garbage', 'This is a correct stack', 'Use vector instead of array', 'Add isEmpty() and isFull() methods'],
    correctIndex: 0, explanation: 'push() does not check if top < 99, risking buffer overflow. pop() does not check if top >= 0, returning garbage when empty. Add bounds checking or use std::stack.' },
{ id: 'bh-cs-m2080', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'void handleSignal(int sig) {\n    cout << "Signal: " << sig << endl;\n}',
    options: ['cout is not signal-safe — only async-signal-safe functions like write() should be used in signal handlers', 'This is correct signal handling', 'Use printf instead', 'Add volatile to sig'],
    correctIndex: 0, explanation: 'Signal handlers can only call async-signal-safe functions. cout and most C++ I/O are not signal-safe and can cause deadlocks. Use write() or just set a volatile sig_atomic_t flag.' },
{ id: 'bh-cs-m2081', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'void handleSignal(int sig) {\n    cout << "Signal: " << sig << endl;\n}',
    options: ['cout is not signal-safe — only async-signal-safe functions like write() should be used in signal handlers', 'This is correct signal handling', 'Use printf instead', 'Add volatile to sig'],
    correctIndex: 0, explanation: 'Signal handlers can only call async-signal-safe functions. cout and most C++ I/O are not signal-safe and can cause deadlocks. Use write() or just set a volatile sig_atomic_t flag.' },
{ id: 'bh-cs-m2082', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'void handleSignal(int sig) {\n    cout << "Signal: " << sig << endl;\n}',
    options: ['cout is not signal-safe — only async-signal-safe functions like write() should be used in signal handlers', 'This is correct signal handling', 'Use printf instead', 'Add volatile to sig'],
    correctIndex: 0, explanation: 'Signal handlers can only call async-signal-safe functions. cout and most C++ I/O are not signal-safe and can cause deadlocks. Use write() or just set a volatile sig_atomic_t flag.' },
{ id: 'bh-cs-m2083', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'void handleSignal(int sig) {\n    cout << "Signal: " << sig << endl;\n}',
    options: ['cout is not signal-safe — only async-signal-safe functions like write() should be used in signal handlers', 'This is correct signal handling', 'Use printf instead', 'Add volatile to sig'],
    correctIndex: 0, explanation: 'Signal handlers can only call async-signal-safe functions. cout and most C++ I/O are not signal-safe and can cause deadlocks. Use write() or just set a volatile sig_atomic_t flag.' },
{ id: 'bh-cs-m2084', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'void handleSignal(int sig) {\n    cout << "Signal: " << sig << endl;\n}',
    options: ['cout is not signal-safe — only async-signal-safe functions like write() should be used in signal handlers', 'This is correct signal handling', 'Use printf instead', 'Add volatile to sig'],
    correctIndex: 0, explanation: 'Signal handlers can only call async-signal-safe functions. cout and most C++ I/O are not signal-safe and can cause deadlocks. Use write() or just set a volatile sig_atomic_t flag.' },
{ id: 'bh-cs-m2085', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'void handleSignal(int sig) {\n    cout << "Signal: " << sig << endl;\n}',
    options: ['cout is not signal-safe — only async-signal-safe functions like write() should be used in signal handlers', 'This is correct signal handling', 'Use printf instead', 'Add volatile to sig'],
    correctIndex: 0, explanation: 'Signal handlers can only call async-signal-safe functions. cout and most C++ I/O are not signal-safe and can cause deadlocks. Use write() or just set a volatile sig_atomic_t flag.' },
{ id: 'bh-cs-m2086', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'void handleSignal(int sig) {\n    cout << "Signal: " << sig << endl;\n}',
    options: ['cout is not signal-safe — only async-signal-safe functions like write() should be used in signal handlers', 'This is correct signal handling', 'Use printf instead', 'Add volatile to sig'],
    correctIndex: 0, explanation: 'Signal handlers can only call async-signal-safe functions. cout and most C++ I/O are not signal-safe and can cause deadlocks. Use write() or just set a volatile sig_atomic_t flag.' },
{ id: 'bh-cs-m2087', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'unique_ptr<int> p(new int(42));\nunique_ptr<int> q = p;',
    options: ['Cannot copy unique_ptr — use unique_ptr<int> q = std::move(p) to transfer ownership', 'This is correct', 'Use shared_ptr instead', 'Use p.release() first'],
    correctIndex: 0, explanation: 'unique_ptr represents exclusive ownership and cannot be copied. Use std::move(p) to transfer ownership, after which p becomes nullptr and q owns the int.' },
{ id: 'bh-cs-m2088', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'unique_ptr<int> p(new int(42));\nunique_ptr<int> q = p;',
    options: ['Cannot copy unique_ptr — use unique_ptr<int> q = std::move(p) to transfer ownership', 'This is correct', 'Use shared_ptr instead', 'Use p.release() first'],
    correctIndex: 0, explanation: 'unique_ptr represents exclusive ownership and cannot be copied. Use std::move(p) to transfer ownership, after which p becomes nullptr and q owns the int.' },
{ id: 'bh-cs-m2089', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'unique_ptr<int> p(new int(42));\nunique_ptr<int> q = p;',
    options: ['Cannot copy unique_ptr — use unique_ptr<int> q = std::move(p) to transfer ownership', 'This is correct', 'Use shared_ptr instead', 'Use p.release() first'],
    correctIndex: 0, explanation: 'unique_ptr represents exclusive ownership and cannot be copied. Use std::move(p) to transfer ownership, after which p becomes nullptr and q owns the int.' },
{ id: 'bh-cs-m2090', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'unique_ptr<int> p(new int(42));\nunique_ptr<int> q = p;',
    options: ['Cannot copy unique_ptr — use unique_ptr<int> q = std::move(p) to transfer ownership', 'This is correct', 'Use shared_ptr instead', 'Use p.release() first'],
    correctIndex: 0, explanation: 'unique_ptr represents exclusive ownership and cannot be copied. Use std::move(p) to transfer ownership, after which p becomes nullptr and q owns the int.' },
{ id: 'bh-cs-m2091', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'unique_ptr<int> p(new int(42));\nunique_ptr<int> q = p;',
    options: ['Cannot copy unique_ptr — use unique_ptr<int> q = std::move(p) to transfer ownership', 'This is correct', 'Use shared_ptr instead', 'Use p.release() first'],
    correctIndex: 0, explanation: 'unique_ptr represents exclusive ownership and cannot be copied. Use std::move(p) to transfer ownership, after which p becomes nullptr and q owns the int.' },
{ id: 'bh-cs-m2092', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'unique_ptr<int> p(new int(42));\nunique_ptr<int> q = p;',
    options: ['Cannot copy unique_ptr — use unique_ptr<int> q = std::move(p) to transfer ownership', 'This is correct', 'Use shared_ptr instead', 'Use p.release() first'],
    correctIndex: 0, explanation: 'unique_ptr represents exclusive ownership and cannot be copied. Use std::move(p) to transfer ownership, after which p becomes nullptr and q owns the int.' },
{ id: 'bh-cs-m2093', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'unique_ptr<int> p(new int(42));\nunique_ptr<int> q = p;',
    options: ['Cannot copy unique_ptr — use unique_ptr<int> q = std::move(p) to transfer ownership', 'This is correct', 'Use shared_ptr instead', 'Use p.release() first'],
    correctIndex: 0, explanation: 'unique_ptr represents exclusive ownership and cannot be copied. Use std::move(p) to transfer ownership, after which p becomes nullptr and q owns the int.' },
{ id: 'bh-cs-m2094', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'class Base {\npublic:\n    Base() { cout << "Base"; }\n};\nclass Derived : public Base {\npublic:\n    Derived() { cout << "Derived"; }\n};\nBase b = Derived();',
    options: ['Object slicing — b only contains the Base part. Use Base& or Base* for polymorphism', 'This prints "BaseDerived"', 'This prints "DerivedBase"', 'Use virtual constructor'],
    correctIndex: 0, explanation: 'Base b = Derived() slices the Derived object — only the Base portion is copied. The Derived part is lost. Use Base& ref = Derived() or Base* ptr = new Derived() for polymorphism.' },
{ id: 'bh-cs-m2095', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'class Base {\npublic:\n    Base() { cout << "Base"; }\n};\nclass Derived : public Base {\npublic:\n    Derived() { cout << "Derived"; }\n};\nBase b = Derived();',
    options: ['Object slicing — b only contains the Base part. Use Base& or Base* for polymorphism', 'This prints "BaseDerived"', 'This prints "DerivedBase"', 'Use virtual constructor'],
    correctIndex: 0, explanation: 'Base b = Derived() slices the Derived object — only the Base portion is copied. The Derived part is lost. Use Base& ref = Derived() or Base* ptr = new Derived() for polymorphism.' },
{ id: 'bh-cs-m2096', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'class Base {\npublic:\n    Base() { cout << "Base"; }\n};\nclass Derived : public Base {\npublic:\n    Derived() { cout << "Derived"; }\n};\nBase b = Derived();',
    options: ['Object slicing — b only contains the Base part. Use Base& or Base* for polymorphism', 'This prints "BaseDerived"', 'This prints "DerivedBase"', 'Use virtual constructor'],
    correctIndex: 0, explanation: 'Base b = Derived() slices the Derived object — only the Base portion is copied. The Derived part is lost. Use Base& ref = Derived() or Base* ptr = new Derived() for polymorphism.' },
{ id: 'bh-cs-m2097', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'class Base {\npublic:\n    Base() { cout << "Base"; }\n};\nclass Derived : public Base {\npublic:\n    Derived() { cout << "Derived"; }\n};\nBase b = Derived();',
    options: ['Object slicing — b only contains the Base part. Use Base& or Base* for polymorphism', 'This prints "BaseDerived"', 'This prints "DerivedBase"', 'Use virtual constructor'],
    correctIndex: 0, explanation: 'Base b = Derived() slices the Derived object — only the Base portion is copied. The Derived part is lost. Use Base& ref = Derived() or Base* ptr = new Derived() for polymorphism.' },
{ id: 'bh-cs-m2098', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'class Base {\npublic:\n    Base() { cout << "Base"; }\n};\nclass Derived : public Base {\npublic:\n    Derived() { cout << "Derived"; }\n};\nBase b = Derived();',
    options: ['Object slicing — b only contains the Base part. Use Base& or Base* for polymorphism', 'This prints "BaseDerived"', 'This prints "DerivedBase"', 'Use virtual constructor'],
    correctIndex: 0, explanation: 'Base b = Derived() slices the Derived object — only the Base portion is copied. The Derived part is lost. Use Base& ref = Derived() or Base* ptr = new Derived() for polymorphism.' },
{ id: 'bh-cs-m2099', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'class Base {\npublic:\n    Base() { cout << "Base"; }\n};\nclass Derived : public Base {\npublic:\n    Derived() { cout << "Derived"; }\n};\nBase b = Derived();',
    options: ['Object slicing — b only contains the Base part. Use Base& or Base* for polymorphism', 'This prints "BaseDerived"', 'This prints "DerivedBase"', 'Use virtual constructor'],
    correctIndex: 0, explanation: 'Base b = Derived() slices the Derived object — only the Base portion is copied. The Derived part is lost. Use Base& ref = Derived() or Base* ptr = new Derived() for polymorphism.' },
{ id: 'bh-cs-m2100', game: 'bug-hunter', language: 'cpp', difficulty: 'medium',
    code: 'class Base {\npublic:\n    Base() { cout << "Base"; }\n};\nclass Derived : public Base {\npublic:\n    Derived() { cout << "Derived"; }\n};\nBase b = Derived();',
    options: ['Object slicing — b only contains the Base part. Use Base& or Base* for polymorphism', 'This prints "BaseDerived"', 'This prints "DerivedBase"', 'Use virtual constructor'],
    correctIndex: 0, explanation: 'Base b = Derived() slices the Derived object — only the Base portion is copied. The Derived part is lost. Use Base& ref = Derived() or Base* ptr = new Derived() for polymorphism.' },
