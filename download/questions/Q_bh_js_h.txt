{ id: 'bh-js-h2001', game: 'bug-hunter', language: 'javascript', difficulty: 'hard',
    code: 'const obj1 = { a: 1 };\nconst obj2 = { b: 2 };\nconst merged = Object.assign(obj1, obj2);\nobj1.a = 99;\nconsole.log(merged.a);',
    options: ['merged is 99 — Object.assign mutates obj1, merged === obj1', 'merged.a is 1', 'merged.a is undefined', 'This throws an error'],
    correctIndex: 0, explanation: 'Object.assign(obj1, obj2) mutates obj1 and returns it. merged and obj1 reference the same object. Use Object.assign({}, obj1, obj2) or {...obj1, ...obj2} to avoid mutation.' },
{ id: 'bh-js-h2002', game: 'bug-hunter', language: 'javascript', difficulty: 'hard',
    code: 'function createFunctions() {\n  const funcs = [];\n  for (var i = 0; i < 3; i++) {\n    funcs.push(function() { return i; });\n  }\n  return funcs;\n}\nconst fns = createFunctions();\nconsole.log(fns[0]());',
    options: ['fns[0]() returns 3 — all closures share the same var i. Use let or IIFE', 'Returns 0', 'Returns undefined', 'Throws ReferenceError'],
    correctIndex: 0, explanation: 'With var, all closures reference the same i which is 3 after the loop ends. Use let i for block scope, or wrap in an IIFE: (function(j) { return j; })(i).' },
{ id: 'bh-js-h2003', game: 'bug-hunter', language: 'javascript', difficulty: 'hard',
    code: 'const arr = [1, 2, 3];\narr.forEach((item, index) => {\n  if (item === 2) {\n    arr.splice(index, 1);\n  }\n});\nconsole.log(arr);',
    options: ['Mutating array during forEach skips elements — use filter instead', 'This works correctly', 'Use for...of instead', 'Use arr.remove(2)'],
    correctIndex: 0, explanation: 'splice() during forEach shifts indices, causing the next element to be skipped. Use arr.filter(item => item !== 2) to create a new array without mutation.' },
{ id: 'bh-js-h2004', game: 'bug-hunter', language: 'javascript', difficulty: 'hard',
    code: 'class EventManager {\n  constructor() {\n    this.handlers = {};\n  }\n  on(event, handler) {\n    this.handlers[event] = handler;\n  }\n  emit(event, ...args) {\n    this.handlers[event](...args);\n  }\n}\n\nconst em = new EventManager();\nem.emit("click", "button1");',
    options: ['emit crashes on undefined handler — check if handlers[event] exists', 'Use addEventListener instead', 'on() should push to an array', 'Both A and C — support multiple handlers'],
    correctIndex: 3, explanation: 'Calling emit for an unregistered event throws TypeError since handlers[event] is undefined. Also, overwriting handlers[event] supports only one handler per event. Use an array of handlers.' },
